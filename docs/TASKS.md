# Tasks

This document is the working task queue for the repository.

Its purpose is to tell a human or AI agent what should be worked on next, what is currently active, what is blocked, and what completion looks like.

This file should be kept current.

---

## How to use this file

1. Start with the **Active Task** section.
2. If there is no active task, pick the top unblocked item from **Priority Queue**.
3. Before implementation, confirm:
   - scope
   - affected modules/layers
   - expected tests
   - documentation impact
4. After completion:
   - **update sub-task status in this file immediately** (mark the sub-task done, e.g. `[x]` or move to completed list) so other IDEs and agents know where to take over
   - move the task to Completed when the whole phase/task is done
   - update follow-ups if needed
   - update `docs/STATE.md` if current reality changed

Do not start multiple unrelated tasks at once unless explicitly instructed.

---

## Active Task

**Phase 11 v2.1 follow-up release — Trace-mode entry-method discoverability** is the next active item (in progress 2026-05-05; ratified in v2.1.0 docs-only planning-checkpoint commit). Addresses the operator-friction signal "the Smali entry-method input in Lab → Trace is unintuitive for non-advanced users" via three coherent deliverables landed across a strictly-linear 7-sub-step backlog (v2.1.0 → v2.1.6, mirrors Phase 11's 11.0 → 11.7 cadence): (1) **Deterministic UX backbone** — `ClassMethodTree` browser embedded in Trace mode (Q1: A1 — collapsible "Browse classes" section above the entry form, default-collapsed; Q2: A — reuse existing App/Libs default toggles; Q3: A — click-to-seed Smali class-prefix → activates `MethodPicker` for overload confirmation → auto-fires trace; Q4: B — hide raw Smali input field behind an "Advanced: type Smali signature directly" toggle, keep `Hops` field always visible inline). (2) **Backend coalescer + spinner** — new `POST /api/trace/{app_id}/normalise-entry` endpoint translating dotted input (`com.example.Foo.onClick`) → Smali class-prefix (`Lcom/example/Foo;->onClick(`) + validating against the call graph (Q5: A — translate class+method only, MethodPicker handles overloads; Q6: B — backend coalescer with class_exists_in_graph + method_count, frontend debounced 400ms with inline spinner + ✓/⚠ pill). (3) **Three-tier LLM integration** — Tier 1 "Find similar classes" button on validation failure (LLM-backed candidates as suggestion pills); Tier 2(a) "Ask AI" button opening chat dock with pre-filled prompt template (leverages existing `search_decompiled_sources` + `query_call_graph` skills via the new `pendingChatPrefill` plumbing in `WorkbenchContext` mirroring existing `pendingTraceEntry` / `pendingHookPrefill` patterns); Tier 3 — new `androscan/skills/suggest_trace_entry.py` skill (RAG over decompiled sources + call-graph context + LLM ranking) **surfaced via the existing chat dock** (operator's preferred UI per Q3 Tier-3 feedback — chat handles natural-language reasoning + selection buttons + follow-up conversation; no new dedicated textarea in Trace mode), with a **new structured-output extension to the skill response schema** (Q7: ii — extend skill response with optional `widgets[]` block alongside `text`; chat renderer detects and renders specialized React components per `kind`; new `<TraceEntryCandidateWidget>` handles candidate selection with auto-fire on click via Q8: a — same pattern as Inspect → Trace seed). **Architectural pattern lock:** Q7 (ii) introduces the first instance of LLM-emitted interactive widgets in chat (DEC-022 cross-link) — pattern is deliberately extensible so future skills (e.g. Hook Builder template suggestions, Inspect "did you mean this UI element?") can reuse the same `widgets[]` schema + render seam without re-inventing the chat-render contract. **DEC-025 stays Active** (no new DEC for v2.1 per Q10: α — still in v2 family; closing-note extended in v2.1.0 with the v2.1 ratification block + Q7-Q11 lock-in record). See § **Phase 11 v2.1 follow-up release — sub-step backlog** below for the strictly-linear sub-step table + DEC-025's v2.1 closing-note extension for the architectural decisions.

**Phase 12 — Behavior Trace v3 / cross-tab follow-ups planning checkpoint (12.0)** is **deferred-after-v2.1** (was the active item before v2.1 was ratified on 2026-05-05 — same posture as the post-DEC-024 MethodPicker shipping before Phase 11 started; Phase 12 / DEC-026 ratification still operator-demand-gated and depends on real-app telemetry from Phase 11 v2 + the new v2.1 entry-method discoverability + chat-widget pattern). **DEC-026 to be ratified as 12.0** picking the operator-demand-gated v3 candidates from DEC-025's deferral list — same workflow shape as the DEC-024 → DEC-025 transition (v1 closing note + v2 follow-up DEC ratified in the same sweep). **v3 candidate menu** (carried forward verbatim from DEC-025's "Explicitly deferred to v3 / Phase 12" list — capture as a v3 follow-up DEC when telemetry justifies one): ISSUE-014 (per-overload precision on two-register comparisons — schema bump on `DecisionPoint.predicate_origin` from a single field to a tuple; doubles per-anchor card count for two-register-heavy methods, so ships with a UI density review); auto-verify bypass after injection (Mirror programmatically re-triggers anchor + diffs UI state — large Mirror integration cost; defers until measured demand); free-form LLM JS for bypass plans via the existing `generate_frida_hook` consent class (Phase 11 v2's three locked templates `force_return_value` / `force_method_skip` / `force_string_compare_equal` need to leave a measurable gap on real apps before this is worth doing); per-app TTL on cached anchors (current invalidation is "drop the file" + the schema-bump-driven wipe from DEC-025 — operator hasn't asked); iOS / WebView / native-binary `BehaviorAnchor` adapters (DEC-024's platform-neutral data model is observably held in v1 + v2; operator demand is the gate); Smali patching as a bypass technique (different workflow class — apktool re-encode → `jarsigner` → install + verify; deserves its own design conversation); LLM-budget real-app measurement follow-up (carried over from 11.6 / 11.7 Q1 (A) deferral — measure actual prompt + output token usage on dogfood-app traces freshly rebuilt under v2 slicer; revert + tighten `_format_decision_for_llm` if qwen3.5:35b runtime can't use the larger `num_ctx`). **Brief Ask-mode planning checkpoint at the top of 12.0** to pick the v3 scope before any code lands (mirrors the 11.0 / 10.0 / v2.1.0 planning-checkpoint pattern).

**Phase 11 — Behavior Trace v2 complete (2026-04-30)** (Lab tab — bounded inter-procedural slicing + operator-UX polish). All sub-steps 11.0 → 11.7 landed across two calendar days; **+26 new tests** end-to-end (`tests/test_bypass_planner.py` +6 + `tests/test_decisions_slicing.py` +35 across 11.4 / 11.5 / 11.6 + `tests/test_config.py` +14 + `tests/test_trace_routes.py` +7); **ISSUE-013 → Resolved (Phase 11 v2)** against the v1-vs-v2 corpus regression-floor measurement test in `tests/test_decisions_slicing.py`; LLM-budget bumps shipped (`num_ctx: 16384` + `num_predict: 12288`) but real-app token-usage measurement deferred per Q1 (A) of the 11.7 planning checkpoint. Sub-steps **11.0 done 2026-04-30** (DEC-025 ratified — docs only), **11.1 done 2026-04-30** (Tier 1 (a) decision-timeline UX clarity polish — frontend-only on `DecisionTimeline.tsx` + `App.css`), **11.2 done 2026-04-30** (Tier 1 (b) "Trace this method" on call-graph nodes — frontend-only on `CallGraphView.tsx`), **11.3 done 2026-04-30** (Tier 1 (c) `BehaviorAnchor`-aware overlay on Manual Hooks Cytoscape pane — new route `GET /api/trace/{app_id}/anchored-methods` + frontend overlay layer + 7 backend tests), **11.4 done 2026-04-30** (Tier 2 (d) part 1 — bounded inter-procedural slicer descent + type-driven `is_stateless` analyzer — slicer +~330 LOC over the v1 base + new module constants `MAX_SLICE_DEPTH = 2` + `HARD_CAP_DEPTH = 4` + `_DescentBudget` + `_STATELESS_LIB_DENYLIST` + 27 new tests), **11.5 done 2026-04-30** (Tier 2 (d) part 2 — same-class field-write-site walking — slicer +~280 LOC + `_walk_field_write_sites` walker + constructor-priority rule + 8 new tests), **11.6 done 2026-04-30** (bypass-planner re-run + LLM-tier `trace_behavior` budget revisit + cache schema bump + ISSUE-013 close-out — slicer ~60 LOC delta for `descent_depth` tagging + `trace.sqlite` `SCHEMA_VERSION` 1 → 2 + two new config knobs `ollama_num_ctx` + `trace_max_slice_depth` + LLM client `num_ctx` forwarding + frontend depth-pill UI + 26 new tests), and **11.7 done 2026-04-30** (docs sweep + DEC-025 closing note — this commit; six docs files modified, no code touched). See § **Phase 11 — Behavior Trace v2 — sub-step backlog** below for the as-built record + `docs/STATE.md` Phase 11 sub-bullets + DEC-025's v2 closing note (DEC-025 stays Active for the v3 / Phase 12 follow-ups it explicitly defers — ISSUE-014, auto-verify, free-form LLM JS, per-app TTL, iOS adapters, Smali patching).

**Parallel non-blocking thread (currently deprioritized):** **CI.0 — Phase 4 CI** (pytest on push/PR; long-deferred from the original Phase 4 backlog). Originally promoted to Active Task by DEC-025 in service of the Phase 11 slicer changes, but ultimately shipped *after* Phase 11 v2 (the suite passed locally throughout 11.x, and operator's "Lab engine before CI" direction (2026-04-30) deprioritized it relative to v3 work). **Currently deprioritized** — promote when Lab engine reaches a stable v3 baseline. Standalone Active Task line — not coupled to the 12.x sub-step ordering. See § Phase 4 CI — sub-step backlog below.

**Phase 10 — Behavior Trace v1 complete (2026-04-29)** (Lab tab gate-identification + bypass-planning mode). All sub-steps 10.0 → 10.8 landed; see § Phase 10 sub-bullets in `docs/STATE.md` for the as-built record + ~104 new tests across the static + skill + REST + frontend + chat layers; DEC-024 stays Active for the cross-platform follow-ups it explicitly defers to v2 (iOS / WebView / native binary mirroring). Sub-steps **10.0 done 2026-04-28** (DEC-024 ratified), **10.1 done 2026-04-28** (decision-point extraction + platform-neutral data model — `androscan/analysis/{trace_types,decisions}.py` + 13 tests), **10.2 done 2026-04-28** (intra-procedural backward slicer + 5 `*Origin` variants — `androscan/analysis/slicing.py` + 16 tests), **10.3 done 2026-04-29** (heuristic branch-outcome classifier with locked verdict thresholds + 4-tier confidence — `androscan/analysis/branch_classifier.py` + `BranchVerdict` / `BranchOutcome` extensions to `trace_types.py` + 19 tests), **10.4 done 2026-04-29** (template-bound bypass planner with three new override Frida templates + risk-threshold filtering + `trace_bypass_risk_max` config knob — `androscan/analysis/bypass_planner.py` + `force_return_value` / `force_method_skip` / `force_string_compare_equal` templates + `BypassPlan` back-reference field extensions + 29 planner tests + 3 new template smoke renders + 21 free parametrized fail-closed walks), **10.5 done 2026-04-29** (`trace_behavior` LLM-tier skill — new `androscan/skills/trace_behavior.py` registered in `_SKILL_MODULES` + pure-SQLite cache layer `androscan/internal/trace_cache.py` at `apps/<app_id>/.decompiled/<sha>/trace.sqlite` + `BehaviorAnchor` extensions to `trace_types.py` (`advanced_plans` / `rationale` / `low_confidence_decision_indices`) + two new `trace_max_hops_default` / `trace_max_hops_hard_cap` config knobs + 22 end-to-end tests), and **10.6 done 2026-04-29** (REST endpoint + tab shell + Hook Lab → Lab code rename — new `androscan/web/trace_routes.py` with five endpoints under `/api/trace` wired into `app.py`; `androscan/web/chat.py` `ALLOWED_TABS` + `_TAB_SYSTEM_PROMPTS` adopt `"lab"` with a `_normalise_tab` back-compat alias for legacy `"hook"` (collapses rate-limit bucket + transcript filename + system prompt onto the canonical id); frontend rename `HookLabTab.tsx → LabTab.tsx` + `HookLabCodeView → LabCodeView` + new `LabTraceMode.tsx` placeholder + new `api/trace.ts` client; 3-mode left-rail switcher inside the Lab tab (`Trace | Manual Hooks | Graph`) defaulting to `Trace` with `localStorage["androscan.lab.mode"]` persistence; `TabId` enum + `WorkbenchContext` chats record + `TabBar` updated to `"lab"`; one-shot `#/hook → #/lab` URL hash redirect for legacy bookmarks via `_LEGACY_TAB_REDIRECTS` table; 14 new `tests/test_trace_routes.py` end-to-end + 4 new `tests/test_chat_stream.py` lab/hook back-compat assertions; full suite **745 / 745 green**, frontend `tsc --noEmit` clean + Vite production build clean), and **10.7 done 2026-04-29** (Trace mode frontend — the headline UI: 6 new components under `androscan/web/frontend/src/components/trace/` (`BehaviorAnchorCard` / `DecisionTimeline` + inline `DecisionCard` / `BranchOutcomeBadge` / `PredicateOriginView` / `BypassPlanCard` / `TraceBanners` exporting `TraceTruncatedBanner` + `TraceIncompleteBanner`); `LabTraceMode.tsx` rewritten from 10.6 placeholder into the real Trace mode shell — entry-method form + hops stepper + Build / Force re-trace buttons, owned by the new `useTraceAnchor` hook in `api/trace.ts` (GET-then-fall-back-to-POST lifecycle, idle / loading / missing / building / loaded / error state machine), populated `BehaviorAnchorCard` + `DecisionTimeline` + per-plan `BypassPlanCard` list (default plans visible, advanced plans behind an `<details>` "Advanced" expander), cached-anchors picker, status footer; `BehaviorAnchor` / `DecisionPoint` / `BypassPlan` / `PredicateOrigin` discriminated-union types tightened in `api/trace.ts` (was `unknown[]` placeholders in 10.6); `WorkbenchContext` extended with `LabMode` + `setLabMode` (lifted from `LabTab.tsx`'s local state) and `pendingHookPrefill` + `setPendingHookPrefill` (new cross-mode "Stage in Manual Hooks" plumbing — mirrors `pendingCodeNav`'s lifecycle); `HookBuilder.tsx` consumes `pendingHookPrefill` via a ref-based handoff (so the existing template-change effect doesn't clobber staged params), gains a "Staged from Trace" pill on the header, drops the pill the moment the operator picks a different template or edits a param; `BypassPlanCard`'s "Stage in Manual Hooks" button writes the prefill + flips `labMode` so the operator lands in HookBuilder with the form pre-populated; `BehaviorAnchorCard` + `PredicateOriginView` (for `MethodCall` origins) write `pendingCodeNav` + flip the active tab to Inspect for cross-tab drill-in; ~430 lines of new CSS in `App.css` under `.trace-*` namespaces (form / banner / anchor card / decision timeline / branch badge / predicate origin / bypass plan card / cached anchors picker / hookbuilder-staged pill); no new backend tests (no FE test runner per project convention; the wire-shape contract is locked by 10.6's `tests/test_trace_routes.py`); full suite **745 / 745 green** unchanged, `tsc --noEmit` clean, `npm run build` clean — **709 modules** (was 703; +6 new components + the hook), 1.19 MB JS / 74 KB CSS / 372 KB gzipped (modest growth from 10.6's 1.18 MB / 63 KB / 367 KB). Next: **10.8 Mirror → Trace integration + chat hook + docs sweep** — see § Phase 10 sub-step backlog below.

Phase 4 CI remains parked. Phases 6–9 all complete (Phase 6 RE Workbench shell + Phases 7 click-to-code + 8 Smali call graph + 9 Frida — the latter two landed via Hook Lab v1 sub-steps 4.1 → 4.8 on 2026-04-27).

---

## Interactive RE Workbench (Phases 6–9)

Planned work: web-based interactive reverse engineering on top of the existing CLI pipeline — emulator mirror, click-to-code, static call graph from Smali, Frida tracing/hooking. **Stack (target):** FastAPI + WebSocket backend, React + Vite + TypeScript frontend, Monaco Editor, Cytoscape.js.

> **Naming note (post-DEC-024, 2026-04-28; code rename completed 2026-04-29):** the tab labelled **Hook Lab** in Phase 6→9 prose below is renamed **Lab** going forward, and the code-level rename (`HookLabTab.tsx` → `LabTab.tsx`, `HookLabCodeView` → `LabCodeView`, `#/hook` URL hash → `#/lab` with one-shot back-compat redirect, `tab="hook"` chat id → `tab="lab"` with `_normalise_tab` back-compat alias) shipped in sub-step **10.6**. Historical references to "Hook Lab" (this section's UX step 4 list, the `### Hook Lab v1 — sub-step backlog` table, completed-task entries) are accurate proper nouns for the v1 milestone and stay as-is. New work uses **Lab**. See **DEC-024** for the full policy.

### Sub-task status (implementation)

| Phase | Title                         | Status   |
|-------|-------------------------------|----------|
| 6     | Web UI shell + mirror         | MVP done (FastAPI + React build; `adb screencap` mirror + logcat WS + tap API; scrcpy / UI log filters = follow-up). **UX step 1 done:** tabbed shell with `react-resizable-panels` + per-tab `ChatDock`. **UX step 2 done:** triage endpoints + chat back-end with guardrails + Reports tab refresh (Markdown findings, per-card triage actions, chat wired to `/api/chat`). **UX step 3 done:** `androscan/rag/` (chunker + embed providers + SQLite vector index + cosine top-k), `/api/rag/{app_id}/{status,rebuild,query}`, auto-build on decompile completion, `search_decompiled_sources` LLM skill, Inspect-tab chat enrichment; click-to-code (`POST /api/inspect/map`) + persistent decompile cache (`/api/decompile`, `/api/code/{tree,file}`) + package-scoped logcat with stable Linux-UID filtering + mirror online/offline (`/api/device/status`) + sandboxed `/api/adb/shell` + `resolve_ui_element` LLM skill (deterministic fuser; also called inline by `/api/inspect/map` to attach a `resolution` block); Inspect-tab frontend wiring (4-column resizable layout, MirrorView with status dot + click-to-map, ElementMappingPanel with `BestBanner` / `ResolutionAlternatives` / `RagHitsList` for the fuser output, CodeView with scroll-to-line + highlight, ScopedLogcat, AdbShell). **UX step 3.5 done:** Settings tab (`#/settings`) — global config (form + raw-YAML editor + reset), per-app overrides (`apps/<app_id>/app_settings.json`), live status (global + per-app health probes via `androscan/web/health_probes.py` + `/api/status/{global,apps/{app_id}}` with `asyncio.gather` + 3 s cache), diagnostics (raw API + `/api/settings/reload`); `androscan/config/loader.py` extended with `CONFIG_FIELD_MAP` + `LIVE_RELOADABLE_FIELDS` + write-back helpers; `app.py` keeps live `Config` on `app.state.config`; `HealthDot` in the global header. See DEC-020 + DEC-021. |
| 7     | Click-to-code mapping         | Backend + frontend both done (`POST /api/inspect/map` + `androscan/web/inspect_map.py`; `resolve_ui_element` LLM skill fuses raw candidates + RAG into a single `best`/`alternatives`/`reasoning` block; Inspect-tab UI surfaces the fused pick in a prominent banner, alternatives + RAG hits as collapsible sections, and auto-jumps the Code Browser to `resolution.best`). |
| 8     | Static call graph (Smali)     | **Done 2026-04-27** via Hook Lab v1 sub-steps **4.1** (Smali parser + virtual/interface dispatch resolver + per-app SQLite store + REST routes) and **4.2** (Cytoscape pane with package-overview / focus-subgraph layouts + click-to-source). DEC-016 amended in the same commit. The `query_call_graph` LLM-tier skill (4.7) wraps the same store. See § Hook Lab v1 — sub-step backlog below + `docs/STATE.md` (Hook Lab 4.1, 4.2, 4.7) for the as-built reality. |
| 9     | Frida integration             | **Done 2026-04-27** via Hook Lab v1 sub-steps **4.3** (headless adapter + readiness probes), **4.4** + **4.6** (six-template hook library + scope_inspector + locked renderer contract), **4.5** (Stage→Inject UI with `pyjsparser` pre-validation + WS trace + JSONL persistence + per-app `hook_target_package_prefix` allowlist), **4.7** (`generate_frida_hook` LLM-tier skill — first consumer of DEC-022's `requires_confirmation=True` consent class), **4.8** (live Cytoscape overlay — fired methods bold cyan with hit counts). Modify-return mutation hooks + self-hosted Monaco (ISSUE-010) + `frida-server` auto-provisioning explicitly **out of scope** for v1. See § Hook Lab v1 — sub-step backlog below + `docs/STATE.md` Hook Lab 4.1–4.8 sub-bullets + DEC-023 for as-built reality + rationale. |
| 10    | Behavior Trace v1 (Lab)       | **Done 2026-04-29.** Planning checkpoint 10.0 done 2026-04-28 (DEC-024 ratified). Sub-steps 10.0 → 10.8 all landed; ~104 new tests across the static + skill + REST + frontend + chat layers. See § **Phase 10 — Behavior Trace v1 — sub-step backlog** below for the as-built record + `docs/STATE.md` Phase 10 sub-bullets. |
| 11    | Behavior Trace v2 (Lab)       | **Done 2026-04-30; v2.1 follow-up release in progress (ratified 2026-05-05).** v2: all sub-steps 11.0 → 11.7 landed across two calendar days; +26 new tests end-to-end; **ISSUE-013 → Resolved (Phase 11 v2)** against the v1-vs-v2 corpus regression-floor measurement test in `tests/test_decisions_slicing.py`; LLM-budget bumps (`num_ctx: 16384` + `num_predict: 12288`) shipped per DEC-025 spec but real-app token-usage measurement deferred per Q1 (A) of the 11.7 planning checkpoint. **v2.1 follow-up release:** Trace-mode entry-method discoverability (`ClassMethodTree` browser + Smali-field collapse + backend coalescer + three-tier LLM integration with Tier 3 surfaced via chat-widget pattern); 7 sub-steps v2.1.0 → v2.1.6; v2.1.0 ratified 2026-05-05 as the docs-only planning-checkpoint commit; DEC-025 stays Active (no new DEC for v2.1 per Q10: α — closing-note extended in v2.1.0 with the v2.1 ratification block). See § **Phase 11 — Behavior Trace v2 — sub-step backlog** below for the v2 as-built record + § **Phase 11 v2.1 follow-up release — sub-step backlog** for the v2.1 sub-step table + `docs/STATE.md` Phase 11 sub-bullets + DEC-025's v2 closing note + v2.1 closing-note extension. |
| 12    | Behavior Trace v3 (Lab)       | **Deferred-after-v2.1.** Was the active item from 2026-04-30 (Q2 (C) of the 11.7 planning checkpoint) until the v2.1 follow-up release was ratified on 2026-05-05; Phase 12 ratification depends on real-app telemetry from Phase 11 v2 + v2.1 use (operator-demand-gated, same posture as the post-DEC-024 MethodPicker shipping before Phase 11 started). Picks the operator-demand-gated v3 candidates from DEC-025's deferral list — strictly operator-demand-gated; Phase 12 ratification will pick the deliverables that show up in real-app telemetry from Phase 11 v2 + v2.1 use, not all of the deferral list at once. v3 candidate menu: ISSUE-014 (per-overload precision on two-register comparisons), auto-verify bypass after injection, free-form LLM JS for bypass plans, per-app TTL on cached anchors, iOS / WebView / native-binary adapters, Smali patching, LLM-budget real-app measurement follow-up. See Active Task above + DEC-025's deferral list for the full v3 candidate menu. |

**Phase 6→9 UX rollout (in addition to backend phases above):**

1. **[done]** Tab shell + `react-resizable-panels` + `WorkbenchContext` + `ChatDock` skeleton.
2. **[done]** Reports tab refresh + chat back-end. `androscan/web/triage.py` (`apps/<app>/<run>/triage.json`); `androscan/web/chat.py` with layered guardrails (allowlisted system prompt per tab, length caps, ANSI/zero-width/secret redaction, per-kind attachment budgets, `<context>` prompt-injection wrapping, rate limit, transcript JSONL). LLM client extended (`response_format=None`, `messages` override). Frontend `FindingCard` with Markdown + per-card triage buttons + severity override + note; `ChatDock` wired to `/api/chat`. **Note:** Lane-1 RAG indexer for static decompiled code is **deferred to step 3** where it pairs with click-to-code mapping (the chat needs code retrieval there; Reports works on dossier + finding alone).
3. Inspect tab — **done:**
   - **[done]** Lane-1 RAG indexer over decompiled sources (one-shot after jadx finishes). New `androscan/rag/` package: brace-balanced chunker for `.java`/`.kt`, `EmbedProvider` protocol with `fastembed` (default ONNX) / `ollama` / `hash` (test fallback) implementations, per-app SQLite vector store (WAL, packed `float32` blobs) at `apps/<app_id>/.decompiled/<sha>/rag.sqlite`, brute-force cosine top-k (numpy when present, pure-Python fallback). Endpoints `GET /api/rag/{app_id}/status`, `POST /api/rag/{app_id}/rebuild`, `POST /api/rag/{app_id}/query`. Auto-built via `schedule_rag_build_after_decompile` when the decompile cache turns ready. New `llm`-tier skill `search_decompiled_sources` (query, top_k, package_prefix, file_substr) advertised in the prompt catalog and fail-open. Inspect-tab chat enrichment in `androscan/web/chat.py::_enrich_inspect_with_rag` appends top-k snippets as `code` attachments (fail-soft when RAG is unavailable). New `pyproject` extra `[rag]` (fastembed, numpy). `Config` adds `rag_embed_provider` / `rag_embed_model` / `rag_top_k_default` (YAML `rag.*`, env `ANDROSCAN_RAG_*`). 197 tests pass.
   - **[done]** Package-scoped logcat backend (`/ws/logcat?package=…`) using **stable Linux-UID filtering** (`adb logcat --uid=<uid>` resolved via `stat /data/data/<pkg>` or `dumpsys package`) so the stream survives app restarts; falls back to `--pid=` re-resolution on older devices.
   - **[done]** Mirror online/offline probe at `GET /api/device/status` (`adb get-state`); UI badge wiring still pending.
   - **[done]** Click-to-map backend (`POST /api/inspect/map` in `androscan/web/inspect_map.py`): `dumpsys activity top` for foreground activity, `uiautomator dump /dev/tty` for view hierarchy, smallest-area-clickable bounds match, regex grep over the persistent decompile cache for handler candidates (`findViewById` > `onClick_near` > `compose_id` > `reference`). Persistent decompile cache backend in `androscan/web/decompile_cache.py` (`POST /api/decompile/{app_id}`, `GET /api/code/{app_id}/{tree,file}`) keyed by `sha256(apk)` under `apps/<app_id>/.decompiled/<sha>/sources/`. Sandboxed `POST /api/adb/shell` proxy with `shlex` parsing + denylist + 20 s timeout + 200 KB output cap.
   - **[done]** New `llm`-tier skill `resolve_ui_element` (`androscan/skills/resolve_ui_element.py`) — deterministic, explainable scorer over raw click-to-code candidates with optional Lane-1 RAG enrichment (synthesises a query from `text` > `content_desc` > short resource id). Pure-function `resolve()` reused inline by `/api/inspect/map` so the response now carries a `resolution` block ({`best`, `alternatives`, `rag_hits`, `reasoning`}) without an LLM round-trip. Tests: `tests/test_resolve_ui_element.py` (13 tests). **210 tests total, all passing.**
   - **[done]** Inspect-tab **frontend** wiring (`androscan/web/frontend/src/tabs/InspectTab.tsx` + components). Four-column resizable layout: Projects sidebar | Classes-&-methods tree + scoped logcat | UI-mapping/Code-browser tabs + chat dock | Mirror + adb-shell. `MirrorView` renders `/ws/mirror` PNG frames, polls `/api/device/status` for an online/offline status dot, translates clicks from image-pixel space to device-pixel space and forwards to `/api/inspect/map`. `ElementMappingPanel` shows a sub-activity progress bar during the map call and then renders the fused `resolution.best` as a prominent banner (`SourceBadge` regex/RAG pill, kind pill, score, reasons grid, snippet, "Open in Code browser"), the deterministic candidates list with kind pills + per-row "Open" buttons, plus collapsible `ResolutionAlternatives` and `RagHitsList` (with the synthesised RAG query). The Code-browser tab uses `CodeView` (line numbers, scroll-to-line, persistent line-range highlight, find query, gear prefs); a tap auto-opens `resolution.best.file` and scrolls/highlights the picked line range, falling back to `candidates[0]` when no fuser pick exists. Chat attachments (`attachments` memo in `InspectTab`) include the fused `best_handler` JSON block alongside the element + candidates so the LLM sees the picked file/line/reasons. New CSS for `.source-badge` (deterministic = green, rag = warn), `.best-banner` (accent border + soft background, reasons grid), `.resolution-alts`, `.rag-hits`. Vite production build clean (308 modules, 32 KB CSS, 393 KB JS).
3.5. **[done]** Settings tab — fourth top-level tab (`#/settings`) with four sub-panels:
   - **Global settings:** form view (with source pills `yaml`/`env`/`default` per field, env-lock indicator, restart-required pill on non-live-reloadable fields) **plus** a raw-YAML editor with server-side validation; "Reset to defaults" button.
   - **App settings (per-app):** project dropdown; per-key override-or-inherit toggles writing to `apps/<app_id>/app_settings.json` (atomic); "Reset to defaults" with force-with-warning when an override would diverge from a global field still consumed via the boot-time closure.
   - **Status:** live cards for global health (adb / jadx / apktool / frida / Ollama / embed-provider / disk free + writability) and per-app health (`pm path`, foreground activity, UID, uiautomator dump, apk-sha drift, RAG status, decompile cache freshness); "Refresh now" cache-bypass button.
   - **Diagnostics:** raw API JSON for global + per-app payloads, `POST /api/settings/reload`.
   - Backend modules: `androscan/web/health_probes.py` (timeboxed pure-function async probes — never raise, return `{ok, label, ...}`), `androscan/web/per_app_settings.py` (atomic schema-versioned writes, `effective_settings` merger), `androscan/web/status_routes.py` (`asyncio.gather` + 3 s in-process cache; `invalidate_status_cache()` on every settings save), `androscan/web/settings_routes.py` (validates raw YAML, `dump_to_yaml`, `restore_defaults_yaml`; returns `restart_required` per field). `androscan/config/loader.py` extended with `CONFIG_FIELD_MAP`, `LIVE_RELOADABLE_FIELDS`, `global_view_from_config`, `effective_sources`, `with_overrides`, `coerce_yaml_value`, `dump_to_yaml`, `save_raw_yaml`, `restore_defaults_yaml`, `validate_raw_yaml`, `read_raw_yaml`, `discover_config_path`. `app.py` keeps the live `Config` on `app.state.config` (`_current_config`/`_set_config` helpers) so live-reloadable fields take effect without restarting uvicorn.
   - Frontend: `SettingsTab` + sub-panels, `HealthDot` in the global header (30 s polling, deep-link to Settings), `api/settings.ts`, `api/status.ts`, ~300 lines of new CSS in `App.css`.
   - Tests: `tests/test_health_probes.py`, `tests/test_per_app_settings.py`, `tests/test_settings_routes.py`. All Python tests green; `tsc --noEmit` + `vite build` clean.
   - See **DEC-020** (settings tab design) and **DEC-021** (probe shape + asyncio.gather + cache).
4. Hook Lab tab: static smali call graph, Frida adapter + ring buffer + agentic tool calls (`frida_query`, `frida_stats`), Cytoscape overlay (static = muted grey, frida = bold cyan), Monaco decompiled + script editor, scope/hooks inspector, chat with frida summary + tail. **Implementation plan locked in DEC-023** (planning commit 2026-04-25); see § **Hook Lab v1 — sub-step backlog** below for the strictly-linear 8-sub-step rollout.

**§ Hook Lab v1 — sub-step backlog** *(**v1 complete 2026-04-27**; strictly linear; one sub-step per Agent-mode session; locked in DEC-023 + amended DEC-016)*

| # | Sub-step | Key deliverables | Depends on |
|---|----------|------------------|------------|
| 4.1 | **[x] Smali call-graph backend** *(landed 2026-04-27)* | `androscan/analysis/` (`apktool_runner`, `smali_parser`, `dispatch`, `smali_types`, `call_graph`) with v2 fidelity (direct `invoke-*` edges + virtual/interface-dispatch BFS against in-app class hierarchy, `MAX_OVERRIDES_PER_INVOKE=64` guard with `truncated` flag on edges; locked edge-kind enum `direct | static | super | virtual_dispatch | interface_dispatch | external`; `may_have_unresolved_reflection` flag on reflection-callsite nodes). Per-app SQLite at `apps/<app_id>/.decompiled/<sha>/call_graph.sqlite` (schema_version 1: `classes` / `class_interfaces` / `nodes` / `edges` with INTEGER FKs, external callees materialised as `is_external=1` rows for uniform neighbour queries; WAL + autocommit; `meta` table; in-process `_RUNNING` registry with orphan-pending recovery after `PENDING_GRACE_SEC=30s`; atomic snapshot writes). Query API: `GET /api/graph/{app_id}` (paginated + `package_prefix` / `kind` / `include_external` filters, hard cap 5000), `GET /api/graph/{app_id}/neighbors/{node_ref:path}` (callers + callees; numeric id or URL-encoded smali signature), `GET /api/graph/{app_id}/paths?source=…&target=…` (bounded BFS, `max_hops≤12`, `max_paths≤50`), `GET /api/graph/{app_id}/status` + `POST /api/graph/{app_id}/rebuild`. Auto-build hook `schedule_call_graph_build_after_decompile` fires parallel to the RAG hook. New `_call_graph_card` in Settings → Status. 57 new tests in `tests/test_call_graph_{parser,dispatch,index}.py` + `tests/test_graph_routes.py` over fixtures at `tests/fixtures/call_graph_smali/` (multi-dex + interface + reflection). DEC-016 ratified in code. **354 / 354 tests green.** | apktool output |
| 4.2 | **[x] Hook Lab UI — Graph pane** *(landed 2026-04-27)* | Cytoscape.js viewer (`androscan/web/frontend/src/components/CallGraphView.tsx` + typed client `src/api/graph.ts`) with **package overview by default** (cose-bilkent layout, cross-package edge weights) and **right-click "Focus subgraph here"** with a 1–6 hops stepper rendering the neighbour subgraph in dagre LR. `virtual_dispatch` / `interface_dispatch` / `external` edges rendered dashed; `may_have_unresolved_reflection` nodes outlined in orange with an `[R]` suffix. Tippy.js node tooltips (method signature + reflection flag); inline filter; hidden-by-default external-node toggle; Rebuild button hits `POST /api/graph/{app_id}/rebuild`. Click a method node → in-tab `HookLabCodeView` loads the Java file via existing `/api/code/{app_id}/file` and renders `CodeView` with `emphasizeMethod`. Right-click context menu's "Open in Inspect" pumps a `pendingCodeNav` through `WorkbenchContext` and switches to the Inspect tab where a new `useEffect` consumes it via the existing `handleSelect` flow. Frontend contract locked by 3 new field-shape tests in `tests/test_graph_routes.py`. **357 / 357 tests green.** | 4.1 |
| 4.3 | **[x] Frida adapter foundation** *(landed 2026-04-27)* | New `androscan/adapters/{__init__.py, frida_client.py}`: `FridaClient` (process-wide; lazy `frida` import via `_frida_python()` test seam; `FridaUnavailableError` with install hint), `FridaSession` (per-attach state — `load_script` / `detach` / `stats`; thread-safe `collections.deque(maxlen=ring_size)` of `TraceEvent`s populated from the Frida message thread under a `threading.Lock`; optional `on_event` callback fired *after* the ring lands so 4.5's WS UI can never race ahead of `stats()`), `TraceEvent` mirroring Frida's native `send` / `error` / `log` kinds (raw message preserved). `detach_all()` wired on uvicorn shutdown via a guarded `@app.on_event("shutdown")` handler in `androscan/web/app.py`; `get_frida_client(app, config)` lazily caches on `app.state.frida_client`. New `frida_trace_ring_buffer_size` config knob (default 5000, clamped `>= 100`) wired through `Config` / `CONFIG_FIELD_MAP` / `LIVE_RELOADABLE_FIELDS` / `_merge_from_yaml` / `ANDROSCAN_FRIDA_TRACE_RING` env / `global_config.yaml`'s new `frida:` section. Two new health probes in `androscan/web/health_probes.py` — `probe_frida_server` (`adb shell pidof frida-server`; returns `{ok, running, pid, error}`) and `probe_frida_version_skew` (compares host `frida` CLI vs. `frida-server --version`; severity `None` / `"minor"` / `"major"`); aggregated by `status_routes.py` into a single `tools.frida_server` card via `asyncio.gather`. Frontend: `GlobalStatus.tools.frida_server` typed in `src/api/status.ts` (`{ok, label, running, pid, host_version, device_version, version_skew, error}`); `rollupGlobal` treats Frida readiness as **yellow** (non-critical for static workflows); one new `<StatusCardView>` in `SettingsTab.tsx`. New `[frida]` `pyproject.toml` extra (`frida>=16`, `frida-tools>=12`, `pyjsparser>=2.7`); `--setup` now installs `.[dev,rag,frida]` by default. New `device` pytest marker registered (deselect with `-m "not device"`; default suite is unchanged). 34 new tests across `tests/test_frida_client.py` (new), `tests/test_health_probes.py`, `tests/test_settings_routes.py`, `tests/test_config.py`, `tests/test_first_run_setup.py`. **391 / 391 tests green** (was 357). DEC-023 promoted Proposed → Active in this commit; DEC-021's "Hook Lab readiness rollup probe" follow-up ticked. **Headless by design** — no HTTP routes, no Hook Lab tab UI changes (only the Settings → Status readiness signal); 4.4 adds the template library and 4.5 lights up the Inject UI + WS + JSONL persistence. | `frida` Python pkg |
| 4.4 | **[x] Hook template library** *(landed 2026-04-27)* | New `androscan/adapters/frida_hooks/` package with the five v1 templates: `entry_exit_log` (3 params: class/method/label), `ssl_pinning_bypass` (multi-strategy: Conscrypt `TrustManagerImpl` + OkHttp3 `CertificatePinner` + generic `X509TrustManager`; 1 param: label), `crypto` (`javax.crypto.Cipher` init/doFinal observer; in/out lengths only — plaintext capture is an explicit consent gate left to hand-rolled scripts; 1 param: label), `shared_preferences` (getString/putString with key-prefix filter; 2 params: label + optional `key_prefix=""`), `intent` (`ContextWrapper.startActivity/startService/sendBroadcast` with action/data/component/package capture; 1 param: label). Each template = one Python module exporting `TEMPLATE: HookTemplate` (frozen dataclass: `id` / `name` / `description` / `params: tuple[HookTemplateParam, ...]` / `js_template` / `pentester_summary_template` / informational `sensitive_apis`). Renderer in `__init__.py`: `render(template, params) -> RenderedHook(template_id, js, summary, params_used)` validates in three stages (unknown-key → missing/empty-required → fill defaults), coerces every value to `str`, then `str.format`s the **same merged params dict** through both JS and summary; literal JS braces are escaped via `{{` / `}}`. Public API: `render` / `render_by_id` / `get_template` / `list_templates` / `register` / `discover` / `extract_format_fields`. Error taxonomy: `HookTemplateError` (base) → `HookTemplateNotFound` (unknown id; lists valid ids alongside) + `HookParamError` (one class for missing-required / empty-required / unknown-key). Explicit `_TEMPLATE_MODULES` tuple drives discovery (auto-globbing rejected — scratch file would silently become a registered template). New `tests/test_frida_hook_templates.py` (**+67 tests**): `TestRenderer` (contract against a hand-rolled fixture; happy path + every error variant + default fill + value coercion + `{{` literal-brace escape + `extract_format_fields` named-only / skip-positional / strip-attribute / index), `TestRegistryFailClosed` (parametrised over `_TEMPLATE_MODULES`: every module exports `TEMPLATE: HookTemplate`; id matches basename; JS + summary non-empty; both `str.format` placeholder sets ⊆ declared params; every required param appears in JS *or* summary; every template renders cleanly with stub inputs — this is the test that makes "stub or missing summary" a hard CI failure), `TestTemplates` (loose substring smoke renders per shipped template). Adding a new template is now a strict two-deliverable change (JS body + non-empty summary). Full suite: **458 / 458 green** (was 391; +67). `tsc --noEmit` + `vite build` clean (no FE changes — headless by design; 4.5 owns Stage→Inject + WS + JSONL persistence, 4.7 owns the `generate_frida_hook` LLM skill that fills these templates). DEC-023 gets a "Hook template library (sub-step 4.4 specifics)" sub-bullet documenting storage / renderer contract / fail-closed mechanism. | 4.3 |
| 4.5 | **[x] Hook builder + Stage→Inject flow** *(landed 2026-04-27)* | New `androscan/web/frida_routes.py` (8 routes — `GET /api/frida/templates` + `/{id}`, `POST /api/frida/render`, `POST /api/frida/sessions`, `GET /api/frida/sessions` + `/{id}`, `DELETE /api/frida/sessions/{id}`, `GET /api/frida/sessions/{id}/events`, `GET /api/frida/sessions/{id}/export`, `WS /ws/frida/sessions/{id}/trace`) factory-built via `build_frida_router(...)` and wired into `androscan/web/app.py` so the same DI seams as `status_routes` / `settings_routes` flow through. JS pre-validation in new `androscan/adapters/frida_hooks/_jsparse.py` (`parse_frida_js(source) -> ParseResult{ok, error, line, column, available}`) — wraps `pyjsparser.PyJsParser().parse()` in a lazy `_pyjsparser_module()` test-seam mirroring DEC-018's RAG / DEC-023's `_frida_python` pattern; returns `available=False` (not a hard error) when `pyjsparser` isn't installed so the suite stays optional. JSONL trace persistence wired into `FridaSession` (`set_persistence_path(path)` + a daemon writer thread + an unbounded `queue.Queue` decoupling Frida's message thread from disk I/O; `detach()` poisons the queue and joins the writer so all events flush; serialisation / I/O failures bump a `persist_dropped` counter via `_jsonl_fallback`). New per-app hook section in `androscan/web/per_app_settings.py` — `_HOOK_KEYS = {hook_target_package_prefix, auto_attach_on_session_start}` (`effective_settings(...)` defaults the prefix to the app's `app_meta.json` package id; `coerce_partial_update` strict-validates type / non-empty / max length / null-clears). Allowlist enforcement on `POST /api/frida/sessions` rejects with **403 hook_blocked** if the requested package doesn't start with the resolved prefix; if the JS fails to parse and `pyjsparser.available` is true, returns **400 render_parse_error** with `{message, line, column}` so the frontend can attach an inline Monaco marker. Frontend: `@monaco-editor/react@^4.6.0` added; new typed client `androscan/web/frontend/src/api/frida.ts` (full REST surface + `useFridaTrace(sessionId)` hook owning the WS lifecycle — replay-then-stream contract, capped buffer at 2000 events, pause/resume with overflow buffering, drop-coalescing); new components `HookBuilder.tsx` (template picker + auto-generated param form prefilled from the call-graph node; debounced server-side render → Monaco read-only JS view with inline `pyjsparser` markers via `setModelMarkers(model, "androscan-jsparse", …)`; pentester-summary card; single Inject button gated on `parse.ok || !parse.available` + missing-required + render-in-flight), `FridaTracePanel.tsx` (WS-driven event list with sticky-tail auto-scroll; substring filter against `label / phase / message / class / method / error`; pause/resume/clear/export buttons — Export is an `<a download>` pointing at `/api/frida/sessions/:id/export`), `FridaSessionsList.tsx` (2.5 s polling + eager refresh after Inject/Detach; per-row template id / package / pid / event count / drop counts / detach button). `HookLabTab.tsx` restructured: graph (left) + (CodeView, HookBuilder, ChatDock) stacked centre + (SessionsList, TracePanel) stacked right; selected graph node prefills `class_name` / `method_name`; successful Inject pins the trace pane to the new session id. ~480 lines of new CSS in `App.css` under explicit `.hookbuilder-*` / `.frida-session-*` / `.trace-*` namespaces. **JSONL persistence design rationale (DEC-023 sub-bullet):** writer thread + queue means a slow disk can never block the Frida message thread or the asyncio loop; a single bad event bumps `persist_dropped` instead of killing the session; the wire format on the WS and the on-disk JSONL are byte-identical (both go through `_event_to_jsonable`) so the export endpoint is a thin `StreamingResponse` over the file. **WS replay-then-stream:** the WebSocket route drains the session's ring buffer first (catch-up for late joiners), then registers a non-async `on_event` hook that bounces events to an `asyncio.Queue(maxsize=2000)` via `loop.call_soon_threadsafe`; queue overflow drops one item and emits a `{type: 'drop'}` notice rather than blocking. **Monaco bundling:** `@monaco-editor/react`'s default loader lazy-fetches Monaco from a jsdelivr CDN — bundle stays small (~1.1 MB / 354 KB gzipped); self-hosting is a single `loader.config({paths: {vs: '/monaco/min/vs'}})` plus a postbuild copy step, deferred to v2 (KNOWN_ISSUES note). **+45 backend tests** in three new files: `tests/test_frida_hook_jsparse.py` (11 cases — happy path, syntax errors with line extraction, defensive non-string / empty / whitespace, `pyjsparser`-missing path), `tests/test_frida_routes.py` (24 cases — full HTTP surface via `TestClient` + a copied stub `frida` module: templates list/get/404, render happy/missing/unknown/template-404, sessions create-happy/persist-false/allowlist-403/unknown-app-404/widened-prefix/template-404/param-error-400, list/get/delete/404, events ring snapshot, export JSONL stream + 404 when persist=false, WS replay-then-stream + WS unknown-session close, 503 frida_unavailable for session routes while templates/render still work), plus extensions to `tests/test_frida_client.py` (+8 — JSONL round-trip, parent dir creation, called-twice / after-detach raises, drop counter on unserialisable payload + unwritable path, detach flushes writer, no-path mode unchanged, app_id / template_id metadata round-trip) and `tests/test_per_app_settings.py` (+13 — defaults, save/load, `effective_settings` defaulting `hook_target_package_prefix` to `app_package`, explicit override / `auto_attach_on_session_start`, `apk_overrides_summary` picks up hook overrides, `coerce_partial_update` validates type / empty / null-clears / max length / `auto_attach` boolean). Full suite: **517 / 517 green** (was 458 — +45 tests + 14 from earlier today's settings updates). `tsc --noEmit` + `vite build` clean (1.13 MB / 354 KB gzipped — Monaco loader is CDN-based at runtime, see above). | 4.2 + 4.4 |
| 4.6 | **[x] Scope inspector + hooks/stats panel** *(landed 2026-04-27)* | New `androscan/adapters/frida_hooks/scope_inspector.py` (sixth v1 template; 3 params — `class_name` / `method_name` / `event_label` — same shape as `entry_exit_log` so the call-graph prefill flow keeps working). The JS hooks every overload via `Java.use`, walks `this.getClass().getDeclaredFields()` per call (with `Field.setAccessible(true)` and per-field try/catch so a single getter that throws never voids the whole snapshot), and emits two new `send` payload variants the aggregator pattern-matches on: `{phase:"entry", class, method, args, this_class, this_fields:{f: String(v), ...}}` + `{phase:"exit", class, method, return, this_fields:{...}}`. Read-only by design — DEC-023's modify-return UI is still v2. Two new pure helpers in `androscan/web/frida_routes.py`: `_summarize_hooks(session)` (iterates `session.events()` once, groups by `(payload.class, payload.method)`, counts entry events as hits, tallies exit `return` strings into `top_returns: [{value, count}]` capped at 5 with stable insertion-order tiebreak, sorts rows by hit count desc → last-seen desc; returns capped per-value at 256 chars so a 200 KB stringified buffer can't blow up the response) + `_summarize_scope(session)` (additionally filters on `payload.this_fields` being a dict so other templates' entry/exit events are explicitly *excluded* from the Scope panel; keeps the most-recent entry + most-recent exit per `(class, method)` independently so a method that's mid-call still shows useful entry data). Two new GET routes wired through `build_frida_router(...)`: `GET /api/frida/sessions/{session_id}/hooks` returns `{session_id, hooks: [...]}`, `GET /api/frida/sessions/{session_id}/scope` returns `{session_id, snapshots: [...]}`. Both 404 on unknown session, both work *without* the `[frida]` extra installed (they only read the ring buffer — no Frida I/O). Frontend: new `HookStatsPanel.tsx` (polls `/hooks` every 2.5 s; renders one row per `(class, method)` with hits / last-seen / template badge / top-3 return values; gracefully handles 404-on-detach as empty state) + new `ScopeInspectorPanel.tsx` (polls `/scope` every 2.5 s; renders one collapsible `<details>` card per `(class, method)` with side-by-side entry/exit panels; computes the entry-vs-exit `this_fields` diff client-side and highlights changed keys + surfaces a "N mutated" pill on the card header). `HookLabTab.tsx`'s right-pane bottom slot is now a 3-button tab strip — `Trace | Hooks | Scope` — defaulting to Trace so existing operators see no behaviour change. Hooks + Scope tabs are disabled when no session is active (with a tooltip pointing the operator at Inject); all three panels share the same `activeSession` state so flipping tabs preserves selection. ~360 lines of new CSS in `App.css` namespaced under `.right-pane-tabs-*` / `.hookstats-*` / `.scope-*` (keeps the grep-affordance from 4.5 intact). **Polling vs. WS rationale:** `/hooks` and `/scope` are pure aggregations over the *same* ring buffer the trace WS consumes; a parallel WS would be redundant and the 2.5 s lag is invisible to a human eye scanning a summary table. Polling also means a paused trace WS doesn't also pause the Hooks/Scope panels — the underlying ring keeps receiving events. **+9 backend tests** in `tests/test_frida_routes.py` (empty session, multi-hook aggregation with top_returns ordering, malformed-payload defensiveness, scope filters out non-this_fields events, latest-entry-and-exit kept per method, plus 404 cases) + **+1 in `tests/test_frida_hook_templates.py`** (`scope_inspector` smoke render asserting JS contains `getDeclaredFields` + `setAccessible` + `this_fields` + the three phases + summary mentions `read-only` / `field` / `this_fields`); the registry-walk fail-closed test from 4.4 auto-parametrises over the new template adding **+7 structural-invariant tests** for free (id matches basename, JS + summary non-empty, both `str.format` placeholder sets ⊆ declared params, every required param appears in JS *or* summary, template renders cleanly with stub inputs). Full suite: **534 / 534 green** (was 517; +17 new tests). `tsc --noEmit` + `vite build` clean (1.14 MB / 357 KB gzipped — modest growth from 4.5's 1.13 MB / 354 KB, attributable to the two new components and ~360 lines of CSS). DEC-023 gets a "Scope inspector + hooks/stats panel (sub-step 4.6 specifics)" sub-bullet documenting the new template + aggregator contract + polling rationale + tab-strip layout decision. **What 4.6 explicitly does *not* ship:** modify-return / mutation UI (v2), the LLM-tier `generate_frida_hook` skill (4.7), and the Cytoscape graph overlay highlighting hit nodes (4.8). | 4.5 |
| 4.7 | **[x] LLM-tier skills (`query_call_graph`, `generate_frida_hook`)** *(landed 2026-04-27)* | New `androscan/skills/query_call_graph.py` — read-only LLM-tier skill (`requires_confirmation=False`) wrapping `androscan.analysis.call_graph.list_graph` / `neighbors` / `paths`. Three modes (`overview` / `neighbors` / `paths`) with parameter clamping (limit ≤ 5000, max_hops ≤ 12, max_paths ≤ 50). Resolves `app_id` from the explicit param OR `run_folder.parent` (same fallback ladder as `resolve_ui_element`). Fail-open on missing app context, unready decompile cache, or unbuilt call graph — returns `success=True` with empty data and clear `text` so the LLM can read the empty result and pivot, mirroring `search_decompiled_sources`. New `androscan/skills/generate_frida_hook.py` — first real consumer of DEC-022's consent-class hook (`requires_confirmation=True`). Headless renderer wrapping `frida_hooks.render_by_id`; converts `HookTemplateNotFound` / `HookParamError` into clean `SkillResult` failures whose `text` includes the valid-template-id list + declared schema so the LLM can self-correct on the next turn. On success returns `{template_id, js, summary, params_used, sensitive_apis, rationale}` plus a human-readable `text` containing a JS preview, the deterministic pentester summary, and an "Operator action required" notice — the skill never injects (DEC-023's Option-A confirmation UX). `SkillMeta` extended with `requires_confirmation: bool = False` (default False keeps every existing read-only skill unchanged); both new skills registered in `androscan/skills/__init__._SKILL_MODULES`. Hook Lab chat dock attachments are wired here: `HookLabTab.tsx` builds a `ChatAttachment[]` with the selected method header (`default`), the decompiled Java source (`code`, capped at 6_000 chars to mirror backend `ATTACHMENT_BUDGETS["code"]`), and a `frida_summary` JSON block aggregating the active session's hooks summary + last-30 trace-event tail (polled every 2.5s alongside the existing `HookStatsPanel` / `ScopeInspectorPanel`). `buildHookChatContextSummary` rewritten to reflect what's actually being sent. **Tests** (`tests/test_skills.py` + new `tests/test_query_call_graph_skill.py` + new `tests/test_generate_frida_hook_skill.py`): catalog inclusion, `requires_confirmation` field defaults, mode dispatch + parameter clamping, fail-open paths, and schema-aware error text on missing-required / unknown-key. **Not yet wired into the workbench's agentic chat loop** — that's DEC-022's job (sub-step 4.8 / Phase 7+). | 4.5 |
| 4.8 | **[x] Frida overlay on graph + docs sweep** *(landed 2026-04-27 — **Hook Lab v1 complete**)* | Pure-frontend Cytoscape overlay layering live Frida hits onto the existing call-graph pane. `androscan/web/frontend/src/components/CallGraphView.tsx` accepts a new optional `hitsByMethod: ReadonlyMap<string, number> \| null` prop (also exports `hitKey(className, methodName)` so producer + consumer share one canonical key). `null` → graph renders unchanged in 4.2 styling; `Map` (possibly empty) → method nodes whose `(class.class_name, method_name)` matches a key render in **bold cyan** per DEC-023 with a `×N` hit-count suffix on the label and a `frida: N hit(s)` line in the tippy tooltip; non-matching nodes dim to opacity 0.45 so the eye is drawn to the hits first. Package-overview mode aggregates per-package totals (`• N hits in M methods` footer + bold-cyan treatment for packages with hits; opacity 0.5 when zero hits). Reflection / focus-root styling still applies — `.hit` / `.has-hits` are declared **after** the reflective / focusroot rules so the bold-cyan border wins where both overlap. New `HIT_CYAN = "#56d4dd"` colour token shared between the Cytoscape stylesheet and the tooltip HTML. `androscan/web/frontend/src/tabs/HookLabTab.tsx` builds `hitsByMethod` via `useMemo` from the same `chatHooks` it already polls every 2.5 s for the chat `frida_summary` attachment — no new poll cadence, no new endpoint — so the overlay refreshes cohesively with the right-pane HookStatsPanel / ScopeInspectorPanel and the chat-attachment payload. While `chatHooks` is still loading the overlay turns on with an empty `Map` rather than `null` so the graph renders dimmed and hits flow in on the next 2.5 s tick — matches the rest of the right pane (HookStatsPanel / FridaTracePanel both surface "session active, no events yet" affordances rather than hiding their UI). **No new tests** in 4.8 — pure-frontend visual layering. Backend suite **566 / 566 green** unchanged. `tsc --noEmit` + `vite build` clean (1.14 MB / 358 KB gzipped — unchanged from 4.7 modulo ~150 lines of new TSX). **Final docs sweep:** `STATE.md` (4.8 sub-bullet + Recent-completed-work entry); this `TASKS.md` row marked `[x] DONE` and the **Hook Lab v1 — sub-step backlog** flipped to "v1 complete (2026-04-27)"; `DESIGN_DOC.md` Phases 8 + 9 annotated as landed-in-Hook-Lab-v1 with a forward pointer to `docs/STATE.md`; `DECISIONS.md` DEC-023 gets a "Frida overlay (sub-step 4.8 specifics)" sub-bullet + a "Hook Lab v1 complete" closing note (DEC-023 was already promoted Proposed → Active in 4.3, so no flip needed); `KNOWN_ISSUES.md` LIMIT-002 marked Resolved + new ISSUE-012 documenting the per-overload precision caveat (hooks aggregate by `(class, method)` so overloaded methods undercount on the overlay); `SAFETY_AND_SECURITY.md` §12.6 augmented with a "v1 complete: what now-shipped controls do" subsection consolidating the operator-managed `frida-server` posture + the now-enforced per-app `hook_target_package_prefix` server-side allowlist + the trace-persistence path at `apps/<app_id>/<run_ts>/frida/<session>.jsonl`. **What v1 ships overall (4.1 → 4.8):** Smali static call graph + REST + Cytoscape pane with click-to-source → headless Frida adapter with ring buffer + readiness probes → six-template hook library with locked renderer contract + scope_inspector → Stage→Inject UI with Monaco + `pyjsparser` pre-validation + WS trace + JSONL persistence + per-app prefix allowlist → hooks-stats + scope-inspector right-pane panels → `query_call_graph` + `generate_frida_hook` LLM-tier skills + chat attachments + DEC-022's `requires_confirmation` consent class → live Frida hits coloured onto the call graph. **Out of scope for v1 (deferred to v2 / next phases):** modify-return / mutation hooks, self-hosted Monaco (ISSUE-010), `frida-server` auto-provisioning (operator-managed by design), the bounded chat agentic loop wiring `query_call_graph` / `generate_frida_hook` into the workbench chat (DEC-022 / ISSUE-009 — `requires_confirmation` is structurally ready, chat-side wiring is the next layer's job). | 4.6 + 4.7 |

**Brief Ask-mode planning checkpoint at the top of 4.1** to settle the SQLite schema (tables, indices, foreign keys) before code lands — otherwise the Agent will improvise a schema and 4.2's query API may force a rework.

**Risks (per DEC-023):**
- JS pre-validation = **`pyjsparser`** (pure-Python, no Node.js dep). Inject button stays disabled until the rendered JS parses; parser errors shown inline.
- Device-touching tests are gated by `pytest -m device` (opt-in marker; not part of the default suite). `pytest -m "not device"` is the CI default.

**Settings tab follow-ups (incremental, P2):**
- Migrate hot-path route handlers (`/api/llm/info`, chat, RAG, decompile) to read from `app.state.config` so live reload covers their fields too — currently they capture `config` at boot, which is why the UI shows a `restart_required` pill for those fields.
- Add a "compare to global" diff view in the per-app settings panel so overrides are visible at a glance.
- Persist the raw-YAML editor's draft locally (sessionStorage) so an accidental tab switch doesn't lose unsaved work.
- **[done 2026-04-27, in Hook Lab 4.3]** Add a "Hook Lab readiness" rollup probe (frida-server present on device, frida CLI on host, target package gadget injectable) once Hook Lab work begins. Two-card design landed in Hook Lab 4.3: existing `tools.frida` (host CLI; from `probe_frida_version`) + new `tools.frida_server` (device-side reachability + host/server version-skew, severity `None` / `"minor"` / `"major"`); see `androscan/web/health_probes.py::probe_frida_server` + `probe_frida_version_skew` and `status_routes.py`. Treated as **yellow** in `rollupGlobal` (non-critical for static-only workflows). "Target package gadget injectable" check is deferred to 4.5 along with the Inject UI it would gate.

**RE Workbench chat — agentic skill loop (P2, planned per DEC-022):**
- Extend `androscan/web/chat.py` with a bounded `while turn < MAX_CHAT_TURNS` loop that calls `parse_response()` + `run_skills()` whenever the LLM emits `skill_requests` (mirroring `androscan/internal/workflow.py`'s pattern). Hard caps in code: `MAX_CHAT_TURNS = 5`, `MAX_SKILLS_PER_TURN = 3`, per-skill timeout = 5 s, per-turn skill-output budget ≈ 6 KB.
- Add `requires_confirmation: bool = False` to `SkillMeta`. Today's read-only LLM-tier skills stay `False`; Hook-Lab-introduced skills (frida hook injection, anything that mutates device or files) ship `True`.
- Extend the SSE vocabulary with `skill_request` / `skill_result` / `skill_pending` event types; render as collapsible cards inside the existing thinking block in `ChatDock`. For `skill_pending` (consent-required skills), the loop awaits `POST /api/chat/skill_decision/{request_id}` (Allow / Deny + optional edited args) with a 90 s TTL on pending state.
- Per-tab "always confirm" toggle in Settings → Per-app overrides (default off); when on, every skill is treated as `requires_confirmation=True` for that tab.
- Transcript schema: interleave `{type: "skill_call", turn, name, args, result_preview, duration_ms, decision?}` records into `apps/<app>/<run>/chat/<tab>.jsonl`.
- Ship behind a per-tab feature flag (`chat.agentic_loop.enabled`) so it can ship dark and enable per tab as confidence grows. Order: Inspect → Reports → Hook Lab. New tests in `tests/test_chat_agentic.py` covering happy path, max-turn cutoff, skill timeout, skill error mid-loop, consent deny, consent TTL expiry, SSE event ordering.
- Quick win to land independently: bump `_INSPECT_RAG_TOP_K` from 4 → 8–10 + raise `_INSPECT_RAG_PER_HIT_CHARS` proportionally. Reduces the failure rate that motivates this DEC but does not replace it.

---

**§ Phase 10 — Behavior Trace v1 — sub-step backlog** *(strictly linear; one sub-step per Agent-mode session; locked in **DEC-024** — concurrent with the Hook Lab → Lab rename)*

Phase 10 ships **Behavior Trace** as the new headline mode of the **Lab** tab (renamed from Hook Lab per DEC-024). The workflow is *Anchor → Locate → Trace → Classify → Manipulate → Verify* — operator picks a UI element or method anchor, the static layer enumerates the closure of conditional gates that govern its behaviour (≤ 3 hops by default), the LLM interprets and proposes template-bound bypass plans, the operator stages → injects via the existing Hook Lab v1 Stage→Inject path. Trace adds **zero new device-touching surface area** — all dynamic effects flow through `generate_frida_hook` + the Stage→Inject UI shipped in DEC-023 sub-steps 4.5 / 4.7. See **DEC-024** for the platform-neutral data model, the static→LLM split, the closure / hop / methods caps, the bypass-planner template binding, the risk taxonomy, and the rename policy.

| #    | Title | Deliverable | Depends on |
|------|-------|-------------|-----------|
| 10.0 | **[x] Planning checkpoint + DEC-024** *(landed 2026-04-28 — docs only)* | DEC-024 added to `docs/DECISIONS.md` (workflow framing, platform-neutral data model, static-enumerate / LLM-interpret split, template-bound bypass planner, ≤ `MAX_TRACE_HOPS = 3` (hard cap 6) + `MAX_TRACE_METHODS = 30`, intra-procedural slicing limitation, Cytoscape demotion + per-app default-mode follow-up, per-app SQLite `trace.sqlite` cache layout, risk taxonomy on `BypassPlan` with operator-configurable threshold, deterministic tests + ~80-test target). DEC-016 cross-link bullet added (Phase 10 reuses call-graph SQLite store unchanged). DEC-023 cross-link bullet added (Phase 10 reuses every substrate; Hook Lab → Lab rename pinned forward; `HookLabTab.tsx` / `HookLabCodeView` / `#/hook` URL hash code rename deferred to **10.6**). Phase 10 stub added to `docs/DESIGN_DOC.md` (after Phase 9). This `TASKS.md` Active Task line + Phase 8 / Phase 9 status rows + Phase 10 status row updated; Naming note added under § Interactive RE Workbench heading. **No code changes, no test changes** — `git diff` shows only `.md` files. | — |
| 10.1 | **[x] Decision-point extraction** *(landed 2026-04-28)* | New module `androscan/analysis/decisions.py` — third Smali pass over the same apktool tree `smali_parser.py` already walks twice (no edits to existing modules). `parse_decisions(roots, classes) -> (list[MethodDecisions], DecisionsParseSummary)` emits one `MethodDecisions` per method with at least one branch (linear methods excluded; full enumeration via the existing `ClassDecl.methods` list). Recognises all 12 if-* opcodes (`if-eq` / `if-ne` / `if-lt` / `if-le` / `if-gt` / `if-ge` / `if-eqz` / `if-nez` / `if-ltz` / `if-lez` / `if-gtz` / `if-gez`) plus `packed-switch` and `sparse-switch`. Switch data blocks parsed inline via deferred-back-fill (`_pending_switches` in `_MethodState`); each switch emits one `Branch` per case label + a `("default", None)` fall-through. Per-method `label_index: tuple[(label_name, instruction_index), ...]` lets 10.3's basic-block walker resolve any branch target without re-parsing. Instruction indexing is conservative — only lowercase-opcode lines advance the index, so labels point at real next-instruction positions. Fail-soft on malformed switch blocks via `_DecisionParseError` → `DecisionsParseSummary.parse_errors`; rest of file keeps parsing. Platform-neutral data model in new `androscan/analysis/trace_types.py`: `MethodRef` + `FieldRef` (Java-form names + raw Smali descriptors with `smali_signature` round-trip joining `call_graph.nodes`); `DecisionKind` (string-valued enum; `is_two_register_predicate` / `is_switch` accessors); `Branch(label, target_label: Optional[str])` (`None` = fall-through); `DecisionPoint(method, instruction_index, source_line, kind, predicate_registers, branches)`; `BehaviorAnchor` + `BypassPlan` shipped as minimal shells locking field names 10.4 / 10.5 will populate without breaking the contract. **13 tests** in `tests/test_decisions_extract.py` covering MethodRef + FieldRef round-trip + ValueError, linear-method exclusion, summary counters, all 12 if-* kinds (zero + two-register variants in dedicated coverage methods), source-line attachment, label_index resolution, packed-switch + sparse-switch branch emission, sparse-switch key-string preservation (`"case 0xa"`), multi-dex coverage via `smali_classes2/HiddenGate.smali`, and `DecisionPoint` JSON round-trip via `dataclasses.asdict` + `json.dumps`. Three new fixture .smali files under `tests/fixtures/trace_smali/`. Full suite **614 / 614 green** (was 601; +13). No FE changes, no schema changes, no new optional deps. | DEC-024 |
| 10.2 | **[x] Backward slicing for predicate origin** *(landed 2026-04-28)* | New module `androscan/analysis/slicing.py` — pure intra-procedural backward slicer. `slice_predicate_origins(method_decisions, *, max_walk=64) -> MethodDecisions` (frozen-dataclass-replace, no input mutation) plus `slice_one_decision(decision, instructions)` for 10.5's per-anchor walk. Five `*Origin` dataclasses added to `androscan/analysis/trace_types.py`: `MethodCallOrigin(method, invoke_kind)`, `FieldReadOrigin(field, is_static)`, `ConstOrigin(value, smali_op)`, `ParamOrigin(register)`, `CompositeOrigin(reason)`. Each carries a `kind` discriminator field for clean JSON round-trip. `DecisionPoint` extended with `predicate_origin: Optional[PredicateOrigin] = None` (additive default → 10.1 tests still pass). Walker follows `move(-wide\|-object)?(/from16\|/16)?` chains transparently; `move-result-*` looks back one step for the preceding `invoke-*` and emits MethodCallOrigin from its target signature; `iget-*` / `sget-*` emit FieldReadOrigin; `const(-*)?` emits ConstOrigin with literal text preserved verbatim; `move-exception` and any unrecognised defining opcode emit CompositeOrigin with the opcode as `reason`. Skip-set excludes `iput-*` / `sput-*` / `aput-*` / `invoke-*` / `goto*` / `return*` / `throw` / `monitor-*` / `check-cast` / `nop` / `fill-array-data` / `filled-new-array*`. Walk exhaustion via `max_walk` → None honestly per DEC-024. Walk reaches start-of-method with a `pN` register tracked → ParamOrigin; with a `vN` tracked → None (defensive). **Two-register policy:** slicer slices each operand independently then combines via `MethodCall > FieldRead > Param > Const > Composite > None`; ties to LHS for stable source-order results. **Small amendment to `decisions.py`:** `MethodDecisions` gained optional `instructions: tuple[str, ...] = ()` populated via a tiny `_emit_instruction(raw)` helper in `_MethodState` (single source of truth for the index counter + instructions list). **16 tests** in `tests/test_decisions_slicing.py`: `predicate_origin` defaults to None on 10.1 output; `instructions` populated and pointing at if-* / switch lines at the right indices; per-variant slicer assertions for all 5 origin kinds (with `iget` + `sget` separately covered for FieldRead); arithmetic + instance-of + move-exception for Composite; move-chain follows aliases; two-register const-vs-field returns FieldRead; two-register both-MethodCall returns LHS; max_walk exhaustion returns None (and same method with full walk resolves); JSON round-trip per variant verifies the `kind` discriminator. New fixture `tests/fixtures/trace_smali/smali/com/trace/Slices.smali` (~13 methods + helpers). Full suite **630 / 630 green** (was 614; +16). No FE / schema / new-deps changes. | DEC-024 |
| 10.3 | **[x] Heuristic branch outcome classifier** *(landed 2026-04-29)* | New module `androscan/analysis/branch_classifier.py` — pure deterministic classifier. Public API: `classify_branch_outcomes(method_decisions) -> MethodDecisions` (frozen-dataclass-replace, mirrors 10.2's slicer API exactly so 10.5 can chain `parse_decisions → slice_predicate_origins → classify_branch_outcomes` without bookkeeping); `classify_one_decision(decision, instructions, label_index)` for tests / 10.5's per-anchor walk. Two new dataclasses in `androscan/analysis/trace_types.py`: `BranchVerdict(branch_label, verdict, score, reasons)` and `BranchOutcome(verdicts, confidence, reasons)`. `DecisionPoint` extended with `branch_outcome: Optional[BranchOutcome] = None` (additive default; 10.1 + 10.2 tests still green). **Heuristic catalog (locked v1):** strong DENY (-1.0): `throw vN` / `Ljava/lang/System;->exit` / `Landroid/os/Process;->killProcess`. Strong ALLOW (+1.0): `L...;->setResult(...)`. Moderate DENY (-0.7): `L...;->finish\|finishAffinity\|finishAndRemoveTask()V` *only when no preceding `setResult` in the same basic block* (legitimate "report-then-close" allow-flow suppressed via order-aware walk); `const-string vN, "..."` matching the curated deny-keyword regex. Moderate ALLOW (+0.7): `L...;->startActivity(ForResult)?(...)`. Weak DENY (-0.3): branch-length ratio ≥ 3:1 with shorter ≤ 3 instructions, only when both branches are otherwise neutral, only for 2-branch ifs (early-exit-gate pattern). **Verdict thresholds:** `score ≤ -0.5 → "deny"`, `score ≥ +0.5 → "allow"`, else `"neutral"`. **Confidence tiers** based on `max(|branch.score|)`: `≥ 1.0 → 1.00` / `≥ 0.7 → 0.85` / `≥ 0.3 → 0.45` (below `LLM_RECLASSIFY_THRESHOLD = 0.6` → 10.5 LLM re-classifies) / else `0.00`. Pre-classify `branch_outcome=None`; post-classify always populated even for all-neutral / 0.0-confidence cases so 10.5 distinguishes "not run" from "run, found nothing". **Curated deny-keyword regex** extends DEC-024's spec with two strict-additive improvements: `\b...\b` boundaries (no `rootView` false-positive on `root`); optional `(?:ed\|ing)?` / `(?:ger\|ging)?` suffixes on `tamper` / `root` / `debug` (the canonical "Device is rooted" / "App was tampered" detection strings now match — without these the spec would miss them). **Basic-block walker** stops at first terminator (`return*` / `throw` / `goto*` / `if-*` / `*-switch`); terminator IS part of the block (so `throw` itself is scoreable in-place). Fall-through branches start at `decision_instruction_index + 1`. **+19 tests** in `tests/test_branch_classifier.py`: pre-classify `None` baseline; frozen-replace input non-mutation; per-signal assertions for throw / System.exit / killProcess / setResult / startActivity / finish-without-setResult; setResult-suppresses-finish (no "finish without setResult" reason in the verdict's reasons list); string-keyword `rooted` (suffix-aware) / `PREMIUM` (case-insensitive) / `unauthorised` (British spelling); `rootView` substring excluded by word-boundary contract; length-ratio yields weak DENY 0.45 explicitly *below* `LLM_RECLASSIFY_THRESHOLD`; symmetric-no-signals NEUTRAL/NEUTRAL/0.0; packed-switch with throw/setResult/neutral cases + neutral default classifies each independently with confidence 1.0 from case 0; clean DENY/ALLOW two-branch split; 4-tier confidence pinning test; `BranchOutcome` JSON round-trip via `asdict` + `dumps`. New fixture `tests/fixtures/trace_smali/smali/com/trace/Outcomes.smali` (15 methods + constructor, hand-crafted for deterministic per-method assertions). One pre-existing 10.1 assertion loosened (`summary.switches == 2 → >= 2`) since the new fixture introduces a third packed-switch — original intent was "at least the packed + sparse from SwitchCases". Full suite **649 / 649 green** (was 630; +19). No FE / schema / new-deps changes. **Out of scope (per DEC-024 v1):** cross-method analysis, inter-procedural string flow (only immediate-block `const-string` literals matched), locale-aware string matching (English-only regex; localised gates fall to LLM re-classification), `setResult(RESULT_OK)` vs `RESULT_CANCELED` discrimination. | 10.2 |
| 10.4 | **[x] Bypass planner** *(landed 2026-04-29)* | New module `androscan/analysis/bypass_planner.py`: maps `(DecisionPoint, BranchOutcome)` to zero or more `BypassPlan` instances, each referencing one of the existing `frida_hooks/` templates (`entry_exit_log` for observation; new templates added here for *override* — `force_return_value`, `force_string_compare_equal`, `force_method_skip` — all template-bound, no free-form JS per DEC-024). Each `BypassPlan` carries `(template_id, params: dict[str, str], rationale: str, risk: "low" \| "medium" \| "high", risks: list[str])`. Risk threshold from `trace.bypass_risk_max` config knob (default `medium`); plans above threshold are flagged but not emitted (UI surfaces them behind an "Advanced" expander per DEC-024). New templates ship with the same DEC-023 sub-step 4.4 contract (declared params + JS `{{` brace escaping + non-empty `pentester_summary_template` + the fail-closed `TestRegistryFailClosed` walk extends to them automatically). New tests in `tests/test_bypass_planner.py` (~20 tests — per-template plan emission + risk threshold filtering + rationale text shape + new templates render under the existing registry-walk invariants). | 10.3 + Hook Lab 4.4 |
| 10.5 | **[x] `trace_behavior` LLM-tier skill** *(landed 2026-04-29)* | New `androscan/skills/trace_behavior.py` — read-only LLM-tier skill (`requires_confirmation=False` per DEC-022 / DEC-024). Takes `entry_method_smali_id` (or resolves it from a `BehaviorAnchor` payload) + optional `hops` (clamped to `trace.max_hops_hard_cap`); walks the call-graph forward closure ≤ `MAX_TRACE_HOPS` and ≤ `MAX_TRACE_METHODS = 30`, runs `decisions.extract` + `slicing.slice` + `branch_classifier.classify` + `bypass_planner.plan` over each method in the closure, builds a populated `BehaviorAnchor` payload, and feeds it to the LLM **once per anchor** (per-decision LLM calls explicitly rejected per DEC-024). Asks the LLM to (a) re-classify gates with `confidence < 0.6`, (b) author rationale strings, (c) propose template-bound bypass plans for cases the deterministic planner returned empty. Persists the populated `BehaviorAnchor` to `apps/<app_id>/.decompiled/<sha>/trace.sqlite` (schema_version 1; rows: `(entry_method_smali_id, hops, payload_json, created_at)`). Fail-open on missing app context, unbuilt call graph, or unresolved entry method (mirrors `query_call_graph` / `search_decompiled_sources` per DEC-024). New `trace.max_hops_default` / `trace.max_hops_hard_cap` / `trace.bypass_risk_max` config knobs in `Config` / `CONFIG_FIELD_MAP` / `LIVE_RELOADABLE_FIELDS` / `_merge_from_yaml` / env / `global_config.yaml`'s new `trace:` section. Skill registered in `androscan/skills/__init__._SKILL_MODULES`. New tests in `tests/test_trace_behavior_skill.py` (~15 tests — registration, fail-open paths, closure walk against fixture call graph + Smali, mocked LLM round-trip with stub interpretation, payload persistence + re-load, risk-threshold filtering surfaces in skill output). | 10.4 + Hook Lab 4.7 |
| 10.6 | **[x] REST endpoint + tab shell + Hook Lab → Lab code rename** *(landed 2026-04-29)* | New routes in `androscan/web/trace_routes.py` factory-built via `build_trace_router(get_config, get_skill_runner, run_folder_root, web_apps_root)` and wired into `androscan/web/app.py`: `GET /api/trace/{app_id}/anchor?entry=<smali_id>&hops=<n>` (returns the populated `BehaviorAnchor` JSON; 404 on missing app or unresolved entry; serves cached payload from `trace.sqlite` when present), `POST /api/trace/{app_id}/anchor` (forces re-trace, invalidates cache row, re-runs `trace_behavior` skill), `DELETE /api/trace/{app_id}/anchor/{anchor_id}` (cache invalidation). **Tab shell:** `HookLabTab.tsx` renamed to `LabTab.tsx`; `HookLabCodeView` → `LabCodeView`; URL hash `#/hook` → `#/lab` with a one-shot redirect from `#/hook` for backwards-compatibility on bookmarks; `frontend/README.md` updated. Lab tab gains a 3-mode left-rail switcher: `Trace | Manual Hooks | Graph` defaulting to `Trace` (`Manual Hooks` = today's `HookBuilder.tsx` flow; `Graph` = today's `CallGraphView.tsx`). One-time tooltip on first launch after the upgrade explains the new default. The per-tab chat-prompt key in `androscan/web/chat.py` (`hook_lab` → `lab`) is updated with a back-compat alias so existing transcripts route correctly. Brief Ask-mode planning checkpoint at the top of 10.6 to confirm the `BehaviorAnchor` JSON wire shape before 10.7 starts depending on it. New tests in `tests/test_trace_routes.py` (~12 tests — full HTTP surface, cache hit / miss, force re-trace, 404s, route registration + DI seams). Code rename touches `androscan/web/chat.py`, `frontend/README.md`, `androscan/web/frontend/src/{App,context/WorkbenchContext,components/TabBar,tabs/*}.{ts,tsx}` and CSS under `App.css` (`.hookbuilder-*` namespaces stay — they refer to the Manual Hooks mode component, which keeps its module name). | 10.5 |
| 10.7 | **[x] Trace mode frontend (the headline UI)** *(landed 2026-04-29)* | Six new components under `androscan/web/frontend/src/components/trace/`: `BehaviorAnchorCard.tsx` (anchor header — class.method(params)return signature with descriptors muted, "Open in Inspect" button writing `pendingCodeNav` + `setTab("inspect")`, italic LLM-authored `rationale`, status footer `hops=N · M decisions · K plans (+J advanced) · from cache | freshly built`); `DecisionTimeline.tsx` exporting an inline `DecisionCard` (top-down `<ol>` keyed by `(method.smali, instruction_index, position)`; per-card method header + `kind` opcode badge + per-branch `BranchOutcomeBadge` row; `flagged` / `LLM-refined` / `low confidence` pills derived from `low_confidence_decision_indices` + `BranchOutcome.confidence == 0.75` (the `LLM_RECLASSIFY_CONFIDENCE` stamp from the 10.5 skill); collapsible body showing `predicate_registers` / `branches` / `predicate_origin` / cross-branch reasons); `BranchOutcomeBadge.tsx` (pure presentational pill — `deny` red filled / `allow` green filled / `neutral` grey outline; tooltip surfaces signed `score` + per-branch `reasons`; dashed-outline + dimmed when `currentlyLowConfidence`); `PredicateOriginView.tsx` (discriminated-union renderer over the 5 `PredicateOrigin.kind` variants — `method_call` clickable to write `pendingCodeNav` + flip the tab to Inspect, `field_read` shows the field signature with `iget` / `sget` tag, `const` shows the literal verbatim with the `const-*` opcode tag, `param` shows the raw `pN` register, `composite` shows the smali opcode that produced the value); `BypassPlanCard.tsx` (per-plan card — `risk` pill (low green / medium yellow / high red), monospace `template_id`, prose `rationale`, "Hooks: <method>" + "Bypasses: <gate> #N" captions when `target_method` / `source_decision_method` populated, params `<details>` with 2-column dl grid, secondary `risks` `<details>`, **"Stage in Manual Hooks"** button writing `pendingHookPrefill = {appId, templateId, params, sourceLabel: "Trace plan: <method>"}` + flipping `labMode` to `"manual-hooks"`); `TraceBanners.tsx` exporting `TraceTruncatedBanner` (renders when `anchor.truncated`; surfaces operator-supplied `hops` + the `MAX_TRACE_METHODS=30` ceiling) + `TraceIncompleteBanner` (renders when `anchor.incomplete`; explains intra-procedural slicer limits per DEC-024). **`LabTraceMode.tsx` rewritten** from the 10.6 placeholder into the real Trace mode shell — top-of-pane form (entry-method smali input + hops stepper clamped 1..6 + Build button + Force re-trace button), result region (idle / loading / missing → "Build trace" CTA / building / error → retry-as-Build / loaded → BehaviorAnchorCard + DecisionTimeline + BypassPlanCard list with `<details>` "Advanced (N higher-risk plans)" expander wrapping `advanced_plans`), `CachedAnchorsList` picker (smaller `<ul>` of `(entry_smali_id, hops, created_at)` rows from `GET /api/trace/{app_id}/anchors`; click loads, × deletes), `TraceStatusFooter` carrying the same status fan-out the 10.6 placeholder shipped (decompile / call graph / trace cache readiness + cached anchor count). Lifecycle owned by the new **`useTraceAnchor(appId, entry, hops)` hook** in `src/api/trace.ts` (state machine: `idle` → `loading` (GET) → `loaded` from cache OR `missing` OR `error` → operator clicks Build → `building` (POST) → `loaded` from build OR `error`; ref-based stale-response guard mirrors `HookBuilder.renderInflightKeyRef`; `force` flag plumbed through to `buildTraceAnchor` for "Force re-trace"). **`api/trace.ts`** also gains tightened types — was `unknown[]` placeholders in 10.6, now `MethodRef` / `FieldRef` / `Branch` / `BranchVerdict` / `BranchOutcome` / `DecisionKind` / `DecisionPoint` / `BypassPlan` / `PredicateOrigin` (discriminated union over `kind`) all match `dataclasses.asdict` output verbatim. **Cross-mode plumbing in `WorkbenchContext.tsx`:** `LabMode` (`"trace" | "manual-hooks" | "graph"`) + `setLabMode` lifted out of `LabTab.tsx`'s local state (with `localStorage["androscan.lab.mode"]` persistence still owned in the context provider) so external callers (the trace components themselves; 10.8's Mirror "Trace this behaviour" button) can flip the mode programmatically; `pendingHookPrefill` + `setPendingHookPrefill` mirror the `pendingCodeNav` plumbing — `BypassPlanCard` writes from Trace mode, `HookBuilder.tsx` consumes on mount/change and clears via `setPendingHookPrefill(null)`. **`LabTab.tsx`** updated to read `labMode` / `setLabMode` from context (was local state); `LAB_MODE_STORAGE_KEY` + `loadStoredLabMode` constant deleted. **`HookBuilder.tsx`** wired with the prefill consumer — ref-based handoff (`stagedParamsRef`) so the existing template-change effect, which rebuilds `paramValues` from declared defaults on `selectedTemplateId` change, drains the staged values *after* defaults rather than clobbering them; "Staged from Trace" pill on the header (with the `sourceLabel` from the prefill) drops the moment the operator picks a different template or edits any param. ~430 lines of new CSS in `App.css` under `.trace-form` / `.trace-empty` / `.trace-result` / `.trace-section` / `.trace-anchor-*` / `.trace-banner-*` / `.trace-decision-*` / `.trace-branch-badge-*` / `.trace-predicate-origin-*` / `.trace-bypass-plan-*` / `.trace-cached-anchors` / `.hookbuilder-staged-pill` namespaces — colour-token discipline matches the rest of `App.css` (`var(--ok)` green for `allow` / Plan A `low` risk, `var(--err)` red for `deny` / Plan C `high` risk, `var(--warn)` yellow for `medium` risk + truncated banner, `var(--accent)` blue for the staged-from-Trace + LLM-refined pills, `color-mix(in srgb, ...)` for soft fills throughout). **No frontend tests** (no test runner configured project-wide; the JSON wire-shape contract is locked by 10.6's `tests/test_trace_routes.py`). Full suite **745 / 745 green** unchanged from 10.6 (no backend changes in 10.7); `npx tsc --noEmit` clean; `npm run build` clean — **709 modules** (was 703 in 10.6; +6 new component files + the hook), 1.19 MB JS / 74 KB CSS / 372 KB gzipped (modest growth from 10.6's 1.18 MB / 63 KB / 367 KB attributable to the ~600 lines of new TSX + ~430 lines of new CSS). | 10.6 |
| 10.8 | **[x] Mirror → Trace integration + chat hook + docs sweep** *(landed 2026-04-29)* | Wire the existing `POST /api/inspect/map`'s `resolution.best` (the fused click-to-code pick from Phase 7 / DEC-019) into Trace mode: a "Trace this behaviour" button on the Inspect tab's `BestBanner` cross-tab-switches to Lab → Trace mode and seeds the entry method via `pendingTraceEntry` plumbing in `WorkbenchContext`. New `ChatAttachment` of kind `trace` for the Lab chat dock — populates from the active `BehaviorAnchor` (entry method header + the per-decision verdict list + the top-3 ranked bypass plans), capped client-side at the same 6_000 chars as the `code` attachment. `buildLabChatContextSummary` updated to enumerate the new attachment. **Docs sweep:** `docs/STATE.md` Phase 10 sub-bullets (10.1 → 10.8 in the same compact sub-bullet style as Hook Lab 4.1 → 4.8, with test counts + bundle deltas); this `TASKS.md` Active Task → "Phase 10 v1 complete" with date; `DESIGN_DOC.md` Phase 10 stub expanded to "landed YYYY-MM-DD" with cross-link to `docs/STATE.md`; `DECISIONS.md` DEC-024 gets a "v1 complete" closing note (DEC-024 stays Active for the cross-platform follow-ups); `KNOWN_ISSUES.md` captures any v1 limitations that surfaced during 10.5–10.7 (intra-procedural slicing false-negative rate, per-overload precision in trace gates if relevant, etc.); `SAFETY_AND_SECURITY.md` §12 augmented with a Trace mode subsection confirming the read-only / inject-via-existing-Hook-Lab-controls posture. Final test count target: ~80 new tests across Phase 10 (versus Hook Lab v1's +175); full suite green; `tsc --noEmit` + `vite build` clean. | 10.7 |

**Brief Ask-mode planning checkpoint at the top of 10.6** to confirm the `BehaviorAnchor` JSON wire shape (field names, type discrimination on `predicate_origin`, optional vs. required keys, version field) before 10.7 starts depending on it. Mirrors the 4.1 SQLite-schema checkpoint that locked the call-graph store before the routes API.

**Risks (per DEC-024):**
- Static-only mode (LLM unavailable): the deterministic stack (decisions + slicing + classifier + planner) ships fully usable without any LLM call — `confidence < 0.6` gates simply stay flagged as "needs review" rather than getting LLM re-classification, and the deterministic planner's bypass plans are still emitted. Trace mode degrades gracefully when Ollama is offline.
- Closure walks on dense apps: hard cap `MAX_TRACE_METHODS = 30` (in code, not config) prevents pathological closures; UI surfaces a "trace truncated, narrow the entry method" affordance when the cap is hit.
- LLM-proposed bypass plans are still template-bound (no free-form JS in v1 per DEC-024); the LLM's job is *interpretation + plan selection*, not script authorship. Free-form JS for bypasses lives in `generate_frida_hook` (DEC-022's confirmation gate) and stays as the operator-driven path.
- Intra-procedural slicing only (per DEC-024); `predicate_origin: None` is honestly surfaced and the UI shows a "trace may be incomplete" banner rather than silently fudging the answer.

---

**§ Phase 11 — Behavior Trace v2 — sub-step backlog** *(strictly linear; one sub-step per Agent-mode session; locked in **DEC-025** — operator-UX polish bundle (Tier 1 a+b+c) + bounded inter-procedural slicer (Tier 2 d / closes ISSUE-013))*

Phase 11 ships the v2 follow-up to Phase 10's Behavior Trace (DEC-024). The scope is operator-feedback-driven from the first week of v1 use — three concrete UX deliverables (decision-timeline clarity polish, "Trace from here" cross-tab button on call-graph nodes, `BehaviorAnchor`-aware overlay on the Manual Hooks Cytoscape pane) plus the largest known precision gap in v1's planner (ISSUE-013 — bounded inter-procedural backward slicing for `predicate_origin` via depth-2 descent + type-driven `is_stateless` analyzer + same-class field-write-site walking). Schema bump on `trace.sqlite` (`SCHEMA_VERSION` 1 → 2) wipes interpretation-stale v1 cached anchors via the existing reader's "drop-the-cache" path; LLM budget bumps (`num_ctx: 16384` + `num_predict: 12288`) cover the ~2× input prompt growth + ~1.5× output growth from the deeper `predicate_origin` chains. **Explicitly NOT in v2 per DEC-025:** ISSUE-014 (per-overload precision on two-register comparisons — schema bump on `DecisionPoint.predicate_origin` to a tuple), auto-verify bypass after injection, free-form LLM JS for bypass plans, per-app TTL on cached anchors, iOS / WebView / native-binary `BehaviorAnchor` adapters. See **DEC-025** for the scope rationale, the four pre-answered planning-checkpoint questions, and the v3 deferral list.

| #    | Title | Deliverable | Depends on |
|------|-------|-------------|-----------|
| 11.0 | **[x] Planning checkpoint + DEC-025** *(landed 2026-04-30 — docs only)* | DEC-025 added to `docs/DECISIONS.md` (v2 scope + four pre-answered open questions: LLM budget = bump both `num_ctx` and `num_predict`; deny-list shape = type-driven `is_stateless` analyzer; cache invalidation = `trace.sqlite` schema_version 1 → 2; UI density on deeper chains = pill-only depth indicator on `PredicateOriginView`; v3 deferral list — ISSUE-014, auto-verify, free-form LLM JS, per-app TTL, iOS adapters). DEC-024 closing note extended with a "v2 follow-up DEC ratified 2026-04-30 as DEC-025" cross-link bullet. Phase 11 status row added to the § Sub-task status (implementation) table; this `TASKS.md` Active Task line flipped from "Phase 10 v1 complete" to "Phase 11 — Behavior Trace v2 active" + parallel CI.0 thread call-out. Phase 11 sub-step backlog table 11.0 → 11.7 added (this table; 11.0 marked `[x]`); Phase 4 CI mini-backlog added below it. Phase 11 stub added to `docs/DESIGN_DOC.md` (after Phase 10). `docs/STATE.md` "Next expected milestone" section reworked from the v2-candidate ranking to a Phase 11 Active stub + the deferred-to-v3 list moved to its own bullet. `docs/KNOWN_ISSUES.md` ISSUE-013 status updated to "Open (v2 planned — Phase 11 11.4 → 11.6)" with a recommended-fix cross-link to this DEC; in-passing fix for the file-name + `*Origin` variant-name drift in ISSUE-013's body (`trace_predicate.py` → `slicing.py`; `FieldLoadOrigin` → `FieldReadOrigin`; `UnresolvedOrigin` → `predicate_origin: None`) so the issue references the actual code as it shipped in 10.2. **No code changes, no test changes, no `global_config.yaml` changes** — `git diff` shows only `.md` files. | DEC-024 |
| 11.1 | **[x] Tier 1 (a) — Decision-timeline UX clarity polish** *(landed 2026-04-30 — frontend-only)* | `DecisionTimeline.tsx` header gains an inline disclosure block: *"Listed in static traversal order (BFS over the call graph, then source order within each method) — not runtime execution order."* Each `DecisionPointCard` gains a "Verify with runtime trace" button (next to the existing collapsible expand affordance) that writes `pendingHookPrefill = { appId, templateId: "entry_exit_log", params: { class_name, method_name, label }, sourceLabel: "Trace verify: <class>.<method>" }` (where the label is auto-generated from the decision's enclosing method) + flips `labMode` to `"manual-hooks"` so the operator lands in `HookBuilder` with the form pre-populated and the mode visibly switched — same `pendingHookPrefill` plumbing 10.7 already established for `BypassPlanCard`'s "Stage in Manual Hooks" button. High-confidence hits (computed as `BranchOutcome.confidence >= 0.85` AND at least one branch verdict is `deny` or `allow` — not `neutral`) get a one-line operator-action hint above the card body: *"This is a candidate gate — review the bypass plans below or stage `force_return_value` to flip the verdict."* No schema changes, no new components — extends existing ones. New CSS in `App.css` under `.trace-decision-timeline-disclosure` / `.trace-decision-action-hint` / `.trace-decision-verify-btn` namespaces with the existing colour-token discipline (`var(--accent)` blue for the disclosure + verify button; `var(--ok)` soft fill for the high-confidence action hint). **No backend changes; no backend tests.** Frontend verification: `tsc --noEmit` + `vite build` clean (target: 709 modules unchanged; +~1 KB CSS for the new namespaces, +~0.5 KB gzipped JS for the verify-button handler). | 11.0 |
| 11.2 | **[x] Tier 1 (b) — "Trace from here" on call-graph nodes** *(landed 2026-04-30 — frontend-only)* | Right-click context menu in `androscan/web/frontend/src/components/CallGraphView.tsx` gains a new entry **"Trace this method"** alongside the existing "Open in Inspect" / "Focus subgraph here". On click, computes the entry method's smali signature from the node's `(class.smali_class, method.method_name, method.descriptor)` triple (the call graph already stores all three on every method node — no new backend lookup needed; `node.smali_id` is the pre-computed canonical form), writes `setPendingTraceEntry({ appId, entryPrefix: <smali_id>, sourceLabel: "Graph → <class>.<method>" })` (re-using the 10.8 plumbing established for the Inspect → Trace handoff — the `pendingTraceEntry` consumer in `LabTraceMode.tsx` already auto-fires on complete signatures via `looksLikeCompleteSmaliSignature`), calls `setLabMode("trace")`, and finally `setTab("lab")`. Edge case: external nodes (`is_external=1` in the call graph SQLite) get the menu item disabled with a tooltip *"External callee — no source available to trace"* (same disabled-state pattern the Cytoscape view already uses for the existing "Open in Inspect" entry on external nodes). New CSS for the disabled menu-item state under `.cy-context-menu-item-disabled` (one rule). **No backend changes; no new backend tests.** Frontend verification: `tsc --noEmit` + `vite build` clean. | 11.1 |
| 11.3 | **[x] Tier 1 (c) — `BehaviorAnchor`-aware overlay on Manual Hooks call-graph pane** *(landed 2026-04-30 — backend route + frontend overlay layer)* | **Backend.** New route `GET /api/trace/{app_id}/anchored-methods` in `androscan/web/trace_routes.py` (factory-built via the existing `build_trace_router(...)` plumbing — no new DI seams) returns `{app_id, sha, methods: [{class_smali, method_name, hops, created_at}, ...], total}` derived from a cheap `SELECT entry_smali_id, hops, created_at, payload_json FROM anchors` over `trace.sqlite` plus a per-row payload unpack to enumerate every method in each cached anchor's `decisions` list (deduped on `(class_smali, method_name)` server-side; cached anchor count typically ≤ 50 per app so the per-row unpack is cheap and synchronous). No new SQLite schema (the new endpoint is a query over the existing `anchors` table — invariant under 11.6's `SCHEMA_VERSION` bump because `read_anchor`'s payload-decode round-trip already handles both v1 and v2 payload shapes via the `_payload_from_json` helper). 404 on missing app or unbuilt trace cache; empty array on built-but-empty cache; surfaces `error` field on payload-decode failures so the frontend can render an operator-actionable empty state. **Frontend.** `androscan/web/frontend/src/api/trace.ts` gains a `listAnchoredMethods(appId)` client function + `AnchoredMethod` type. `CallGraphView.tsx` gains an optional `anchoredMethods?: ReadonlySet<string>` prop that mirrors the existing `hitsByMethod: ReadonlyMap<string, number> \| null` pattern (uses the same `hitKey(className, methodName)` join helper for cohesion — producer + consumer share one canonical key). When non-null and a method node's `(class.class_name, method_name)` is in the set, render a small purple "⚓" glyph in the corner + a tooltip line `trace cached: <hops> hops · <created_at relative time>` (the relative time computed via the existing `formatRelativeTime` helper); the glyph is layered after the existing `.hit` / `.has-hits` rules so the bold-cyan Frida overlay still wins where both apply (Frida hits beat trace anchors visually because they're live data — DEC-023 / 4.8 sets the same precedence rule for reflection vs. focus-root vs. hit). `LabTab.tsx` fetches the anchored set when `labMode === "manual-hooks"` (only — Trace mode and Graph mode don't need the overlay; Trace mode already shows the anchor data inline in `CachedAnchorsList`) and on every successful trace build / delete (re-uses the existing `reloadTick` pattern from 10.7's `LabTraceMode`). New CSS in `App.css` under `.cy-trace-anchored-glyph` and the `.has-trace-anchor` Cytoscape selector. New `TRACE_PURPLE = "#9d6dff"` colour token shared between the Cytoscape stylesheet and the tooltip HTML so the overlay reads as one visual unit (matches DEC-023's `HIT_CYAN` token pattern). **Tests.** `tests/test_trace_routes.py` (+~6 — new `/anchored-methods` route happy path against fixture, empty set on built-but-empty cache, 404 on missing app, 404 on unbuilt cache, payload-decode failure surfaces `error`, dedupe across multiple cached anchors that share methods). Frontend verification: `tsc --noEmit` + `vite build` clean. | 11.2 + 10.5's `trace.sqlite` |
| 11.4 | **[x] Tier 2 (d) part 1 — Bounded inter-procedural slicer descent + type-driven `is_stateless` analyzer** *(landed 2026-04-30 — slicer + skill wiring + 27 new tests; deferred from spec: `trace.max_slice_depth` config knob → 11.6 per planning checkpoint Q4)* | Extend `androscan/analysis/slicing.py` with: (1) **`_descend_into_callee(method_call_origin, *, depth, max_depth, visited, classes_by_smali) -> PredicateOrigin`** — when the slicer terminates at a `MethodCallOrigin` AND `depth < MAX_SLICE_DEPTH = 2` (new module constant; `trace.max_slice_depth` config knob added in 11.6 alongside the LLM-budget bumps; hard cap 4 in code) AND the callee resolves in the call graph (not external) AND `is_stateless(callee_method, classes_by_smali, visited)` returns True (see (3) below), descend into the callee's body and re-run `slice_predicate_origins` on its `return-*` instruction's source register. The descended `PredicateOrigin` *replaces* the original `MethodCallOrigin` in the slicer's output (operator sees the new terminal, not the chain that produced it — depth pill UI surfaces the "via N helper method(s)" signal per DEC-025 open question 4). Visited set keyed on `(class_smali, method_name, descriptor)` to terminate cycles. (2) **`_DescentBudget`** dataclass shared between method descent and 11.5's field-write-site walking: `(remaining_depth: int, visited: set[tuple[str, str, str]])`. Both passes draw from the same budget so an aggressive method descent doesn't exhaust budget for a subsequent field-write-site walk (closed economy per DEC-025). (3) **`is_stateless(method, classes_by_smali, visited) -> bool`** — type-driven analyzer (new function, ~120 LOC). Walks the method's body looking for *side effects*: `iput-*` / `sput-*` / `aput-*` (field / array writes — definitely stateful), `invoke-*` to non-stateless callees (recursive — reuses the same visited set to terminate cycles), `monitor-*` (synchronization — defensive: assume stateful), exception throws (`throw vN`), and reflection (any callsite the call graph flagged `may_have_unresolved_reflection`). Pure-arithmetic / pure-comparison / `move-*` / `return-*` / `nop` are stateless. New `_STATELESS_LIB_DENYLIST` constant in `slicing.py` listing stdlib classes we *can't* walk into (no Smali source for them, so we have to assume statelessness from the class name): `Ljava/lang/Math;`, primitive boxing classes (`Ljava/lang/Integer;`, `Ljava/lang/Long;`, `Ljava/lang/Boolean;`, `Ljava/lang/Float;`, `Ljava/lang/Double;`, `Ljava/lang/Byte;`, `Ljava/lang/Short;`, `Ljava/lang/Character;`), `Ljava/lang/String;` getter methods (length, charAt, substring, equals, hashCode, isEmpty, indexOf — explicitly listed; `concat` / `replace` etc. NOT included since they allocate), `Lkotlin/jvm/internal/Intrinsics;`. Hand-curated, small, easy to audit, lives next to `_DescentBudget`. **Brief Ask-mode planning checkpoint at the top of 11.4** to confirm the `_DescentBudget` shape and the `_STATELESS_LIB_DENYLIST` initial seed before code lands (mirrors the 10.6 `BehaviorAnchor` JSON wire-shape checkpoint; one-tool-call sanity check on the worst-case fixture cost so we don't ship a slicer that quadratics on a hub-method like `Log.d`). Tests in `tests/test_decisions_slicing.py` (+~18–20 — single-hop descent into helper-method getter; two-hop descent into builder chain; deny-list short-circuits Math.abs; cycle-termination via visited set; cross-class descent allowed when both in same APK; external callee terminates as before; depth cap respected; `is_stateless` returns False on iput / sput / aput / invoke-virtual-with-stateful-callee / monitor-enter / throw / reflection-flagged-method; `is_stateless` returns True on pure-arithmetic getter / `String.length()`-only chain / primitive-boxing roundtrip; visited-set cycle termination on mutual recursion). New fixture file `tests/fixtures/trace_smali/smali/com/trace/Helpers.smali` (~10 methods exercising every descent + statelessness path). | 11.0 (planning checkpoint); independent of 11.1–11.3 |
| 11.5 | **[x] Tier 2 (d) part 2 — Field-write site walking** *(landed 2026-04-30 — slicer extension + 8 new tests; constructor-priority + strict same-class + receiver-agnostic + lazy-walk per planning checkpoint Q1-Q4)* | Extend `slicing.py` with **`_walk_field_write_sites(field_read_origin, *, class_decl, budget) -> PredicateOrigin`** — when the slicer terminates at a `FieldReadOrigin` AND the field is on the same class (not cross-class — cross-class field-flow stays out of scope per ISSUE-013's recommended-fix note) AND `budget.remaining_depth > 0`, walk the class's `iput-*` / `sput-*` instructions in source order looking for the most recent write to that field, then re-run `slice_predicate_origins` on the written register at the write site (with `budget.remaining_depth -= 1`). The descended `PredicateOrigin` replaces the original `FieldReadOrigin` (same UI density discipline as 11.4's method descent — operator sees the new terminal, depth pill surfaces the "via 1 field write" signal). The shared `_DescentBudget` from 11.4 keeps the closed-economy guarantee — a method that descends 2 hops via callees can't *also* walk a field-write site, and vice versa. Multiple writes pick the most recent (last `iput-*` in source order before the read site); constructor-init writes (in `<init>` / `<clinit>`) are eligible (textbook one-time-init field-cached predicate idiom — exactly the case ISSUE-013 listed as v1's largest false-negative class). Tests (+~8 — single-write field; multiple writes pick most recent; cross-class read terminates as before; constructor-init write site; `<clinit>` write site; budget shared with method descent — 1 hop method + 1 hop field exhausts the v1 default `MAX_SLICE_DEPTH = 2`; budget exhaustion in mid-chain returns the unchanged `FieldReadOrigin`). Adds ~3 methods to the `Helpers.smali` fixture from 11.4. | 11.4 |
| 11.6 | **[x] Bypass-planner re-run + LLM-tier `trace_behavior` budget revisit + cache schema bump + ISSUE-013 close-out** *(landed 2026-04-30 — backend `descent_depth` plumbing + cache schema bump + config knobs + frontend depth-pill UI + ISSUE-013 close-out + 26 new tests across slicer / planner / config; planning-checkpoint Q1 (A) literal-spec descent_depth placement + Q2 (A) depth-only pill + Q3 (A) surgical YAML inserts + Q4 (A) automated v1-vs-v2 measurement test)* | **Bypass planner.** `androscan/analysis/bypass_planner.py` itself shouldn't need changes (it operates on `PredicateOrigin` and the discriminated-union shape didn't change), but verify with new tests in `tests/test_bypass_planner.py` (+~6 — confirm planner correctly emits `force_method_skip` against the deeper terminal method when 11.4's descent succeeds; confirm `force_return_value` against the field-write site's source method when 11.5's walk succeeds; confirm planner output is unchanged when descent is short-circuited by deny-list / depth cap / `is_stateless=False`). **LLM-tier skill `trace_behavior`.** `androscan/skills/trace_behavior.py` may need its prompt size revisited — measure on real fixture how much the prompt grows when the closure's `predicate_origin` chains are 2–3× longer than v1; if the per-anchor token budget is squeezed, tighten the per-decision prose payload (the `_format_decision_for_llm` helper in the skill). **`global_config.yaml` LLM-budget bumps** (per DEC-025): `ollama.num_ctx: 16384` (was unset → defaulted to 8192) AND `ollama.num_predict: 12288` (was 8192). New `Config.ollama_num_ctx: int = 8192` field added to `androscan/config/loader.py` next to the existing `ollama_num_predict`; both wired through `CONFIG_FIELD_MAP` (yaml: `("ollama", "num_ctx", "ANDROSCAN_OLLAMA_NUM_CTX")`) and `LIVE_RELOADABLE_FIELDS` (both True — Ollama re-reads them on each request). `androscan/llm/client.py` `_build_request_body` extended to forward `num_ctx` to Ollama's `options` dict alongside the existing `num_predict` / `temperature`. New `trace.max_slice_depth: int = 2` config knob in the existing `trace:` section (live-reloadable, hard cap 4 in `slicing.py` defensively). **Cache schema bump.** `androscan/internal/trace_cache.py` `SCHEMA_VERSION` bumped from `"1"` to `"2"`. Existing `get_status()` reader already returns `status="failed"` with `error="schema_version mismatch"` on mismatch — all v1 cached anchors silently re-build on first 11.x open via the existing route layer's "missing → build" fallback path. No new migration code, no UI banner needed (the existing `from cache | freshly built` footer on `BehaviorAnchorCard` will read "freshly built" the first time each anchor is re-opened post-deploy). **Frontend pill-only depth indicator.** `androscan/web/frontend/src/components/trace/PredicateOriginView.tsx` gains a small "via N helper method(s)" / "via 1 field write" pill next to the existing origin-kind tag when the slicer descended past at least one terminal. `BehaviorAnchor` / `DecisionPoint` / `BypassPlan` / `PredicateOrigin` discriminated-union shapes themselves are unchanged (per DEC-025 — "richer leaves, no schema bump"); the depth signal lives on a new optional `descent_depth: int = 0` field on the variant subclasses (`MethodCallOrigin` / `FieldReadOrigin` only — `ConstOrigin` / `ParamOrigin` / `CompositeOrigin` are by definition terminal in v1's slicer and stay terminal in v2). Pill links to a tooltip listing the methods walked through (so the operator can audit the path without expanding the card — per DEC-025 open question 4). New CSS for the depth pill under `.trace-predicate-origin-depth-pill`. **Update `TraceIncompleteBanner` copy** in `androscan/web/frontend/src/components/trace/TraceBanners.tsx` to reflect the reduced incidence — banner still surfaces when `predicate_origin: None` slips through (depth cap exhaustion or `is_stateless=False` short-circuit), but the copy acknowledges v2's bounded inter-procedural improvements. **`KNOWN_ISSUES.md` ISSUE-013 status update**: closes to *"Resolved (Phase 11 v2)"* if 11.4 + 11.5 measurably reduce the false-negative rate on the dogfood app during 11.6's verification (criterion: >50% of v1's "decision with `predicate_origin: None`" cases on the dogfood-app's existing cached anchors now resolve to a deeper terminal under v2 slicer); else status remains *"Open (partial v2 progress)"* with a note describing the remaining gap (most likely: a class of multi-step-builder predicates that need depth > 2 to resolve, which would push the depth knob to its hard cap and warrant a v3 conversation about full inter-procedural slicing). **Frontend types** in `api/trace.ts` gain the new optional `descent_depth?: number` field on `MethodCallOrigin` / `FieldReadOrigin` (matches `dataclasses.asdict` output verbatim — the new field defaults to 0 via Python `dataclass` field default so v1-style payloads still parse). | 11.5 |
| 11.7 | **[x] Docs sweep + DEC-025 closing note** *(landed 2026-04-30 — docs-only commit across the six canonical files; closes Phase 11; flips Active Task to Phase 12 v3 planning checkpoint per Q2 (C); LLM-budget measurement honestly deferred per Q1 (A))* | **Planning checkpoint Q1-Q2 answered:** Q1 (A) honest deferral on the LLM-budget measurement outcome — 11.6 shipped the bumped budgets per spec but the actual real-app prompt + output token-usage measurement was deferred (no dogfood-app traces accrued under v2 within the planning window); follow-up tracked separately when telemetry surfaces, with the existing measure-first-tighten-if-needed posture intact. Q2 (C) flip directly to a v3 / Phase 12 planning checkpoint as the new Active Task — DEC-026 to be ratified as 12.0 picking the operator-demand-gated v3 candidates from DEC-025's deferral list. **`docs/STATE.md`:** Phase 11 parent line flipped from "active 2026-04-30 (sub-steps 11.0–11.6 done; 11.7 pending)" → "complete 2026-04-30 (all sub-steps 11.0 → 11.7 done; +26 new tests; ISSUE-013 → Resolved (Phase 11 v2))"; 11.7 sub-bullet added at the top of the indented Phase 11 sub-bullets list; matching 11.7 entry in "Recent completed work" log; "Next expected milestone" section reworked from "Phase 11 active" to "Phase 11 complete + Phase 12 — Behavior Trace v3 / cross-tab follow-ups planning checkpoint pending (DEC-026 to be ratified as 12.0)" with the full DEC-025 deferral list (ISSUE-014, auto-verify, free-form LLM JS, per-app TTL, iOS adapters, Smali patching, LLM-budget real-app measurement follow-up) carried forward as the v3 candidate menu. **This `TASKS.md`:** Active Task block flipped from "Phase 11 — Behavior Trace v2 active" → "Phase 12 — v3 planning checkpoint pending (DEC-026)" as the new primary slot per Q2 (C); "Phase 11 — Behavior Trace v2 complete (2026-04-30)" tombstone added below it summarising +26 tests + ISSUE-013 resolution + the full per-sub-step landing dates; Phase 11 row in § Sub-task status (implementation) table flipped from "Active." → "Done 2026-04-30." with the same +26 tests / ISSUE-013 resolution / LLM-budget deferral summary; Phase 12 row stub added below it as the next planning-checkpoint anchor; this 11.7 row marked `[x]` with this as-built description. **`docs/DESIGN_DOC.md`:** Phase 11 stub status promoted from "active — 11.0 planning checkpoint + DEC-025 done 2026-04-30 (docs only); 11.1 → 11.7 pending..." to "**v2 landed 2026-04-30** — all sub-steps 11.0 → 11.7 complete; ISSUE-013 → Resolved (Phase 11 v2)..." with cross-link to STATE.md sub-bullets + DEC-025's v2 closing note. **`docs/DECISIONS.md`:** DEC-025 `status:` line annotated with "**v2 complete 2026-04-30** — see closing note at end of entry; remains Active because the deferred v3 / Phase 12 scope is still load-bearing for future contributors..." (mirrors DEC-024's same posture verbatim). New "v2 closing note (2026-04-30)" block at the end of DEC-025 with eight bullets covering: sub-steps 11.0 → 11.7 landed; closed-economy budget discipline held; `descent_depth` schema additivity held; cache schema bump 1 → 2 worked as designed; all four planning-checkpoint pre-answers held; ISSUE-013 → Resolved (Phase 11 v2) with the > 50% production-dogfood threshold deferred; honest LLM-budget measurement deferral per Q1 (A); v3 / Phase 12 = next planning slot per Q2 (C). **`docs/KNOWN_ISSUES.md`:** ISSUE-013 status verified — already finalised in 11.6 to "Resolved (Phase 11 v2)" — no edit needed in 11.7. **`docs/SAFETY_AND_SECURITY.md` §12.7:** explicitly **unchanged** per DEC-025 + the 11.7 spec — Phase 11 v2 adds zero new device-touching surface area; a "v2 closing block" §12.8 is **not** added (would be performative; no new safety-relevant surface to document). **Verification:** all 882 non-device pytest tests still pass (no code changes in 11.7); ReadLints clean; six docs files modified; no frontend / backend / config / test code touched; `tsc --noEmit` + `vite build` not re-run (frontend untouched). **Final Phase 11 test count: +26 new tests** end-to-end (`tests/test_bypass_planner.py` +6 + `tests/test_decisions_slicing.py` +35 across 11.4 / 11.5 / 11.6 + `tests/test_config.py` +14 + `tests/test_trace_routes.py` +7) — at the spec target of ~32 (slightly under because 11.5's +8 overlapped 11.4's +27 in the slicer-tests file count; the spec target was "spread weighted toward the static layer", which the +35 in `test_decisions_slicing.py` honors). | 11.6 |
| 12.0 | **Planning checkpoint + DEC-026** *(deferred-after-v2.1; was "pending — next active session" until 2026-05-05 when v2.1 was ratified)* | Brief Ask-mode planning checkpoint to pick the v3 scope from DEC-025's deferral list (ISSUE-014, auto-verify, free-form LLM JS, per-app TTL, iOS / WebView / native-binary adapters, Smali patching, LLM-budget real-app measurement follow-up). Strictly operator-demand-gated: 12.0 will pick the deliverables that show up in real-app telemetry from Phase 11 v2 + v2.1 use, not all of the deferral list at once. DEC-026 ratification mirrors the DEC-024 → DEC-025 transition shape (v1 closing note + v2 follow-up DEC ratified in the same sweep). **Pre-checkpoint inputs to gather:** real-app traces under Phase 11 v2 slicer + operator usage of v2.1's entry-method discoverability + chat-widget pattern to populate the v3 candidate-prioritization conversation (current state: zero dogfood-app traces accrued under v2 within the Phase 11 planning window; v2.1 still in flight as of 2026-05-05 — Phase 12 ratification depends on both). **Sequencing:** v2.1.6's docs sweep flips Active Task back to "Phase 12 — Behavior Trace v3 planning checkpoint pending (DEC-026)" + this row to "Active. Planning checkpoint 12.0 pending (DEC-026)" — see § **Phase 11 v2.1 follow-up release — sub-step backlog** below for the v2.1 sub-step table that gates this row's promotion back to Active. | 11.7 + v2.1.6 |

**Brief Ask-mode planning checkpoint at the top of 11.4** to confirm the `_DescentBudget` shape (single shared budget across method + field descent vs. separate budgets) and the `_STATELESS_LIB_DENYLIST` initial seed (the small hand-curated stdlib classes we can't walk into). Mirrors the 10.6 `BehaviorAnchor` JSON wire-shape checkpoint that locked the data model before the frontend in 10.7 started depending on it.

**Risks (per DEC-025):**
- **Slicer depth blow-up on cyclic call patterns:** mitigated by the visited-set in `_DescentBudget` + hard cap `MAX_SLICE_DEPTH = 4` (config-knobbed default 2). Sanity-checked during 11.4's planning checkpoint with a worst-case fixture (mutual-recursion + hub-method patterns).
- **Per-anchor LLM token budget squeeze from deeper chains:** addressed by `num_ctx: 16384` + `num_predict: 12288` bumps in 11.6; if the qwen3.5:35b runtime can't actually use the larger context (some local-LLM setups silently fall back to the model's smaller training-time context), 11.6 falls back to tightening the per-decision prose payload (the `_format_decision_for_llm` helper in `trace_behavior` skill).
- **Cached-anchor staleness misleads operators on first 11.x deploy:** addressed by the `trace.sqlite` `SCHEMA_VERSION` 1 → 2 bump in 11.6 — all v1 cached anchors silently re-build on first open via the existing reader's "drop-the-cache" path. No operator action required, no UI banner needed.
- **ISSUE-013 only partially closes on real apps:** acceptable outcome per DEC-025 — the issue stays Open with a "partial v2 progress" status note describing the remaining gap (likely: multi-step-builder predicates needing depth > 2). Better than declaring victory before measuring on the dogfood app.
- **Type-driven `is_stateless` analyzer adds recursion + cycle detection** to the slicer. Mitigated by the visited-set keyed on `(class_smali, method_name, descriptor)` and the hard depth cap. Worst-case behaviour on a cyclic call pattern: depth cap fires, `is_stateless` returns False (defensive — assume side effects when we can't prove statelessness), descent terminates with the existing terminal `PredicateOrigin` unchanged.
- **Phase 4 CI deferral risk:** if CI.0 doesn't land before 11.4, the slicer changes ship without the regression safety net. Recommendation: prioritise CI.0 sequencing wherever possible; but Phase 11 sub-steps remain individually shippable into a hand-run pytest gate (the same gate every Phase 10 sub-step shipped into).

---

**§ Phase 11 v2.1 follow-up release — sub-step backlog** *(strictly linear; one sub-step per Agent-mode session; ratified in v2.1.0 2026-05-05 — extends DEC-025's "v2 closing note" with a "v2.1 follow-up release ratified 2026-05-05" block + Q7-Q11 lock-in record; no new DEC per Q10: α — still in v2 family)*

The v2.1 follow-up release addresses the operator-friction signal "the Smali entry-method input in Lab → Trace is unintuitive for non-advanced users." Scope locked at v2.1.0 ratification: three coherent deliverables — (1) **Deterministic UX backbone** (`ClassMethodTree` browser embedded in Trace mode + Smali-field collapse + Hops-inline restructure); (2) **Backend coalescer + spinner** (new `POST /api/trace/{app_id}/normalise-entry` endpoint translating dotted input → Smali class-prefix + validating against the call graph, with frontend debounced 400ms inline spinner + ✓/⚠ pill); (3) **Three-tier LLM integration** (Tier 1 "Find similar classes" on validation failure; Tier 2(a) "Ask AI" button opening chat dock with pre-filled prompt; Tier 3 new `suggest_trace_entry` skill surfaced via chat-widget pattern). **Architectural pattern lock per Q7 (ii):** structured-output extension to skill response schema (optional `widgets[]` block alongside `text`; chat renderer detects and renders specialized React components per `kind`) — first instance of LLM-emitted interactive widgets in chat (DEC-022 cross-link); deliberately extensible so future skills can reuse the same `widgets[]` schema + render seam. **Q8 (a):** auto-fire trace on candidate click in chat-widget (matches Inspect → Trace seed behavior). **Q11 (α):** `androscan/skills/suggest_trace_entry.py` lives next to `trace_behavior.py` (canonical skills directory; no `androscan/llm/skills/` exists in the codebase). **Explicitly NOT in v2.1 (deferred to a future v2.2 / v3 / Phase 12):** native semantic-search textarea inside Trace mode (operator's preferred UX is chat-as-UI per Q3 Tier-3 feedback); LLM-driven Smali-signature autocomplete in the "Advanced" textfield (out of scope — coalescer covers the dotted-input → Smali translation deterministically); class-tree filtering by package-prefix beyond what `ClassMethodTree` already supports; widget kinds beyond `trace_entry_candidate` (ship the pattern with one consumer in v2.1, add more in subsequent releases as demand surfaces).

| #      | Title | Deliverable | Depends on |
|--------|-------|-------------|-----------|
| v2.1.0 | **[x] Planning checkpoint + DEC-025 closing-note v2.1 extension** *(landed 2026-05-05 — docs only)* | This commit. **`docs/STATE.md`:** new "Recent completed work" entry at top for v2.1.0; "Next expected milestone" section reworked to insert the v2.1 follow-up release as the active item ahead of Phase 12 (with Phase 12 listed as deferred-after-v2.1). **`docs/TASKS.md`:** Active Task block flipped from "Phase 12 v3 planning checkpoint pending (DEC-026)" → "Phase 11 v2.1 follow-up release in progress" with Phase 12 listed as deferred-after-v2.1; Phase 11 row in § Sub-task status (implementation) table updated to "Done 2026-04-30; v2.1 follow-up release in progress (ratified 2026-05-05)" with the v2.1 scope summary; Phase 12 row updated to "Deferred-after-v2.1" with the rationale; this Phase 11 v2.1 follow-up release sub-step backlog table added (with v2.1.0 marked `[x]`). **`docs/DECISIONS.md`:** DEC-025 `status:` line annotated with the v2.1 follow-up release ratification (DEC-025 stays Active per Q10: α — still in v2 family); existing "v2 closing note (2026-04-30)" block extended with a new "v2.1 follow-up release ratified 2026-05-05" sub-block containing the Q7-Q11 lock-in record (Q7: ii — structured-output `widgets[]` schema extension; Q8: a — auto-fire on candidate click; Q9: 7 sub-steps; Q10: α — extend DEC-025, no new DEC; Q11: α — `androscan/skills/suggest_trace_entry.py` next to `trace_behavior.py`) + sub-step backlog reference + DEC-022 cross-link for the new chat-interactive-widget pattern. **`docs/KNOWN_ISSUES.md`:** unchanged (no new issues opened in v2.1.0; ISSUE-013 still Resolved). **`docs/SAFETY_AND_SECURITY.md`:** unchanged (v2.1 adds zero new device-touching surface area — Trace stays read-only, the new coalescer endpoint is read-only over the call graph, the new LLM skill is `requires_confirmation=False` like every other tier-3 read-only skill; the chat-widget pattern's auto-fire on click is bounded to the existing `pendingTraceEntry` plumbing which has no device-touching effect on its own — the trace itself is read-only per DEC-024 § "Trace itself is read-only"). **`docs/DESIGN_DOC.md`:** unchanged in v2.1.0 (Phase 11 stub already says "v2 landed 2026-04-30"; v2.1 closing-note will be added in v2.1.6 when the release ships). **No code changes, no test changes, no `global_config.yaml` changes** — `git diff --name-only` shows only `STATE.md` + `TASKS.md` + `DECISIONS.md`. | Phase 11 v2 complete (DEC-025) |
| v2.1.1 | **Deterministic UX backbone — `ClassMethodTree` in Trace mode + Smali-field collapse + Hops-inline** *(pending)* | Frontend-only on `androscan/web/frontend/src/tabs/LabTraceMode.tsx` + `androscan/web/frontend/src/components/ClassMethodTree.tsx` (potentially adds a small `compact` prop) + `androscan/web/frontend/src/util/smaliClassToFile.ts` (adds `looksLikeSmali(s: string): boolean` helper — used to decide whether to show the picker vs. the coalescer-validation pill) + `App.css` (new `.trace-class-browser` + `.trace-advanced-form` namespaces). **Q1 (A1):** collapsible "Browse classes" section above the entry form, default-collapsed (operator opt-in). **Q2 (A):** reuse `ClassMethodTree`'s existing App/Libs default toggles (no new defaults — the existing toggle behavior matches operator expectations from Inspect tab). **Q3 (A):** clicking a method in the tree seeds the entry field with a Smali class-prefix (`Lcom/example/Foo;->onClick(`), activates the existing `MethodPicker` for overload confirmation, then auto-fires the trace on overload selection (re-uses the `looksLikeCompleteSmaliSignature` + auto-fire path already established in 10.8 + 11.2). **Q4 (B):** hide the raw Smali input field behind an "Advanced: type Smali signature directly" toggle (default-collapsed); the `Hops` field stays always visible as a small inline control next to the Browse / Advanced toggles. **No backend changes, no schema changes, no new tests** (frontend-only; the wire-shape contract is locked by 10.6's `tests/test_trace_routes.py`). Frontend verification: `tsc --noEmit` clean; `vite build` clean. | v2.1.0 |
| v2.1.2 | **Backend coalescer + frontend debounce + spinner + validation pill** *(pending)* | **Backend.** New `POST /api/trace/{app_id}/normalise-entry` endpoint in `androscan/web/trace_routes.py` (factory-built via the existing `build_trace_router(...)` plumbing — no new DI seams). Request: `{entry: str}` (the operator's typed input — could be dotted Java form, partial Smali, or full stack-trace line). Response: `{normalised_entry: str | null, class_exists_in_graph: bool, method_count: int, error: str | null}`. Logic: (1) parse the input with a small heuristic dispatcher (dotted form `com.example.Foo.onClick` → Smali prefix `Lcom/example/Foo;->onClick(`; partial Smali stays as-is; stack-trace line `com.example.Foo.onClick(Foo.java:42)` → Smali prefix); (2) validate against the call graph (`class_exists_in_graph` via `call_graph.list_graph(package_prefix=class_smali)`; `method_count` via the existing `_normalise_smali_class` + method-count count from 10.8's MethodPicker plumbing); (3) return the normalised entry + validation flags. 404 on unknown app; 409 when call graph isn't ready; 422 on un-parseable input (with a clear `error` message the frontend renders inline). **Frontend.** `androscan/web/frontend/src/api/trace.ts` gains a `normaliseTraceEntry(appId, entry)` client function + `NormaliseEntryResponse` type. `LabTraceMode.tsx` adds a debounced effect (400ms) on entry-field change that calls `normaliseTraceEntry`, displays an inline spinner during the call, and renders a ✓ / ⚠ pill with the validation result (✓ "valid class with N methods" / ⚠ "class not found in call graph"). The pill is the entry point for v2.1.3's Tier 1 "Find similar classes" button. New CSS in `App.css` under `.trace-entry-spinner` + `.trace-entry-validation-pill`. **Tests.** `tests/test_trace_routes.py` (+~5-7 — happy path with dotted input, happy path with partial Smali input, stack-trace-line normalisation, class-not-found 200 with `class_exists_in_graph: false`, un-parseable input 422, unknown-app 404, call-graph-not-ready 409). Frontend verification: `tsc --noEmit` clean; `vite build` clean. **Q5 (A):** translate class+method only (no descriptor / overload resolution — `MethodPicker` from 10.8 already handles overloads). **Q6 (B):** backend coalescer (not frontend-only) so we get authoritative call-graph validation in the same round-trip — frontend-only translation would lack the validation signal, and validation is what makes the spinner / pill meaningful. | v2.1.1 |
| v2.1.3 | **Tier 1 — "Find similar classes" suggestions on validation failure** *(pending)* | When v2.1.2's validation pill renders ⚠ (class not found in call graph), grow a "Find similar classes" button next to the pill. On click, fires a backend call (extends `normalise-entry` response with an opt-in `suggest_similar: bool` request flag, OR adds a sibling `POST /api/trace/{app_id}/suggest-similar-classes` endpoint — pick during the v2.1.3 implementation based on which is cleaner; the response shape is `{candidates: [{smali_id, rationale, confidence}, ...]}`). Backend logic: (1) fast path — fuzzy-match the input class name against the call graph's class list via `difflib.get_close_matches` (no LLM; sub-100ms; covers typos like `MainActvity` → `MainActivity`); (2) if fuzzy match yields no results AND the input has at least 3 word-segments, fall back to the new `suggest_trace_entry` skill from v2.1.5 (LLM-backed semantic search; ~500ms-2s). Frontend: candidates render as clickable suggestion pills below the validation pill; clicking a pill seeds the entry field with the candidate's Smali class-prefix and fires the v2.1.2 coalescer again to re-validate (no auto-fire on Tier 1 — operator picks one of N suggestions, then explicitly Traces). **Tests.** `tests/test_trace_routes.py` (+~3-4 — fuzzy-match happy path, no-match returns empty candidates, malformed input 422). | v2.1.2 + (v2.1.5's `suggest_trace_entry` skill if v2.1.3 lands after v2.1.5; otherwise v2.1.3 ships fuzzy-only and v2.1.5 wires in the LLM fallback as part of its own scope) |
| v2.1.4 | **Tier 2(a) — "Ask AI" button + `pendingChatPrefill` plumbing** *(pending)* | Frontend-only. New "Ask AI" button next to the entry field in `LabTraceMode.tsx` (small icon button — opens chat dock with a pre-filled prompt template). On click, writes `pendingChatPrefill = {tab: "lab", message: "I want to trace <operator-typed entry>. What entry methods should I consider?"}` to `WorkbenchContext` and ensures the chat dock is expanded. New `pendingChatPrefill: PendingChatPrefill | null` + `setPendingChatPrefill(p: PendingChatPrefillInput | null)` plumbing in `androscan/web/frontend/src/context/WorkbenchContext.tsx` — mirrors the existing `pendingTraceEntry` / `pendingHookPrefill` / `pendingCodeNav` patterns exactly (timestamp-stamped, re-fire semantics same as `pendingTraceEntry`, consumed via `useEffect` + `consumedTsRef` in `ChatDock.tsx`). The pre-filled prompt leverages the existing tier-3 skills (`search_decompiled_sources` for RAG over decompiled sources + `query_call_graph` for call-graph context) — no new skill needed in v2.1.4 (the new `suggest_trace_entry` skill from v2.1.5 will be picked up automatically by the chat agentic loop once it ships). **No backend changes, no schema changes, no new tests** (FE-only; the chat-prefill plumbing pattern is already test-covered via the existing `pendingTraceEntry` / `pendingHookPrefill` patterns). Frontend verification: `tsc --noEmit` clean; `vite build` clean. | v2.1.1 (the entry field needs to exist before "Ask AI" can pre-fill its value) |
| v2.1.5 | **Tier 3 — `suggest_trace_entry` skill + skill-response `widgets[]` schema extension + `<TraceEntryCandidateWidget>` chat renderer** *(pending — biggest sub-step)* | **Backend skill.** New `androscan/skills/suggest_trace_entry.py` registered in `androscan/skills/__init__._SKILL_MODULES` (Q11: α — lives next to `trace_behavior.py` in the canonical skills directory; no `androscan/llm/skills/` subdirectory exists in the codebase). Skill is `requires_confirmation=False` (read-only — no device-touching surface). Inputs: `description: str` (operator's natural-language description of what they want to trace) + `app_id: str` (with the same `run_folder.parent` fallback ladder every other tier-3 skill uses). Logic: (1) RAG over decompiled sources via the existing `androscan/rag/` package — top-k decompiled-source chunks matching the description; (2) call-graph context via `query_call_graph`'s `overview` mode for the matched classes' packages; (3) LLM ranking pass — combines RAG hits + call-graph context + a small prompt template asking the LLM to rank top-3 candidate trace entries by relevance + emit a one-line rationale per candidate. Output: `SkillResult(success=True, text="<natural-language summary>", widgets=[{kind: "trace_entry_candidate", smali_id, rationale, confidence: float} x3])`. Fail-open on every unavailability mode (missing app context, RAG not built, call graph not built — returns `success=True` with empty widgets + a clear `text`; mirrors `query_call_graph` / `search_decompiled_sources` failure semantics). **Skill-response schema extension** (Q7: ii — first chat-interactive-widget pattern; cross-link DEC-022). `androscan/skills/base.py` `SkillResult` extended with optional `widgets: tuple[SkillWidget, ...] = ()` field — additive default keeps every existing read-only skill unchanged; `SkillWidget` is a typed union (initial member `TraceEntryCandidateWidget(kind="trace_entry_candidate", smali_id: str, rationale: str, confidence: float)`; future widget kinds add as new union members without breaking the schema). `androscan/web/chat.py` agentic-loop SSE event vocabulary extended to forward widgets through to the chat client — new `widget` SSE event kind alongside the existing `skill_request` / `skill_result` event kinds (per DEC-022 § "Extend the SSE vocabulary"). **Frontend chat renderer.** New `<TraceEntryCandidateWidget>` React component in `androscan/web/frontend/src/components/chat/widgets/TraceEntryCandidateWidget.tsx` — renders a compact card with the candidate's Smali sig + rationale + confidence + a "Trace this" button. On click, writes `pendingTraceEntry = {appId, entryPrefix: candidate.smali_id, sourceLabel: "Chat → suggest_trace_entry"}` (re-uses the 10.8 / 11.2 plumbing) + flips `labMode` to `"trace"` + `setTab("lab")` — auto-fire is honoured via the existing `looksLikeCompleteSmaliSignature` + auto-fire path (Q8: a — same pattern as Inspect → Trace seed). New `<ChatWidgetRenderer>` dispatcher component in `androscan/web/frontend/src/components/chat/widgets/index.tsx` — typed switch on `widget.kind` that returns the matching widget component (currently just `TraceEntryCandidateWidget`; future widgets register here). `ChatDock.tsx` consumes `widget` SSE events and passes them through to `<ChatWidgetRenderer>`. New CSS in `App.css` under `.chat-widget-trace-entry-candidate` namespace (compact card with the existing `.trace-*` colour palette to visually cluster with Trace-mode UI). **Tests.** `tests/test_skills.py` (+~3 — `suggest_trace_entry` registered with `requires_confirmation=False`; `widgets` field defaults to `()` on every existing skill); `tests/test_suggest_trace_entry_skill.py` (new file, +~5-7 — happy path with mocked LLM + mocked RAG + mocked call graph; fail-open on missing RAG; fail-open on missing call graph; widgets payload shape; LLM-budget within DEC-022's per-skill envelope; rationale string non-empty + truncated to a sane size); `tests/test_chat_stream.py` (+~2 — `widget` SSE event forwarded through the agentic loop; widgets array on `SkillResult` round-trips through the JSON wire format). Frontend verification: `tsc --noEmit` clean; `vite build` clean. | v2.1.1 (chat dock needs to be available — it already is, but the Tier 2(a) "Ask AI" button is the obvious entry point); v2.1.4 ("Ask AI" button surfaces this skill via chat) |
| v2.1.6 | **Docs sweep + DEC-025 closing-note v2.1 extension** *(pending — wrap-up; docs-only)* | Wrap-up sub-step that closes the v2.1 follow-up release. **`docs/STATE.md`:** add per-sub-step bullets for v2.1.1 → v2.1.5 to the "Recent completed work" log; flip the "Next expected milestone" section back to "Phase 12 — Behavior Trace v3 planning checkpoint (DEC-026)" as the next active item (mirrors the 11.7 → Phase 12 transition). **`docs/TASKS.md`:** Active Task block flipped from "Phase 11 v2.1 follow-up release in progress" → "Phase 12 — Behavior Trace v3 planning checkpoint pending (DEC-026)" with the v2.1 follow-up release tombstone added below it summarising what shipped + the +N-test delta + the chat-widget pattern as a load-bearing architectural addition; Phase 11 row in § Sub-task status (implementation) table flipped from "Done 2026-04-30; v2.1 follow-up release in progress" → "Done 2026-04-30 (v2) + v2.1 follow-up release done YYYY-MM-DD"; Phase 12 row flipped back to "Active. Planning checkpoint 12.0 pending (DEC-026)"; this v2.1.6 row marked `[x]`. **`docs/DESIGN_DOC.md`:** Phase 11 stub status promoted from "v2 landed 2026-04-30" → "v2 landed 2026-04-30 + v2.1 follow-up release landed YYYY-MM-DD" with a cross-link to STATE.md sub-bullets + DEC-025's v2.1 closing-note extension. **`docs/DECISIONS.md`:** DEC-025 `status:` line annotated with "v2.1 follow-up release complete YYYY-MM-DD"; v2.1 closing-note extension at the end of DEC-025 grown with the v2.1.1 → v2.1.5 implementation outcomes (architectural deviations from the v2.1.0 spec, test counts, frontend bundle deltas, the chat-widget pattern's empirical behaviour in operator hands); cross-link DEC-022 for the chat-interactive-widget pattern (extends DEC-022's chat agentic loop with the new `widgets[]` outbound channel). **`docs/KNOWN_ISSUES.md`:** add any v2.1-discovered issues to the issue list with appropriate status; v2.1 doesn't open ISSUE-013 / ISSUE-014 — those stay in their existing status. **`docs/SAFETY_AND_SECURITY.md`:** explicitly **unchanged** per the v2.1.0 ratification (v2.1 adds zero new device-touching surface area — Trace stays read-only, the new coalescer endpoint is read-only over the call graph, the new LLM skill is `requires_confirmation=False`, the chat-widget pattern's auto-fire on click is bounded to the existing `pendingTraceEntry` plumbing which has no device-touching effect on its own); no §12.7 update needed. **Verification:** all non-device pytest tests still pass (no code changes in v2.1.6); ReadLints clean; six docs files modified; no frontend / backend / config / test code touched in v2.1.6. **Operator impact:** the doc tree now correctly reflects "Phase 11 v2 + v2.1 follow-up release done; Phase 12 v3 planning checkpoint is the next active item." | v2.1.5 |

**Risks (per v2.1.0 ratification):**
- **Coalescer false-negatives misleading operators on legitimate-but-not-yet-built classes:** the call-graph-validation step in v2.1.2 returns `class_exists_in_graph: false` for any class not in the call graph SQLite — including classes that exist in the decompiled source but haven't been picked up by the call-graph builder (extremely rare, but possible if the call-graph build had errors that silently dropped the class). Mitigation: the validation pill copy is "class not found in call graph" rather than "class doesn't exist" so operators understand the scope of the check; v2.1.3's "Find similar classes" button also covers this case (the LLM fallback will surface the legitimate class as a candidate even if the fuzzy-match misses it).
- **LLM budget squeeze on v2.1.5's `suggest_trace_entry` skill:** the skill combines RAG hits + call-graph context + an LLM ranking pass — the per-skill output budget of ~6 KB (DEC-022) might get tight on apps with very long class names + verbose rationale. Mitigation: the skill caps each rationale at 200 chars + the candidates list at 3; if the budget squeeze surfaces in operator use, tighten the per-decision prose payload (same playbook DEC-025 used for the v2 LLM-budget bumps).
- **Chat-widget pattern adoption beyond v2.1.5:** Q7 (ii) is deliberately scoped to "ship the pattern with one consumer in v2.1, add more in subsequent releases as demand surfaces." The risk is the pattern becomes load-bearing across multiple skills and the schema needs a breaking change before v2.2 lands. Mitigation: the `SkillWidget` typed union is additive-by-design (new widget kinds add as new union members without breaking the schema); the chat renderer's `<ChatWidgetRenderer>` dispatcher gracefully handles unknown widget kinds (renders the skill's `text` field only, ignores the unknown widget). DEC-022 cross-link in DEC-025's v2.1 closing-note documents this discipline so future contributors don't trip over the schema-additivity rule.
- **`pendingChatPrefill` plumbing collision with future cross-tab handoff patterns:** v2.1.4 introduces a fourth `pending*Prefill` plumbing seam in `WorkbenchContext` (alongside `pendingTraceEntry` / `pendingHookPrefill` / `pendingCodeNav`). If the count grows beyond 5-6, refactor into a single `pendingHandoff: {kind, payload}` discriminated union — but that's a v3 concern, not v2.1's. Mitigation: keep the 4-seam pattern for v2.1; revisit if v3 / Phase 12 scope adds a fifth seam.
- **Sub-step ordering risk:** v2.1.3 (Tier 1) depends on v2.1.5's `suggest_trace_entry` skill if it wants the LLM-backed fallback; if the spec says v2.1.3 ships before v2.1.5, the Tier 1 sub-step will ship with fuzzy-match-only and v2.1.5 will wire in the LLM fallback as part of its own scope. Mitigation: the sub-step backlog table's "Depends on" column flags this explicitly so the implementing agent knows which order to ship.

---

**§ Phase 4 CI — sub-step backlog** *(parallel non-blocking thread per DEC-025; lands before Phase 11 11.4 if scheduling permits, but unblocked otherwise)*

Phase 4 CI was originally parked from the 2026-03 Phase 4 partial scope (error handling + docs cleanup landed; CI deferred). DEC-025 promotes it to an Active Task line because the Phase 11 slicer changes are the largest behavioural change of v2 and warrant a regression safety net before shipping. The work is small, standalone, and independent of Phase 11 — it can land any time during 11.x, but sequencing it before 11.4 is the better calendar.

| #     | Title | Deliverable |
|-------|-------|-------------|
| CI.0  | **Phase 4 CI — pytest on push/PR** *(non-blocking, can land any time during 11.x; sequence before 11.4 if possible)* | New `.github/workflows/test.yml` running the full pytest suite on `push` to `main` + `pull_request` against `main`. Python 3.11+ matrix (matches local dev — qwen3.5:35b operator setup runs against Python 3.11 per `pyproject.toml`'s `requires-python`). Caches pip + the apktool / jadx tarballs the slow tests use (per the `tests/test_first_run_setup.py` install-once pattern). Skips `device`-marked tests by default (`pytest -m "not device"` matches the local CI default; opt-in marker registered in DEC-023 sub-step 4.3). No frontend tests yet (no FE test runner per project convention; `tsc --noEmit` + `vite build` are still hand-run gates — adding them to CI is a separate v3 follow-up if FE-test demand surfaces). Locks in the **~750-test floor as a regression safety net** before Phase 11's slicer changes ship in 11.4. |

**Risks:**
- GitHub Actions minutes cost: cached pip + apktool/jadx tarballs keep cost bounded; the suite runs in ~3–5 min on a `ubuntu-latest` runner per local profiling. Not a meaningful ops cost.
- Default-suite skips `device`-marked tests, so a Frida-touching regression won't surface in CI — same posture as the local default (`pytest -m "not device"`); the operator gate for device runs is unchanged.

---

### Phase 6 — Web UI shell with emulator mirror

**Goal:** Serve a React frontend from a FastAPI backend with live emulator screen mirroring and basic project browsing.

1. **Backend skeleton** (`androscan/web/`): FastAPI app with CORS, static file serving, WebSocket endpoints; `GET /api/projects` (list `apps/` folders), `GET /api/projects/{app_id}/runs`, `GET /api/dossier/{app_id}/{run_ts}`, `GET /api/findings/{app_id}/{run_ts}`; config keys `web_host`, `web_port` in `global_config.yaml`.
2. **React frontend scaffold** (`androscan/web/frontend/`): Vite + React + TypeScript; layout — sidebar (projects, dossier components, findings), main area (mirror + detail panels); dark theme, monospace code areas.
3. **Emulator screen mirror:** prefer `scrcpy` piped to WebSocket (MJPEG/h264) or `adb exec-out screencap` polling fallback; `<canvas>` or `<video>` in React; touch forwarding via `adb shell input tap x y`.
4. **Live logcat streaming:** backend `adb logcat` subprocess → WebSocket; frontend scrollable panel with level coloring; filter by tag/level/keyword.
5. **CLI integration:** e.g. `python androscan.py --serve [--port …]` starts server; optional `python androscan.py --apk … --serve` runs analysis then opens UI.
6. **Tests:** unit tests for REST responses (mock data); WebSocket connect/disconnect lifecycle.
7. **Dependencies (target):** Python `fastapi`, `uvicorn[standard]`, `websockets`; frontend `react`, `react-dom`, `vite`, `typescript`.

---

### Phase 7 — Click-to-code mapping

**Goal:** Tap on mirrored screen → identify UI element → trace to Activity/class/method → show decompiled source in Monaco.

1. **UI element identification:** on tap, `uiautomator dump` (or equivalent); parse XML; match coordinates to bounds; extract `resource-id`, `class`, `content-desc`, `text`, `package`, `bounds`.
2. **Activity resolution:** `dumpsys activity top` (or equivalent) for foreground Activity.
3. **Resource ID → source:** parse `R.java` / `R$id.java` from jadx output; search for `findViewById(R.id.…)` / View Binding; resolve class + method.
4. **Monaco Editor:** `@monaco-editor/react` in detail panel; Java/Kotlin highlighting; read-only default; jump to line for resource reference.
5. **API:** `POST /api/tap` (x, y) → element + source mapping; `GET /api/source/{class_name}` (delegate to existing jadx-backed decompilation path).
6. **LLM-assisted mapping (stretch):** when deterministic trace fails, optional LLM hint with reasoning shown alongside deterministic result.
7. **Tests:** unit — XML parsing, coordinate matching, R.java parsing; integration — tap → element → source with fixtures. **[done]** `tests/test_inspect_map.py` covers element pick + handler grep + adb glue; `tests/test_web.py::test_api_inspect_map_returns_element_and_candidates` covers the end-to-end `/api/inspect/map` response (including the new `resolution` block); `tests/test_resolve_ui_element.py` covers the fuser scorer + RAG enrichment + skill registry path.
8. **[done]** New skill `resolve_ui_element` (**llm** tier): takes element + foreground activity + raw handler candidates; scores them deterministically (kind base + foreground-activity bonus + activity-named-file bonus + early-line decay) and optionally enriches with Lane-1 RAG hits; returns `{best, alternatives, rag_hits, reasoning}`. Pure-function `resolve()` is also called inline by `/api/inspect/map`.

---

### Phase 8 — Static call graph from Smali

**Goal:** Build navigable call graph from decoded Smali; visualize with Cytoscape.js; support LLM-assisted path questions.

1. **Smali analysis** (`androscan/analysis/call_graph.py` or equivalent): walk apktool output; extract classes, methods, `invoke-*` targets; adjacency caller.method → callees; `extends` / `implements`.
2. **Graph storage:** `apps/<app_id>/call_graph.json` (nodes + edges); rebuild when APK hash changes; node metadata (exported flag from dossier where applicable).
3. **Graph query API:** `GET /api/graph/{app_id}` (paginated/filtered); `GET /api/graph/{app_id}/neighbors/{class.method}`; `GET /api/graph/{app_id}/paths?from=…&to=…`; `POST /api/graph/{app_id}/query` (LLM-assisted natural-language path questions).
4. **Cytoscape.js:** zoom/pan/search/filter; click node → Monaco source; highlight paths; layouts (e.g. dagre, cose-bilkent).
5. **New skill (target):** `query_call_graph` (**llm** tier): question + graph subset → relevant paths; usable from analysis workflow.
6. **Tests:** unit — fixture Smali, graph build, path algorithms; integration — APK → graph → query (where feasible without device).

---

### Phase 9 — Frida integration (live tracing & hooking)

**Goal:** Dynamic instrumentation from the UI — hook methods, trace calls, optional return tampering; LLM-assisted hook generation.

1. **Frida adapter** (`androscan/adapters/frida_client.py` or equivalent): detect/push frida-server (configurable); Python `frida` attach/load/detach; availability checks like other external tools.
2. **Hook templates** (`androscan/adapters/frida_hooks/`): parameterized JS — method entry/exit log, SSL pinning bypass patterns, crypto/SharedPreferences/Intent hooks; render from class/method/signature from graph or dossier.
3. **Live trace streaming:** Frida `message` → WebSocket → UI trace panel (pause/resume, search, export).
4. **Interactive hook builder:** pick method from graph or editor → hook type (log args, log return, modify return, custom JS); optional LLM “generate hook for objective”; **user confirm before deploy**.
5. **New skill (target):** `generate_frida_hook` (**llm** tier): class, method, objective → Frida JS; basic syntax validation before deploy.
6. **Exploit verification integration (optional):** extend signal matrix (`vuln_module_skills_signals.json`) with e.g. `frida_trace` where product requires deeper runtime evidence.
7. **Tests:** unit — template rendering, JS generation, adapter with mocks; device integration opt-in only.
8. **Dependencies (target):** `frida`, `frida-tools`; frida-server on device (operator-managed).

**Cross-cutting:** artifacts under `apps/<app_id>/` (graphs, traces, hooks); new skills follow three-tier model; web server **bind 127.0.0.1** by default (local single-user); `global_config.yaml` sections `web:` and `frida:` (when implemented).

---

### Phase 10 — Behavior Trace (Lab tab gate-identification mode)

**Goal:** From a UI element or method anchor, enumerate the closure of conditional gates that govern its behaviour, classify each gate's outcome, and propose template-bound bypass plans the operator can stage → inject via the existing Hook Lab v1 Stage→Inject path. Trace becomes the headline mode of the **Lab** tab (renamed from Hook Lab per DEC-024); the existing Cytoscape pane is demoted to "Graph mode" alongside `Manual Hooks` and `Trace`.

**Workflow:** *Anchor → Locate → Trace → Classify → Manipulate → Verify*. Operator picks an anchor (Mirror tap → click-to-code resolution from Phase 7 / DEC-019, or a method node from the Lab Graph mode); the static layer enumerates branches in the forward closure (≤ 3 hops by default, hard-capped at 6, methods-cap at 30); the LLM is invoked once per anchor to interpret and propose plans; the operator stages a plan via the existing `HookBuilder.tsx` flow (which is now Lab's `Manual Hooks` mode) and injects via the existing `POST /api/frida/sessions` allowlist-gated path; verification is operator-driven (re-tap the anchor, watch the Frida overlay light up the same gate methods in cyan).

**Deliverables (target):**

- New module `androscan/analysis/decisions.py` (decision-point extraction over the Smali instruction stream); new `androscan/analysis/slicing.py` (intra-procedural backward slicing for predicate origin); new `androscan/analysis/branch_classifier.py` (deterministic deny/allow/neutral classifier with a confidence float); new `androscan/analysis/bypass_planner.py` (template-bound plan synthesis with `low/medium/high` risk taxonomy); new `androscan/analysis/trace_types.py` (platform-neutral data model — `BehaviorAnchor`, `DecisionPoint`, `BypassPlan`, `MethodRef`, `FieldRef`).
- New LLM-tier skill `trace_behavior` (`requires_confirmation=False`; per-anchor LLM call only — per-decision rejected per DEC-024); per-app SQLite cache at `apps/<app_id>/.decompiled/<sha>/trace.sqlite` (schema_version 1, mirrors call-graph / RAG storage layout).
- New REST routes `GET / POST / DELETE /api/trace/{app_id}/anchor`; new `androscan/web/trace_routes.py` factory-built and wired into `androscan/web/app.py`.
- New frontend Trace mode under `androscan/web/frontend/src/components/trace/` (anchor card, decision timeline with verdict badges, bypass plan cards with "Stage in Manual Hooks" buttons, truncated / incomplete banners); 3-mode left-rail switcher in Lab (`Trace | Manual Hooks | Graph`) with Trace as the default; Inspect → Trace cross-tab plumbing via `pendingTraceEntry`; new `trace` chat-attachment kind; back-compat `#/hook` → `#/lab` URL hash redirect.
- Hook Lab → Lab code rename (`HookLabTab.tsx` → `LabTab.tsx`, `HookLabCodeView` → `LabCodeView`, per-tab chat-prompt key in `androscan/web/chat.py`, `frontend/README.md` reference) — all in sub-step 10.6 alongside the tab routing changes.

**Tests:** ~80 new tests across Phase 10 (mostly `tests/test_decisions_*.py`, `tests/test_branch_classifier.py`, `tests/test_bypass_planner.py`, `tests/test_trace_behavior_skill.py`, `tests/test_trace_routes.py`); deterministic fixture-driven static analysis; LLM mocked at the test boundary; no device touching in default suite (the `device` pytest marker from DEC-023 sub-step 4.3 covers any opt-in cases).

**Full sub-task checklist:** § Phase 10 — Behavior Trace v1 — sub-step backlog above (10.0 → 10.8). Locked in **DEC-024**.

---

## Priority Queue

### P1
- Phase 4 CI: pytest on every push/PR (remains parked).
- Implement `content_provider_query` and `app_data_snapshot` signal captures for `exported_provider` profile (currently stub).
- Add second vulnerability module.

### P2
- Integration test with fixture APK.
- JSON output renderer.
- Richer evidence provenance tracking.
- **Interactive RE Workbench — Phase 7+** (Phase 6 shell landed; continue with click-to-code, graph, Frida per § below).

---

## Blocked Tasks

### Phase 4: CI setup
- blocked by: prioritization decision.
- why blocked: Error handling and docs cleanup done; CI (pytest on push/PR) remains parked.
- unblock condition: Decision to prioritize CI setup.

Format:

### [Task title]
- blocked by:
- why blocked:
- unblock condition:

Example:

### Add PDF report renderer
- blocked by: normalized report output contract
- why blocked: report model not stable enough
- unblock condition: shared rendering schema finalized

---

## Backlog / Future Work

Use for real future work, not vague ideas.

- **Integration test with fixture APK** (extraction + dossier shape) — parked; optional for later.
- add second vulnerability module
- add JSON output renderer
- add richer evidence provenance tracking
- add adapter for additional mobile tooling
- **Interactive RE Workbench (Phases 6–9)** — see § Interactive RE Workbench in this file (replaces vague “web UI shell” backlog item)
- **Standalone "search code" UI for Lane-1 RAG** — backend (`POST /api/rag/{app_id}/query`) and the `SearchHit` shape are already shipped; today RAG is only consumed implicitly via Inspect-tab chat enrichment, the `/api/inspect/map` fuser, and the `search_decompiled_sources` LLM skill. A small operator-facing search affordance (e.g. a sub-tab inside Inspect, or a "Code search" panel inside the Code Browser) with click-to-jump into `CodeView` would make the index directly useful during manual RE. Pull this forward only if operators ask for it; nothing else depends on it.
- add queue-backed job execution
- add configurable policy layer

---

## Completed Tasks

### 2026-04-29 Phase 10 (sub-step 10.8): Mirror → Trace integration + chat hook + docs sweep — Phase 10 v1 complete
- outcome: The wrap-up that closes the Phase 10 loop on the operator surface — completes the Inspect → Trace cross-tab handoff, surfaces the active `BehaviorAnchor` to the Lab chat dock as a new attachment kind, and lands the Phase 10 docs sweep across the six canonical files. **Mirror → Trace cross-tab handoff.** New "trace ↗" button on the Inspect tab's `BestBanner` (rendered only when the new `onTraceBehaviour` prop is supplied — `BestBanner` stays a pure presentational component). The button hands the fuser's `ResolutionCandidate` back to `InspectTab.handleTraceBehaviour`, which converts the picked file path into a Smali class descriptor via the new `javaRelPathToSmaliClass(relPath)` utility (`com/example/Foo.java` → `Lcom/example/Foo;`) and combines it with `best.method_name` (when present — only RAG candidates carry it) via `javaRelPathToSmaliMethodPrefix(relPath, methodName)` to produce a *prefix* (`Lcom/example/Foo;->onClick(` when method known, else `Lcom/example/Foo;->`); the descriptor list is intentionally left for the operator to fill in since the deterministic resolver doesn't carry per-overload parameter descriptors. Handler then writes `setPendingTraceEntry({appId, entryPrefix, sourceLabel: "Inspect → <file>#<method>" | "Inspect → <file>:<line>"})`, calls `setLabMode("trace")`, and `setTab("lab")`. **`pendingTraceEntry` plumbing** mirrors `pendingCodeNav` / `pendingHookPrefill` exactly: new `PendingTraceEntryInput` / ts-bumped `PendingTraceEntry` types + `setPendingTraceEntry` callback on `WorkbenchContext`. `LabTraceMode.tsx` reads it on mount + on `(pendingTraceEntry?.ts, appId)` change: prefills draft + hops, sets `seedLabel`, **auto-fires the trace** only when the seeded value is a complete signature (heuristic via `looksLikeCompleteSmaliSignature(s)` — closing paren + recognised return descriptor `^\[*([VZBSCIJFD]|L[\w/$]+;)$`); otherwise just prefills so the operator can finish the descriptor list. The seed label drops on operator edit / Trace fire; `×` button on the pill clears both label and draft. **Lab chat — `trace` `ChatAttachment`.** Cross-mode plumbing: `LabTab.tsx` lifts `activeAnchor: BehaviorAnchor | null`; `LabTraceMode` republishes via a new `onActiveAnchorChange?(anchor | null)` callback; `ManualHooksMode` accepts `activeAnchor` and threads it into both `buildHookChatAttachments` and `buildHookChatContextSummary`. `_renderTraceAttachment(anchor)` emits human-readable plain text — entry-method header, summary line (`hops=N · decisions=M · plans=K (+J advanced) · TRUNCATED · INCOMPLETE`), optional rationale, numbered decision timeline (one line per gate: `class.method @N [kind] branch=verdict(score), … ← <origin> [LOW-CONF]`), and the **top-3 default-tier bypass plans**. Three soft caps: `CHAT_TRACE_BUDGET = 6_000` chars, `CHAT_TRACE_TOP_PLANS = 3`, `CHAT_TRACE_MAX_DECISIONS = 40` (with `+ N more decision(s)` trailer). `buildHookChatContextSummary` extended with a 2-line block (`Active behaviour trace: <class.method> · hops=N · M decision(s) · K plan(s) (+J advanced)` + budget caption). Active anchor cleared via `useEffect([appId])` so a second project never inherits the first's trace context. **Backend.** `androscan/web/chat.py` `ATTACHMENT_BUDGETS["trace"] = 6_000` (matches `code` — same shape, same ceiling). Lab system prompt extended with a sentence telling the model to treat the trace attachment's decision-timeline list as authoritative ground truth and prefer recommending bypass plans rooted in those gates over speculation from the decompiled code alone (alias-aware via `_normalise_tab` so legacy `tab="hook"` requests get the new prompt too). `ChatDock.tsx` `KIND_LABELS["trace"] = "Behavior trace"`. **Tests.** **+4 backend** in `tests/test_chat_guardrails.py` (the `trace` budget key is set + matches `code`; oversized trace truncates at the `trace` budget, not the default 2,000-char cap; under-budget trace passes through verbatim wrapped in `<context kind="trace">`; lab system prompt mentions `trace` and the alias prompt is identical). No new FE tests per project convention. Full suite: **749 / 749 green** (was 745; +4). `tsc --noEmit` clean; `vite build` clean — 709 modules, 1.20 MB JS / 75 KB CSS / 373 KB gzipped (modest growth from 10.7's 1.19 / 74 / 372 attributable to the trace-attachment renderer + the seed-pill CSS + the cross-tab handler). **Docs sweep landed in this commit:** `docs/STATE.md` (10.8 sub-bullet under the Phase 10 entry + Recent completed work entry; Phase 10 header gets a "v1 complete 2026-04-29" stamp); this `docs/TASKS.md` (Active Task → "Phase 10 — Behavior Trace v1 complete (2026-04-29)" + 10.8 row marked `[x]` + this Completed Tasks entry); `docs/DESIGN_DOC.md` (Phase 10 stub status promoted to "**v1 landed 2026-04-29**" with cross-link to STATE.md); `docs/DECISIONS.md` DEC-024 (Status line gets a `**v1 complete 2026-04-29**` closing note while staying `Active` for the cross-platform follow-ups DEC-024 explicitly defers to v2); `docs/KNOWN_ISSUES.md` (new ISSUE-013 captures the v1 intra-procedural-slicing false-negative posture; new ISSUE-014 captures the per-overload precision gap on `predicate_origin` two-register tie-breaks); `docs/SAFETY_AND_SECURITY.md` §12.7 (new "Trace mode (Phase 10)" subsection — read-only static layer, single per-anchor LLM call, template-bound bypass plans, dynamic effects flow exclusively through the existing Hook Lab v1 Stage→Inject controls + the per-app `hook_target_package_prefix` allowlist). **Final v1 test count for Phase 10:** ~104 new tests across 10.1 → 10.8 (over the 80-test target from DEC-024), spread weighted toward the static layer (10.1 → 10.4 = +77 tests) where determinism makes regression coverage cheap; 10.5 = +22 (LLM mocked at the test boundary), 10.6 = +18 (REST + alias), 10.7 = +0 (no FE test runner per convention), 10.8 = +4 (chat attachment).
- notes: The choice to leave the Smali signature *prefix* incomplete on cross-tab seed (rather than have the backend resolve `(class_name, method_name)` to a list of overload signatures via the call-graph store) preserves the "v1 honest about its limits" posture from DEC-024 — the operator sees exactly what the resolver produced, and a backend overload-resolver would either pick the wrong overload silently (dangerous) or surface a picker (scope creep for 10.8). The descriptor-completion path in v2 should land as an autocomplete affordance in the Trace mode form input itself (typing `Lcom/example/Foo;->onClick(` triggers a backend lookup against the call graph for matching overloads), not as a backend resolver in the cross-tab handoff. The choice to auto-fire only on a complete signature (vs. always firing) preserves operator control — false-positive auto-fires would burn an LLM call per click on partial seeds, defeating the purpose of the "Trace this behaviour" button. The conservative `looksLikeCompleteSmaliSignature` heuristic (closing paren + return descriptor regex match) is intentionally narrow — it's a "yes I'm confident" signal, not a "good enough" check. The decision to lift `activeAnchor` into `LabTab` (rather than into `WorkbenchContext`) keeps the cross-mode coupling local — the chat dock and the trace mode are siblings in the same tab, and `LabTab` already owns the mode-switcher state; bumping it to context would invite the Inspect tab to read it too, which has no use for it. The 6,000-char `trace` budget is precedented by the `code` budget — both attachments are linear human-readable text where 6 KB carries enough signal for the model to reason about a non-trivial method or anchor. The decision to render the trace attachment as plain text (rather than JSON) follows the same precedent as `frida_summary` (also JSON-rendered into the model context) and keeps the model's parsing cheap — JSON-shaped attachments incur tokenisation overhead from the structural punctuation + force the model to track nesting, which doesn't help when the consumer is a chat assistant. Top-3 plans is the right ceiling for chat — operators who want the full ranked list read the Trace UI directly, where rendering is rich + interactive; the chat is for "explain the gates and tell me which plan to try first". The 40-decision per-anchor cap is generous enough for realistic apps (typical anchor closure surfaces 5–20 decisions) while preventing a pathological 200-decision closure from monopolising the budget. The decision to NOT add a cross-tab "Trace this behaviour" button on the Manual Hooks mode's CallGraphView (despite the obvious symmetry with the BestBanner button) is deliberate v2 scope — the call-graph node click already prefills HookBuilder, which is the operator's primary action there; adding a "Trace from here" affordance would dilute that flow and is better landed alongside an in-graph "behavioral marker" overlay (call-graph nodes that have a cached `BehaviorAnchor` get a different render).
- follow-up: **Phase 10 v1 is complete** — the Behavior Trace mode is fully operational across the static layer (10.1–10.4), the LLM-tier skill + per-app cache (10.5), the REST surface + tab shell (10.6), the headline UI (10.7), and the Inspect → Trace cross-tab handoff + chat integration + docs sweep (10.8). DEC-024 stays `Active` for the cross-platform follow-ups it explicitly defers to v2 (iOS / WebView / native binary mirroring, inter-procedural slicing, switch-case bypass plans, per-decision LLM calls, free-form LLM JS via the `generate_frida_hook` operator-driven path). v2 candidates surfacing from Phase 10's v1 ship: (a) backend smali-signature autocomplete in the Trace mode form (typing `Lcom/example/Foo;->onClick(` queries the call graph for matching overloads), (b) cross-tab "Trace from here" button on the Manual Hooks mode's CallGraphView nodes, (c) `BehaviorAnchor`-aware in-graph overlay (cached anchors get a different render), (d) per-app TTL on cached anchors (current invalidation is sha-keyed cache directory + operator `force=true`), (e) operator-configurable confidence-tier thresholds via `trace.length_ratio_*` config keys, (f) inter-procedural slicing for the `ParamOrigin` case (the v1 punt is "method parameter — bypass via the calling site"; v2 walks the calling sites automatically). Next phase to tackle is operator-driven — Phase 4 CI is still parked; Phase 6 polish (scrcpy, richer logcat filtering, RAG attachments in chat) is deferred. Open Hook Lab v1 follow-up: **ISSUE-010** (self-host Monaco from a CDN-fetched bundle) remains v2 — the Trace mode UI stays Monaco-free per design.

### 2026-04-29 Phase 10 (sub-step 10.5): `trace_behavior` LLM-tier skill + per-app `trace.sqlite` cache
- outcome: Fifth code-bearing Phase 10 sub-step and the first one to wire the per-decision static layer (10.1–10.4) into a per-anchor LLM layer. New skill `androscan/skills/trace_behavior.py` (LLM-tier, `requires_confirmation=False` per DEC-022 / DEC-024 — read-only, no device touching, no APK mutation, no Frida injection) registered in `_SKILL_MODULES` (15 → 16 skills). Public API: `execute({"entry_method": <smali_id>, "app_id"?: <id>, "hops"?: <int>, "force"?: <bool>}, context) -> SkillResult`. Pipeline: resolve app/cache directory via the same fallback as `query_call_graph` / `resolve_ui_element`; cache lookup against `apps/<app_id>/.decompiled/<sha>/trace.sqlite` keyed by `(entry_method.smali_signature, hops)` (hit returns immediately with `[cached] ` text prefix); call-graph forward BFS depth ≤ `hops` (clamped to `trace.max_hops_hard_cap`) collecting up to `MAX_TRACE_METHODS = 30` internal callees with truncation flags; per-method `parse_decisions → slice_predicate_origins → classify_branch_outcomes → plan_bypasses` aggregating decisions + plans into the anchor; identify LLM workload (low_confidence_indices = `branch_outcome.confidence < LLM_RECLASSIFY_THRESHOLD = 0.6`; planless_keys = decisions where the deterministic planner returned no plans AND the heuristic outcome had non-zero confidence); **one LLM call per anchor** via `androscan.llm.client.complete(..., response_format="json", stream=False)` with the inline `build_trace_behavior_prompt(...)` emitting a strict JSON-schema system prompt + a deterministic per-decision rendering; re-classification application bumps `BranchOutcome.confidence` to `LLM_RECLASSIFY_CONFIDENCE = 0.75` and appends `"llm-reclassified: <reason>"` to verdict reasons (heuristic score preserved for audit); proposed-plan validation (template_id ∈ `{force_return_value, force_method_skip, force_string_compare_equal}` AND round-trips through `frida_hooks.render_by_id` — invalid plans silently dropped + logged); `partition_by_risk` splits into `default_plans` + `advanced_plans` per `config.trace_bypass_risk_max`; persist via `trace_cache.write_anchor` (INSERT OR REPLACE, idempotent, lazy DB init). **Fail-soft posture (mirrors `query_call_graph`):** missing app context, unbuilt decompile cache, missing call graph, or unresolved entry method → `success=True` with `data=None` and a clear `text` explanation; LLM transport failure / non-JSON response / response-not-an-object → return the deterministic-only payload with `[llm-skipped: <reason>]` in `text` (the static layer alone is enough for an operator to make progress without LLM augmentation). **Per-app SQLite cache** in new module `androscan/internal/trace_cache.py` — pure file-I/O over a `cache_dir: Path` argument with no Smali / call-graph imports so 10.6's REST routes can read directly without spinning up the full skill. Schema v1: `meta(key, value)` + `anchors(entry_smali_id, hops, payload_json, created_at, PRIMARY KEY (entry_smali_id, hops))`. Six-function API (`get_status` / `read_anchor` / `list_anchors` + `init_db` / `write_anchor` / `delete_anchor` / `clear_all_anchors`) plus the canonical `anchor_to_json(anchor) -> str` (sort_keys for diffing) + `anchor_from_json(str) -> BehaviorAnchor` decoder dispatching `PredicateOrigin` variants by `kind` discriminator. **`BehaviorAnchor` extended** in `androscan/analysis/trace_types.py` with three additive optional fields: `advanced_plans: tuple[BypassPlan, ...] = ()` (high-risk-filtered partition for 10.7's "Advanced" expander), `rationale: str = ""` (LLM-authored prose; empty when LLM call was skipped/failed), `low_confidence_decision_indices: tuple[int, ...] = ()` (instruction_index values flagged for LLM re-classification). All three default to safe empties so 10.1's minimal-shell construction pattern still works. **Two new config knobs** `trace_max_hops_default: int = 3` + `trace_max_hops_hard_cap: int = 6` wired through every loader site (`Config` dataclass + `Config.default()` + `CONFIG_FIELD_MAP` + `LIVE_RELOADABLE_FIELDS` + `_merge_from_yaml` via `_safe_int` + env-override branch + `with_overrides` clamp + `coerce_yaml_value`'s `int_fields` set + `global_config.yaml` documentation). **Tests.** **+22 tests** in `tests/test_trace_behavior_skill.py`: catalog/registration (3); input validation (2); fail-open paths (4 — bare run-folder, decompile not ready, call graph not ready, unknown entry method); closure walk + per-method static layer (2 — single-method closure for `Plans.gateBoolPredicate()V` emits 1 decision + 2 plans; closure walk writes to `trace.sqlite` + visible via the pure cache reader); cache hit + force=True (2 — second call hits cache without re-invoking LLM via an unreachable stub; `force=True` re-walks even when cached and overwrites the row); LLM round-trip (3 via monkeypatched `androscan.llm.client.complete` returning a stub `_LLMResponse` — re-classification lifts confidence + appends `llm-reclassified` reason; valid LLM-proposed plan round-trips through `frida_hooks.render_by_id` + surfaces in `data.plans`; invalid template_id silently dropped); LLM fail-soft (2 — `complete()` raises → deterministic payload + `[llm-skipped: transport: ...]` marker; non-JSON response → deterministic payload + `[llm-skipped: json-parse: ...]` marker); risk partitioning (1 — `trace_bypass_risk_max="low"` config routes Plan B's MEDIUM `force_method_skip` to `advanced_plans` while Plan A's LOW `force_return_value` stays in `plans`); config-knob smoke (3 — defaults match spec, both new knobs in `LIVE_RELOADABLE_FIELDS`, both in `CONFIG_FIELD_MAP`). All 22 tests pass via the same `_seed_apps_with_graph` pattern as `test_query_call_graph_skill.py` (real apktool tree, real call-graph build, real per-method static analysis end-to-end). Test stub helpers `_LLMResponse` / `_stub_llm_unreachable` / `_stub_llm_must_not_call` / `_first_decision_index` are reusable for future trace-behavior regressions. Full suite: **727 / 727 green** (was 705; +22). No FE changes, no schema changes outside the new `trace.sqlite`, no new optional dependencies, no edits to `smali_parser.py` / `dispatch.py` / `call_graph.py`.
- notes: The choice to put the prompt builder (`build_trace_behavior_prompt`) inline in the skill module rather than `androscan/llm/prompts.py` mirrors the established pattern in `verify_exploit_result.py` (per-skill prompts inline; `prompts.py` is the workflow-agent's chat catalog) — keeps the skill self-contained so future regressions are localised + tests can pin the prompt shape via direct module import. Per-anchor (vs per-decision) LLM calls are forced by DEC-024 — the per-decision pattern would blow up the token budget on real apps with 30 methods × 5 decisions each; one LLM call with all flagged gates rendered into a structured prompt is the contract. The choice to lazy-import `androscan.llm.client.complete` inside `_invoke_llm` (rather than at module load) is what makes test stubbing via `monkeypatch.setattr("androscan.llm.client.complete", ...)` work cleanly across all 8 LLM-touching tests; module-level imports would require setattr on `tb_skill.complete` instead which is fragile to refactors. The pure-SQLite `trace_cache` module is deliberately import-free of `smali_parser` / `call_graph` so 10.6's REST routes can read directly without spinning up the full skill — the same separation that lets the Settings tab's status card render the cache state without invoking analysis. The `LLM_RECLASSIFY_CONFIDENCE = 0.75` floor is between the heuristic moderate tier (0.85) and the weak tier (0.45) deliberately — operators reading the UI can distinguish "heuristic strong (1.0)" from "LLM refined (0.75)" from "heuristic moderate (0.85)" without ambiguity, and the gap is wide enough that 10.7's UI badge logic stays simple. The `force=True` re-walk is the operator's escape hatch for when a Smali file changed (rare — usually a re-decompile changes the sha and invalidates the cache directory entirely) or when the LLM had a bad day; explicit per-anchor TTL is deferred to v2 (the sha-keyed cache directory is good enough invalidation for v1).
- follow-up: Sub-step **10.6 — REST endpoint + tab shell + Hook Lab → Lab code rename** (new `androscan/web/trace_routes.py` factory-built via `build_trace_router(get_config, get_skill_runner, run_folder_root, web_apps_root)` and wired into `androscan/web/app.py`: `GET /api/trace/{app_id}/anchor?entry=<smali_id>&hops=<n>` reads `trace.sqlite` directly without invoking the skill, `POST /api/trace/{app_id}/anchor` forces re-trace via the skill, `DELETE /api/trace/{app_id}/anchor/{anchor_id}` invalidates one cache row; `HookLabTab.tsx` renamed to `LabTab.tsx`, `#/hook` URL hash → `#/lab` with one-shot redirect, 3-mode left-rail switcher `Trace | Manual Hooks | Graph` defaulting to `Trace`; per-tab chat-prompt key in `androscan/web/chat.py` `hook_lab` → `lab` with back-compat alias). 10.6 is the first sub-step where the UI surface starts shifting — the pure-SQLite layer + the `BehaviorAnchor` JSON contract from 10.5 are the two seams 10.6's routes will read against; the rename touches `androscan/web/chat.py` + `frontend/README.md` + every frontend component file with `HookLab` in the name.

### 2026-04-29 Phase 10 (sub-step 10.4): Bypass planner + three override Frida templates
- outcome: Fourth code-bearing Phase 10 sub-step. New module `androscan/analysis/bypass_planner.py` — pure deterministic synthesiser that maps a single `(DecisionPoint, BranchOutcome)` to zero or more template-bound `BypassPlan` instances. Public surface: `plan_bypasses(decision, instructions=(), label_index=None) -> tuple[BypassPlan, ...]` (the per-decision API; 10.5's `trace_behavior` skill loops over the closure and aggregates), `partition_by_risk(plans, threshold) -> (default_plans, advanced_plans)` (operator-facing risk-threshold split — 10.7's UI surfaces `advanced_plans` behind an "Advanced" expander per DEC-024), and the locked taxonomy primitives `VALID_RISKS = ("low", "medium", "high")` + `DEFAULT_RISK_THRESHOLD = "medium"` + `risk_at_or_below(plan_risk, threshold)` (strict total-order comparator with fail-soft fallback to `"medium"` on unknown values — mirrors how the loader handles `llm_provider`). **`BypassPlan` extended** in `androscan/analysis/trace_types.py` with three additive back-reference fields per DEC-024's data-model lock: `target_method: Optional[MethodRef]` (the method actually hooked — Plan A targets the predicate's source method, Plan B targets the gate method itself, Plan C targets the synthetic `Ljava/lang/String;->equals(Ljava/lang/Object;)Z` ref since the hook is on String.equals app-wide); `source_decision_method: Optional[MethodRef]` (the gate method the plan was generated for — the back-reference 10.5's persistence layer indexes by and 10.7's `BypassPlanCard` renders alongside); `source_decision_instruction_index: Optional[int]` (which `DecisionPoint` within `source_decision_method` — methods with multiple decisions need the disambiguator). All three default to `None` so 10.1's minimal-shell construction pattern still works (the planner always populates them; tests for the partitioner construct shells without them). **Three new Frida hook templates** under `androscan/adapters/frida_hooks/` — all template-bound, no free-form JS per DEC-024, all picked up automatically by the existing `TestRegistryFailClosed` parametrized fail-closed registry walk: **`force_return_value`** (params: `class_name` / `method_name` / `return_value_expr` / `event_label`; LOW risk; hijacks one named method and returns the operator-supplied raw JS literal — the original method body is NOT executed, side effects skipped — used by Plan A); **`force_method_skip`** (params: `class_name` / `method_name` / `return_descriptor` / `event_label`; MEDIUM risk; replaces every overload with a no-op stub returning the descriptor's natural zero — `false` for `Z`, `0` for `B/S/C/I/J`, `0.0` for `F/D`, `null` for `L...;` / `[...`, nothing for `V` — the only template that handles void gates honestly since Frida refuses non-void implementations on void methods — used by Plan B); **`force_string_compare_equal`** (params: `target_literal` / `event_label`; MEDIUM risk; hooks `String.equals(Object)` AND `String.equalsIgnoreCase(String)` app-wide and force-returns `true` only when EITHER the receiver OR the argument stringifies to `target_literal` — the literal-gated nature is what makes the override survivable; a plain `force_return_value` on `String.equals` would break class loading + hash table lookups + every internal use of equals — used by Plan C). All three follow the DEC-023 sub-step 4.4 contract verbatim: declared `HookTemplateParam` schema + JS `{{` brace escaping enforced by the registry walk + non-empty `pentester_summary_template` rendered alongside the JS so the Lab UI's confirmation card shows the operator what the script will *do* in plain English + auto-registered via `_TEMPLATE_MODULES` (bumped from 6 → 9 modules; `TestRegistryFailClosed::test_module_list_is_non_empty` updated `>= 5` → `>= 9`). **Plan emission rules (locked v1, justified inline in the planner module docstring so 10.5's LLM gate + 10.7's UI can reason about them from the same source):** for each `(decision, outcome)` where `outcome.confidence > 0` AND there's both a DENY *and* an ALLOW verdict in `outcome.verdicts` (no flip target → no plans), and where `decision.is_switch` is False (switches punt to v2 — per-case forced literals are non-trivial and the operator can hand-craft via Manual Hooks for these), three independent rules each may contribute one plan in deterministic order (Plan A → Plan B → Plan C): **Plan A — flip the predicate value** (only when `predicate_origin` is `MethodCallOrigin` AND the source method is NOT `String.equals` / `equalsIgnoreCase` — those route to Plan C — AND the planner can synthesise a literal for the source method's return descriptor): emit `force_return_value` targeting the *predicate's source method* with `return_value_expr` derived from `(decision.kind, allow-branch direction, source method's return descriptor)` via the locked `_SINGLE_REG_TRUE_TAKES_ZERO` table for `if-*z` opcodes and the `_zero_for_descriptor` / `_nonzero_for_descriptor` helpers — `Z → "true" / "false"`, `I/J/B/S/C → "1" / "0"`, `F/D → "1.0" / "0.0"`, `L...;` zero side → `"null"`, `L...;` non-null side → no plan (can't reliably synthesise an instance — operator authors via Manual Hooks), `V` → no plan (can't force-return on a void source method); risk LOW (precise — one named method, one literal). Two-register predicates (`if-eq` / `if-ne` / etc.) and switches are filtered upstream → no Plan A (the slicer's priority rule returns one operand; the other is unknown to the planner). **Plan B — force-skip the gate method** (only when `decision.method.return_descriptor == "V"`): emit `force_method_skip` targeting the enclosing gate method with `return_descriptor="V"`; risk MEDIUM (skips legitimate side effects too — operator must verify gate semantics before injecting). Useful for `void enforceLicense() throws SecurityException` style gates that throw on deny — skipping the method short-circuits the throw and lets execution continue past the call site. **Plan C — string-literal bypass** (only when `predicate_origin` is `MethodCallOrigin` AND source method is `String.equals` / `equalsIgnoreCase` AND a recoverable `const-string` literal exists in either branch's basic block OR via a backward scan over the gate method body up to the decision — gates frequently load the secret right above the equals call, e.g. `const-string v0, "LICENSE_VALID_42"` then `invoke-virtual {p1, v0}, String.equals`): emit `force_string_compare_equal` with `target_literal` filled from the const-string text verbatim; risk MEDIUM (app-wide `String.equals` interception, but only fires when the literal appears in either side — blast-radius limited to other gates comparing against the exact same literal, rare for non-trivial tokens). **New config knob** `trace_bypass_risk_max` (string, default `"medium"`) wired through every loader site in `androscan/config/loader.py`: `Config` dataclass + `Config.default()` + `CONFIG_FIELD_MAP[trace_bypass_risk_max] = ("trace", "bypass_risk_max", "ANDROSCAN_TRACE_BYPASS_RISK_MAX")` + `LIVE_RELOADABLE_FIELDS` (read per-call by 10.5's skill, no in-flight state to bounce) + `_merge_from_yaml` (parses new `trace:` section, fail-soft to `"medium"` on missing / empty / invalid) + env-override branch + `with_overrides` clamp + `coerce_yaml_value`'s `str_fields` set. `global_config.yaml` gets a new `trace:` section documenting the default + the valid enum + the fail-soft policy. Loader stays permissive (any string accepted at config-load time); planner / 10.5 skill validate against `{"low", "medium", "high"}` downstream with fail-soft fallback to `"medium"` (mirrors how `llm_provider` is handled — strings out-of-set don't blow up config loading; consumers handle the fallback). **Tests.** **+29 tests** in `tests/test_bypass_planner.py` (run end-to-end through the static layer `parse_classes → parse_decisions → slice_predicate_origins → classify_branch_outcomes → plan_bypasses` so each test is anchored to real Smali rather than synthesised data — catching contract drifts between layers that pure-unit tests would miss): eight Plan A tests (bool predicate emits force_return_true; int predicate emits force_return_one; ref-zero side emits force_return_null; ref-non-null side honestly skips Plan A; ConstOrigin / FieldReadOrigin / ParamOrigin / CompositeOrigin all skip Plan A; the tests use the existing `denyAllowSplit(Z)V` from 10.3's `Outcomes.smali` for the ParamOrigin coverage to demonstrate fixture reuse across phase sub-steps); two Plan B tests (void gate emits force_method_skip with `target_method == source_decision_method`; non-void gate skips Plan B but still emits Plan A); three Plan C tests (literal-found via backward scan emits force_string_compare_equal with `target_literal="LICENSE_VALID_42"`; literal-missing skips Plan C; String.equals routes away from Plan A so we don't double-emit a catastrophic blanket force-true); five planner-skip tests (no `branch_outcome` → empty; `confidence == 0.0` → empty via 10.3's `neutralWhenSymmetric`; both-deny → empty; switch via 10.3's `switchOutcomes` → empty; no-instructions arg skips Plan C honestly); seven risk-taxonomy tests (locked enum order; default threshold; total-order comparator inclusive; fail-soft on unknown; threshold-medium splits low+medium-default vs high-advanced; threshold-low pushes medium to advanced; threshold-high keeps everything default); one end-to-end integration test that the bool fixture's Plan A + Plan B output renders cleanly through `frida_hooks.render_by_id` (closes the loop on "planner output is consumable by the renderer without further massaging"); three config-knob tests (default value; live-reloadable membership; field map shape). **+3 new per-template smoke renders** in `tests/test_frida_hook_templates.py::TestTemplates` (each new template gets substring assertions on JS body + summary + sensitive-APIs metadata, mirroring the existing `entry_exit_log` / `scope_inspector` test shape). **+21 free** from the parametrized `TestRegistryFailClosed` walk auto-picking up the three new templates (7 walks × 3 modules = 21 new parametrized invocations covering the structural invariants — module exports TEMPLATE, id matches module basename, JS non-empty, summary non-empty, JS placeholders subset of declared params, summary placeholders subset, every required param appears in JS or summary, template renders with placeholder inputs). New fixture `tests/fixtures/trace_smali/smali/com/trace/Plans.smali` — 12 methods + constructor, every method shaped so the pipeline `parse → slice → classify → plan_bypasses` emits a fully-deterministic plan list (the general pattern: predicate value sourced via a recognisable means; false branch carries an ALLOW signal via `setResult` so the classifier emits a clean DENY/ALLOW pair; true branch carries a strong DENY signal via `throw` — except where the test specifically wants both-deny or symmetric-neutral). Full suite: **705 / 705 green** (was 649; +56 — 29 planner tests + 3 frida smoke renders + 21 free parametrized walks + 3 fixture-pickup deltas in earlier registry-walk parametrized tests). No FE changes, no schema changes, no new optional dependencies. No edits to `smali_parser.py` / `dispatch.py` / `call_graph.py` or any SQLite schema.
- notes: The choice to make Plan A's "non-null reference" direction honestly skip rather than emit a placeholder plan with `return_value_expr="null"` (which would take the *zero* side, wrong direction) preserves the planner's "if I emit a plan, it works" contract — operators who see a Plan A in the UI can trust it will flip the gate. The alternative (emit a `null` plan with a "WARNING: this returns null, you probably want a non-null instance" rationale) was rejected because it forces every operator to read the rationale carefully on every plan, defeating the deterministic-vs-LLM split. The MethodCallOrigin → Plan A path requires the source method to NOT be String.equals because a blanket `force_return_value` on `java.lang.String.equals` would break class loading + hash table lookups + every internal use of equals catastrophically; routing String.equals to Plan C (which is literal-gated) keeps the override survivable. The decision to skip switches entirely for v1 is conservative — switches *can* be bypassed via per-case forced literals on the predicate's source method, but the value-to-literal mapping per-case is non-trivial (you need to know which case label the operator wants to take, which is more nuanced than the if-* binary direction), and 10.5's LLM can recommend a hand-rolled hook for these. The decision to populate `target_method` / `source_decision_method` / `source_decision_instruction_index` as additive optional fields on `BypassPlan` (rather than bake them into a new `BypassPlanRef` wrapper) keeps 10.1's minimal-shell construction pattern working — tests for the partitioner construct empty-shell `BypassPlan(template_id="x", params={}, rationale="", risk="low")` instances without populating the back-references, and the existing minimal-shell pattern in `BehaviorAnchor.plans` doesn't need to change. The risk taxonomy is a closed three-value enum (`low / medium / high`) rather than a continuous numeric scale because operators reason about risk in discrete buckets and the UI renders threshold pickers as 3 radio buttons; v2 may add a `critical` tier if operators ask for it. The fail-soft on unknown risk values (coerce to `medium` on both sides of the comparator) prevents a typo in `global_config.yaml` from silently dropping all plans below `low` or surfacing all plans above `high`. The Plan A return-literal synthesis logic is the most consequential v1 design call — operators may want a *specific* non-zero value (e.g. a magic `0xDEADBEEF` int) rather than the generic `1`, but v1 picks the simplest distinguishable literal and lets the operator hand-edit before injecting (the `HookBuilder` UI in 10.7 will surface the rendered JS in Monaco for hand-editing, same flow as the Manual Hooks mode). The `force_string_compare_equal` template's literal-gated semantics (only fires when the literal appears in either side) is intentionally narrower than the spec's "force string equals to true" interpretation — the unguarded version was prototyped during 10.4 design and immediately failed to attach because the JVM's class loader threw on the first internal `String.equals` call.
- follow-up: Sub-step **10.5 — `trace_behavior` LLM-tier skill** (new `androscan/skills/trace_behavior.py`: read-only LLM-tier skill that walks the call-graph forward closure ≤ `MAX_TRACE_HOPS` and ≤ `MAX_TRACE_METHODS = 30` from a given entry method, runs `decisions.parse_decisions → slicing.slice_predicate_origins → branch_classifier.classify_branch_outcomes → bypass_planner.plan_bypasses` over each method in the closure, builds a populated `BehaviorAnchor` payload, and feeds it to the LLM **once per anchor** for re-classification of low-confidence gates + rationale authoring + bypass plan proposals for cases the deterministic planner returned empty; persists the populated anchor to `apps/<app_id>/.decompiled/<sha>/trace.sqlite`). 10.5 is the first sub-step that wires the per-decision static layer into the per-anchor LLM layer — the four enrichments from 10.1–10.4 (decision + slice + classification + plan) all flow into one LLM call per anchor.

### 2026-04-29 Phase 10 (sub-step 10.3): Heuristic branch outcome classifier
- outcome: Third code-bearing Phase 10 sub-step. New module `androscan/analysis/branch_classifier.py` — pure deterministic per-branch deny/allow/neutral classifier. Public API: `classify_branch_outcomes(method_decisions) -> MethodDecisions` (frozen-dataclass-replace, no input mutation; mirrors 10.2's slicer API exactly so 10.5's `trace_behavior` skill can chain `parse_decisions → slice_predicate_origins → classify_branch_outcomes` without intermediate bookkeeping); `classify_one_decision(decision, instructions, label_index)` for tests / 10.5's per-anchor walk where the caller may hold the decision separately. Two new dataclasses added to `androscan/analysis/trace_types.py`: `BranchVerdict(branch_label, verdict, score, reasons)` (one per `Branch` in the parent `DecisionPoint.branches`, same order; `branch_label` matches the corresponding `Branch.label` so consumers can join by index OR by label) and `BranchOutcome(verdicts, confidence, reasons)`. `DecisionPoint` extended with `branch_outcome: Optional[BranchOutcome] = None` (additive default → all 29 of 10.1's + 10.2's tests still pass unchanged; verified before any 10.3 test was authored). **Heuristic catalog (locked v1, justified inline in the module docstring so 10.4 / 10.5 / 10.7 can reason about confidence tiers from the same source):** strong DENY (-1.0): `throw vN` (exception unconditionally raised), `Ljava/lang/System;->exit` (process termination), `Landroid/os/Process;->killProcess` (process killed by PID). Strong ALLOW (+1.0): `L...;->setResult(...)` (Activity reports a result back; v1 doesn't distinguish RESULT_OK vs RESULT_CANCELED — both signal "this branch reports a result"; 10.5's LLM can refine via the const slice that precedes the invoke). Moderate DENY (-0.7): `L...;->finish\|finishAffinity\|finishAndRemoveTask()V` *only when no preceding `setResult` in the same basic block* (the legitimate "I reported a result and now I'm closing" allow-flow is suppressed via order-aware walk — `setresult_seen` flag tracked through the linear scan); `const-string vN, "..."` whose literal matches the curated deny-keyword regex. Moderate ALLOW (+0.7): `L...;->startActivity(ForResult)?(...)` (a new Activity is launched — the happy path continues). Weak DENY (-0.3): branch-length ratio ≥ 3:1 with shorter branch ≤ 3 instructions, applied *only* when both branches are otherwise neutral (the early-exit-gate pattern — short deny-branch + long real-work-branch — when no explicit signal fired). Restricted to 2-branch ifs only; switches use per-case verdicts directly without cross-branch ratio. **Verdict thresholds:** `score ≤ -0.5 → "deny"`, `score ≥ +0.5 → "allow"`, else `"neutral"`. **Confidence tiers (deterministic, pinned by tests so 10.5 / 10.7 can rely on them as a stable contract):** based on `max(|branch.score|)` across all branches — `≥ 1.0 → 1.00` (strong signal — operator can trust without LLM review), `≥ 0.7 → 0.85` (moderate / string-keyword signal — still above the 0.6 threshold, no LLM re-classification needed), `≥ 0.3 → 0.45` (weak signal only — *below* the `LLM_RECLASSIFY_THRESHOLD = 0.6` constant exported from the module; 10.5's `trace_behavior` skill will invoke the LLM to re-classify), else `0.00` (no signals — LLM re-classifies). Pre-classification, `branch_outcome=None`; post-classification it's *always* populated (even when verdicts are all neutral and confidence is 0.0) so 10.5 distinguishes "didn't run the classifier" (`is None`) from "ran it and found nothing" (`confidence == 0.0`). **Curated deny-keyword regex** extends DEC-024's spec `\b(premium|locked|jailbroken|unauthori[sz]ed|forbidden|denied|tamper|root|debug)\b` with two strict-additive improvements documented in the module docstring: (1) word boundaries (`\b...\b`) around the alternation prevent partial-word false positives — `rootView` does not match `root`, `debugger` does not match `debug` (matched via the explicit `(?:ger|ging)?` suffix instead); (2) optional `(?:ed|ing)?` / `(?:ger|ging)?` suffixes on `tamper` / `root` / `debug` so canonical pentest vocabulary like `rooted` / `tampered` matches (without these, the spec would miss "Device is rooted" — the textbook root-detection string). Both extensions are operator-facing improvements that strictly enlarge the match set; v2 may add more pentest vocabulary (`frida`, `xposed`, `emulator`, etc.) per operator feedback. **Basic-block walker** (`_basic_block`) walks forward from a branch target's first instruction (or `decision_instruction_index + 1` for fall-through / switch default) until hitting a basic-block terminator (`return*` / `throw` / `goto*` / `if-*` / `*-switch`); the terminator IS part of the returned block since `throw` is itself a deny signal. End-of-method-body acts as an implicit terminator. Unresolved labels return an empty block → 0-score → neutral verdict (defensive; well-formed apktool output never hits this). **Tests.** **+19 tests** (slightly over the planned ~18 budget) in `tests/test_branch_classifier.py`: pre-classify `branch_outcome` defaults to `None` on 10.2's output (baseline contract); `classify_branch_outcomes` returns a fresh `MethodDecisions` and doesn't mutate input (frozen-replace contract); per-signal assertions for `throw` / `System.exit` / `killProcess` (strong DENY, score -1.0, confidence 1.0) + `setResult` (strong ALLOW, +1.0, conf 1.0) + `startActivity` (moderate ALLOW, +0.7, conf 0.85); `finish` without `setResult` yields moderate DENY (-0.7, confidence 0.85, reason text contains "finish without setResult"); `setResult` preceding `finish` suppresses the finish signal (net score +1.0 — pure ALLOW; the verdict's `reasons` list explicitly does NOT contain "finish without setResult"); string-keyword matches for `rooted` (suffix-aware regex extension), `PREMIUM` (case-insensitive), `unauthorised` (British `s` not `z` — verifies the `[sz]` alternation); `rootView focused` is NOT matched by `\broot(?:ed|ing)?\b` (word-boundary contract — verdict NEUTRAL, score 0.0, no signal in reasons); length-ratio fixture (1-instruction true branch vs 8-instruction false branch, ratio 8:1) yields weak DENY (-0.3) on shorter side, NEUTRAL on longer side, confidence 0.45 explicitly *below* `LLM_RECLASSIFY_THRESHOLD` (asserted via the exported constant), method-level reason surfaces the cross-branch ratio; symmetric-no-signals method → both verdicts NEUTRAL/0.0/empty reasons + confidence 0.0; packed-switch with throw/setResult/neutral cases + neutral default classifies each case independently (case 0 DENY -1.0, case 1 ALLOW +1.0, case 2 + default NEUTRAL 0.0) with confidence 1.0 from case 0's strong throw signal; clean two-branch DENY/ALLOW split (throw on one side, setResult on the other) — both verdicts non-neutral, both scores ±1.0, confidence 1.0; **confidence-tier pinning test** asserts the four documented tier values (1.00 / 0.85 / 0.45 / 0.00) one-per-fixture so 10.5 / 10.7 can rely on them as a stable wire contract; `BranchOutcome` JSON round-trip via `dataclasses.asdict` + `json.dumps` (the contract 10.5's `trace.sqlite` cache + 10.6's wire format both depend on). New fixture `tests/fixtures/trace_smali/smali/com/trace/Outcomes.smali` (15 methods + constructor; each method exercises exactly one heuristic with a hand-crafted basic-block layout so test assertions are fully deterministic — predictable per-branch verdict + score + confidence). One pre-existing 10.1 test loosened (`summary.switches == 2 → >= 2`) since the new `Outcomes.smali` introduces a third packed-switch — original intent was "at least the packed + sparse from SwitchCases", lower bound keeps the assertion robust against fixture growth without losing coverage of the switch-counting path itself. Full suite: **649 / 649 green** (was 630; +19). No FE changes, no schema changes, no new optional dependencies.
- notes: The choice to populate `branch_outcome` for ALL post-classify decisions (even when all verdicts are neutral and confidence is 0.0) over leaving it `None` preserves the "did we run the classifier?" / "what did the classifier say?" distinction explicitly — 10.5 needs to tell "this gate hasn't been classified yet" apart from "this gate was classified and the heuristics found nothing actionable" because the second case still triggers LLM re-classification (different cache/work logic). The `setResult`-suppresses-`finish` rule is the most opinionated v1 choice; the alternative (additive scoring → setResult +1.0 cancels finish -0.7 = +0.3 → NEUTRAL by threshold) loses the operator's mental model of "this branch is reporting success cleanly". Order-aware suppression preserves that signal: if `finish` precedes `setResult` in the block (unusual but legal), the finish-deny signal still fires — the rule only suppresses the legitimate "report-then-close" pattern. The deny-keyword regex extension is justified inline in the module docstring; without the suffix variants, common pentest detection strings like "Device is rooted" / "App was tampered" would slip past the heuristic, defeating the whole point of the keyword check. The 4 confidence tiers are deliberately discrete (not continuous) — 10.5's LLM gate is a hard `< 0.6` threshold, and the UI in 10.7 will surface tier names ("strong" / "moderate" / "weak" / "none") rather than raw floats, so discrete values give the operator a stable mental model. Length-ratio thresholds (≥ 3:1 with shorter ≤ 3) are conservative — tighter than this generates noise in normal code; looser misses the canonical early-exit-gate pattern. Tests pin the boundary (1-instruction vs 8-instruction = 8:1 ratio; would also fire at 3-instruction vs 9-instruction = 3:1). v2 may make these per-app configurable via `trace.length_ratio_*` but v1 holds them as code constants for predictable behaviour across apps.
- follow-up: Sub-step **10.4 — Bypass planner** (new `androscan/analysis/bypass_planner.py`: maps `(DecisionPoint, BranchOutcome)` → zero or more `BypassPlan` instances each referencing one of `frida_hooks/`'s templates). 10.4 is the first sub-step where the static layer's three enrichments (decision + slice + classification) all pay off together — the planner needs all three to choose template + parameters: `branch_outcome.verdicts` to identify which branch is "deny" vs "allow"; `predicate_origin` (10.2) to pick the right hook template (`MethodCallOrigin` → `force_return_value`, `FieldReadOrigin` → `patch_field`, `ConstOrigin` → no plan since you can't patch a const without re-decompiling, `ParamOrigin` → `intercept_arg`, `CompositeOrigin` → no plan, too ambiguous for v1); `decision.method` (10.1) for the hook target. Three new templates ship with the same DEC-023 sub-step 4.4 contract (declared params + JS `{{` brace escaping + non-empty `pentester_summary_template` + the fail-closed `TestRegistryFailClosed` walk extends to them automatically). New tests in `tests/test_bypass_planner.py` (~20 tests — per-template plan emission + risk-threshold filtering + rationale text shape + new templates render under the existing registry-walk invariants).

### 2026-04-28 Phase 10 (sub-step 10.2): Backward slicing for predicate origin
- outcome: Second code-bearing Phase 10 sub-step. New module `androscan/analysis/slicing.py` — pure intra-procedural backward slicer that walks each `DecisionPoint`'s `predicate_registers` backward through the method body to its closest defining instruction. Public surface: `slice_predicate_origins(method_decisions, *, max_walk=64) -> MethodDecisions` (frozen-dataclass-replace, no input mutation) plus `slice_one_decision(decision, instructions)` for 10.5's per-anchor walk where the caller may hold the decision separately from the enclosing `MethodDecisions`. Five `*Origin` dataclasses added to `androscan/analysis/trace_types.py` per DEC-024: `MethodCallOrigin(method, invoke_kind)` for `invoke-* + move-result-*` chains; `FieldReadOrigin(field, is_static)` for `iget-*` (instance) and `sget-*` (static); `ConstOrigin(value, smali_op)` for `const-*` literal loads with the raw text preserved verbatim (`"premium"` not `premium`); `ParamOrigin(register)` for unmodified Smali parameter registers (walk reached start-of-method with a `pN` tracked); `CompositeOrigin(reason)` for arithmetic / `instance-of` / `cmp-long` / `array-length` / `new-instance` / casts / `move-exception` / etc. — v1 deliberately doesn't break composites down further per DEC-024's intra-procedural slicing limitation. Each origin variant carries a `kind` discriminator field (`"method_call"` / `"field_read"` / `"const"` / `"param"` / `"composite"`) so `dataclasses.asdict` + `json.dumps` round-trips the union variant unambiguously — 10.5's `trace.sqlite` cache + 10.6's wire format both deserialise via a `kind`-keyed dispatch with no custom encoder needed. `DecisionPoint` extended with `predicate_origin: Optional[PredicateOrigin] = None` (additive default → all 13 of 10.1's tests still pass unchanged; verified before any 10.2 test was authored). Slicer behaviour: follows `move(-wide|-object)?(/from16|/16)?` aliases transparently (replaces tracked register with source register and keeps walking — so `iget v1 → move v0, v1 → if-eqz v0` resolves to the underlying FieldReadOrigin, not a "move" composite); `move-result-*` looks back one step for the preceding `invoke-*` and emits MethodCallOrigin from its target signature (orphan `move-result-*` falls back to CompositeOrigin); skip-set excludes opcodes that don't define registers (`iput-*` / `sput-*` / `aput-*` / `invoke-*` / `goto*` / `return*` / `throw` / `monitor-*` / `check-cast` / `nop` / `fill-array-data` / `filled-new-array*`); walk exhaustion via `max_walk` (default 64) → `predicate_origin = None` honestly per DEC-024 rather than fabricating an origin (10.5 will then either flag the gate as low-confidence or invoke the LLM with the raw method body); walk reaches start-of-method with a `pN` tracked → ParamOrigin, with a `vN` tracked (uninitialised — defensive) → None. **Two-register predicate combination policy (DEC-024 v1):** for `if-eq` / `if-ne` / `if-lt` / `if-le` / `if-gt` / `if-ge`, the slicer slices each operand independently then combines via the priority rule `MethodCall > FieldRead > Param > Const > Composite > None`; the more-actionable side wins (operators typically want to manipulate the non-constant operand of value comparisons — a comparison against a constant encodes "compare against this value", and the value itself is rarely the bypass lever). Ties break to the LHS so source order produces a stable result. v2 may widen `predicate_origin` to a `tuple[PredicateOrigin, ...]` when both sides matter equally. **Small amendment to `decisions.py`:** `MethodDecisions` gained an additive `instructions: tuple[str, ...] = ()` field carrying the raw stripped text of every "real" instruction line indexed by `instruction_index`; populated via a tiny `_emit_instruction(raw)` helper in `_MethodState` that's now the single source of truth for both the index counter and the instructions list (every previous `self._instruction_index += 1` site was migrated). Memory cost: only methods with at least one branch retain their bodies — typical APK is a few hundred KB. This is what enables the slicer to be a pure function over `MethodDecisions` without re-reading files. **Tests.** **+16 tests** in `tests/test_decisions_slicing.py`: `predicate_origin` defaults to None on 10.1 output (pre-slice baseline contract); `instructions` is populated and points at `if-` / `*-switch` lines at the indices each `DecisionPoint` records; per-variant slicer assertions for MethodCallOrigin (invoke-static + move-result + if-eqz; `invoke_kind` and `MethodRef.smali_signature` round-trip), FieldReadOrigin (iget — instance) + FieldReadOrigin (sget — static) covered separately, ConstOrigin (`const/4 v0, 0x1`) + ConstOrigin (`const-string v0, "premium"` — string literal preserved verbatim including quotes), ParamOrigin (`if-eqz p1` with no preceding def — register `"p1"`), CompositeOrigin (arithmetic — `add-int`), CompositeOrigin (`instance-of`), CompositeOrigin (`move-exception` from a try/catch fixture); move chain `iget v1 → move v0, v1 → if-eqz v0` resolves to the underlying FieldReadOrigin (slicer follows aliases); two-register `iget vs const` returns FieldReadOrigin via the priority rule; two-register both-MethodCall returns the LHS via tie-breaking; `max_walk=2` exhaustion against a method with the def 12 instructions back returns None (and the same method with `max_walk=64` resolves to ConstOrigin — both assertions in one test); JSON round-trip across all 5 variants verifies the `kind` discriminator survives `dataclasses.asdict` + `json.dumps`. New fixture `tests/fixtures/trace_smali/smali/com/trace/Slices.smali` — ~13 methods + 4 helpers, hand-crafted so every origin kind has a known terminus and the test assertions can be fully deterministic (each method exercises exactly one slicer code path). Full suite: **630 / 630 green** (was 614; +16). No FE changes, no schema changes, no new optional dependencies.
- notes: The choice to put `instructions` on `MethodDecisions` rather than re-parse the file in the slicer is justified by the slicer being a pure function — it doesn't carry file-path state and never needs to know where the smali came from. Memory cost for a typical APK (a few hundred KB across all methods with branches) is tiny vs. the ergonomic + testability win of pure-function semantics. The two-register priority rule is the most opinionated v1 choice; the alternatives considered were (a) returning a tuple of origins (richer but changes the data-model contract before 10.3/10.4 wire it up), (b) returning Composite for any two-register comparison (loses information), (c) returning the LHS unconditionally (less informative when LHS is Const). The chosen rule preserves "one PredicateOrigin per DecisionPoint" while giving 10.4's bypass planner a usable lever in 90% of realistic gates. Two-non-Const cases still return *one* origin but the operator can drill into the source for the other operand if needed. The slicer's defaults (`max_walk=64`) are deliberately generous — most realistic gates resolve in <10 walked instructions. The walk cap exists to bound 10.5's per-anchor budget, not to filter out long-chain definitions.
- follow-up: Sub-step **10.3 — Heuristic branch outcome classifier** (new `androscan/analysis/branch_classifier.py`: deterministic per-branch classification into `deny` / `allow` / `neutral` based on basic-block content — System.exit / throw / Activity.finish detection, branch-length heuristics, string-constant regex scoring, weak-signal `setResult(RESULT_OK)` / `startActivity` patterns; emits `BranchOutcome(true_branch, false_branch, confidence, reasons)` per DecisionPoint; gates with `confidence < 0.6` flagged for LLM re-classification in 10.5). 10.3 is the first sub-step where the deterministic-vs-LLM split actually shows up — heuristics produce the confident classifications, LLM only sees the ambiguous ones.

### 2026-04-28 Phase 10 (sub-step 10.1): Decision-point extraction + platform-neutral data model
- outcome: First code-bearing Phase 10 sub-step. Two new modules under `androscan/analysis/`. **`trace_types.py`** ships the platform-neutral data model per DEC-024: `MethodRef` and `FieldRef` (Java-form names + raw Smali descriptors with `smali_signature` round-trip joining `call_graph.nodes`); `DecisionKind` (string-valued enum covering all 12 if-* opcodes + packed/sparse switch; `is_two_register_predicate` / `is_switch` accessors); `Branch(label, target_label: Optional[str])` (`None` = fall-through); `DecisionPoint` (the deliverable of 10.1 — `method` / `instruction_index` / `source_line` / `kind` / `predicate_registers` / `branches`); `BehaviorAnchor` and `BypassPlan` shipped as minimal shells locking field names 10.4 / 10.5 will populate without breaking the contract. **`decisions.py`** is a third Smali pass over the same apktool tree `smali_parser.py` already walks twice — `parse_classes` and `parse_invokes` are completely untouched. `parse_decisions(roots, classes) -> (list[MethodDecisions], DecisionsParseSummary)` emits one `MethodDecisions` per method with at least one branch (linear methods are excluded — callers that need full enumeration join against the existing `ClassDecl.methods` list). Switch data blocks (`:pswitch_data_0` + `.packed-switch <start>` / `:sswitch_data_0` + `.sparse-switch`) are parsed inline via a deferred-back-fill pattern (`_pending_switches` dict in `_MethodState`) so each switch decision's `branches` are populated with one `Branch` per case label + a `("default", None)` fall-through; the per-method `label_index: tuple[(label_name, instruction_index), ...]` lets 10.3's basic-block walker resolve any branch target without re-parsing the file. Instruction indexing is conservative — only lowercase-opcode lines advance the index; `.directives`, `:labels`, `# comments`, blank lines do not — so labels point at real next-instruction positions. Fail-soft on malformed switch blocks: `_DecisionParseError` caught at file level, surfaced via `DecisionsParseSummary.parse_errors`, rest of file keeps parsing (mirrors `smali_parser`'s posture). **No edits to `smali_parser.py`, `dispatch.py`, `call_graph.py`, or any SQLite schema** — additive only. **Tests.** New `tests/test_decisions_extract.py` (**13 tests**): MethodRef + FieldRef round-trip (including malformed-input `ValueError`), linear method excluded from output, summary counters match emitted decisions, `if-eqz` 2-branch shape with correct register + true-target + fall-through (modelled as `target_label=None`), all six zero-comparison kinds covered in one method, all six two-register kinds covered with predicate-register tuples, source-line attachment tracks most recent `.line` directive, label_index resolves branch targets to next-instruction indices, packed-switch emits one branch per case + default, sparse-switch preserves original key strings (`"case 0xa"` not `"case 10"`), multi-dex coverage via `smali_classes2/HiddenGate.smali`, and `DecisionPoint` round-trips through `dataclasses.asdict` + `json.dumps` (the contract 10.5's `trace.sqlite` cache + 10.6's wire format both depend on). Three new fixture .smali files under `tests/fixtures/trace_smali/` (`smali/com/trace/Gates.smali` exercising every if-* opcode + a realistic `checkRoot` / `openPremium` gate; `smali/com/trace/SwitchCases.smali` with packed + sparse switch coverage; `smali_classes2/com/trace/HiddenGate.smali` for multi-dex). Full suite: **614 / 614 green** (was 601; +13). No FE changes, no schema changes, no new optional dependencies.
- notes: The branch model uses `target_label: Optional[str]` where `None` means fall-through, rather than synthesising an explicit fall-through label. This is honest about the Smali model (fall-through is implicit — the next instruction wins) and lets the 10.3 basic-block walker join `target_label` against the `label_index` without a special-case for synthetic labels. The `MethodDecisions` aggregate excludes linear methods (no branches) entirely — keeping the result list tight for downstream consumers; the trade-off is that 10.5 has to join against the full method list when it walks a closure, but that join is already needed for the call-graph forward closure walk so no extra cost. `BehaviorAnchor` and `BypassPlan` shipped as minimal-but-locked shells (canonical field names only) per DEC-024's "additive-fields-OK" rule — adding new optional fields to a frozen dataclass after consumers are wired is harder than locking the canonical names now.
- follow-up: Sub-step **10.2 — Backward slicing for predicate origin** (new `androscan/analysis/slicing.py`: intra-procedural backward slice from each `DecisionPoint`'s `predicate_register` to its closest defining instruction; emits `predicate_origin: Optional[PredicateOrigin]` discriminated union of `MethodCall` / `FieldRead` / `Const` / `Param` / `Composite` / `None`). 10.2 is the consumer that proves out the `MethodRef` / `FieldRef` shape from 10.1 — if the round-trip needs a tweak it surfaces there before 10.3 starts depending on it.

### 2026-04-28 Phase 10 (sub-step 10.0): Planning checkpoint + DEC-024 + Hook Lab → Lab rename pin
- outcome: Docs-only planning checkpoint that locks Phase 10 — Behavior Trace v1 before any code lands. **DEC-024** added to `docs/DECISIONS.md` covering the workflow framing (client-side trust manipulation: *Anchor → Locate → Trace → Classify → Manipulate → Verify* — explicitly **not** source/sink reachability), the platform-neutral data model (`BehaviorAnchor` / `DecisionPoint` / `BypassPlan` / `MethodRef` / `FieldRef` ship in 10.1; Android is one adapter, iOS / desktop / embedded land later behind the same contract), the static-enumerate / LLM-interpret split (one LLM call per anchor — per-decision explicitly rejected), the template-bound bypass planner (no free-form LLM JS in v1; `generate_frida_hook` keeps that operator-driven path separate), the closure caps (`MAX_TRACE_HOPS = 3` default / hard cap 6 / `MAX_TRACE_METHODS = 30`), the intra-procedural slicing limitation (honestly surfaced via `predicate_origin: None` + a "trace may be incomplete" banner), the Cytoscape demotion (kept as Lab's `Graph` mode alongside `Trace` and `Manual Hooks`; demoted, not deleted), per-app SQLite at `apps/<app_id>/.decompiled/<sha>/trace.sqlite` (schema_version 1, mirrors the DEC-016 / DEC-018 / DEC-023 `<sha>`-keyed cache pattern), the `BypassPlan` risk taxonomy (`low / medium / high` + operator-configurable `trace.bypass_risk_max` threshold defaulting `medium`; high-risk plans hidden behind an "Advanced" expander), deterministic test discipline (LLM mocked at the boundary, fixture-driven static layer, ~80-test target), and the **Hook Lab → Lab** rename policy (forward-looking only; historical references in DEC-023 / Hook Lab v1 backlog / completed-task entries / ISSUE-010 / 011 / 012 / LIMIT-002 retain "Hook Lab" as accurate proper nouns; code rename `HookLabTab.tsx` → `LabTab.tsx` etc. deferred to sub-step **10.6** alongside tab routing changes). Cross-link bullets added: **DEC-016** related-docs gains DEC-024 forward pointer (Phase 10 reuses the call-graph SQLite store unchanged); **DEC-023** related-docs gains DEC-024 forward pointer (Phase 10 reuses every Hook Lab v1 substrate; rename is concurrent with this DEC). **`docs/TASKS.md` updates:** Active Task line points at Phase 10; Phase 8 / Phase 9 status rows stale-fixed (both shipped via Hook Lab v1 — were still flagged "Not started"); new Phase 10 status row added; **§ Phase 10 — Behavior Trace v1 — sub-step backlog** table inserted (10.0 → 10.8 with deliverable + dependency columns, 10.0 marked `[x]` in the same commit per DEC-024); brief Phase 10 — Behavior Trace section added at the same nesting as Phase 6 / 7 / 8 / 9 historical sub-sections; **Naming note** added under the § Interactive RE Workbench heading flagging the rename policy. **`docs/DESIGN_DOC.md` updates:** new Phase 10 stub added after Phase 9 with goal + workflow + deliverable summary + cross-link to DEC-024 and the Phase 10 sub-step backlog. **No code changes, no test changes, no frontend changes** — `git diff` shows only `.md` files.
- notes: This sub-step is the planning checkpoint itself — DEC-024 + the docs scaffolding it lights up. The intent (per DEC-024 follow-up) is that 10.1 → 10.8 then ship strictly linearly, one sub-step per Agent-mode session, with a brief Ask-mode planning checkpoint at the top of 10.6 to confirm the `BehaviorAnchor` JSON wire shape before the frontend in 10.7 starts depending on it (mirrors the 4.1 SQLite-schema checkpoint that locked the call-graph store before the routes API). The Hook Lab → Lab rename is intentionally docs-policy-only at 10.0; the actual code rename (`HookLabTab.tsx` → `LabTab.tsx`, `HookLabCodeView` → `LabCodeView`, `#/hook` URL hash → `#/lab`, the per-tab chat-prompt key in `androscan/web/chat.py`, `frontend/README.md`) lands in 10.6 alongside the tab routing changes — keeping Phase 10's docs-vs-code commits cleanly separated. Until then, code-level identifiers in DEC-024 and the Phase 10 sub-step entries reference the existing `HookLabTab.tsx` filename for accuracy. The Hook Lab v1 historical text across `docs/STATE.md`, `docs/TASKS.md` (Hook Lab v1 backlog table), `docs/KNOWN_ISSUES.md` (ISSUE-010 / 011 / 012, LIMIT-002), and `docs/SAFETY_AND_SECURITY.md` §12.6 is **not** rewritten — those entries describe completed work under the name it shipped under, which is the right historical record.
- follow-up: Sub-step **10.1 — Decision-point extraction** (new `androscan/analysis/decisions.py` extending `smali_parser.py` with one extra pass over the instruction stream emitting `DecisionPoint`s per Smali conditional branch + `androscan/analysis/trace_types.py` for the platform-neutral data model). Strictly linear sub-step rollout from there per § Phase 10 sub-step backlog.

### 2026-04 Phase 6 polish (UX step 3.5): Settings tab — global + per-app + status + diagnostics
- outcome: New top-level `Settings` tab (`#/settings`) covering global config, per-app overrides, live status, and diagnostics. Backend: `androscan/web/health_probes.py` (timeboxed pure-function async probes for adb/jadx/apktool/frida/Ollama/embed-provider/disk + per-app pm-path/foreground/UID/uiautomator/apk-sha-drift/RAG/decompile-cache; never raise; `{ok, label, ...}` shape); `androscan/web/per_app_settings.py` (atomic schema-versioned writes to `apps/<app_id>/app_settings.json`, `effective_settings(global_view, per_app)` merger, `apk_overrides_summary`); `androscan/web/status_routes.py` (`GET /api/status/global`, `GET /api/status/apps/{app_id}` — `asyncio.gather` + 3 s in-process cache; `invalidate_status_cache()` on every settings save/reset/reload); `androscan/web/settings_routes.py` (`GET/PUT /api/settings/global`, `POST /api/settings/global/raw`, `POST /api/settings/global/reset`, per-app GET/PUT/reset, `POST /api/settings/reload`; validates raw YAML server-side; atomic write-back via tempfile + `os.replace`; returns `restart_required` per field). `androscan/config/loader.py` grew `CONFIG_FIELD_MAP` (Config field → `(yaml_section, yaml_key, env_var)`), `LIVE_RELOADABLE_FIELDS`, `global_view_from_config`, `effective_sources` (returns `yaml`/`env`/`default` per field for source pills), `with_overrides`, `coerce_yaml_value`, `dump_to_yaml`, `save_raw_yaml`, `restore_defaults_yaml`, `validate_raw_yaml`, `read_raw_yaml`, `discover_config_path`. `app.py` refactored to keep the live `Config` on `app.state.config` (`_current_config`/`_set_config` helpers) so live-reloadable fields take effect without uvicorn restart; new routers read via callable providers. Frontend: `SettingsTab` + 4 sub-panels (`GlobalSettingsPanel` with form + raw-YAML editor + dirty state + reset; `AppSettingsPanel` with dropdown + override-or-inherit + force-with-warning reset; `StatusPanel` with cards + per-card `HealthDot` + "Refresh now"; `DiagnosticsPanel` with raw API JSON + `/api/settings/reload`). New `HealthDot` in the global header polls `/api/status/global` every 30 s and rolls up to a single green/yellow/red dot that deep-links to Settings on click. Two new API clients (`api/settings.ts`, `api/status.ts`) and ~300 lines of new CSS in `App.css` (`.source-badge`, `.env-lock`, `.restart-pill`, `.status-card`, `.health-dot.{green,yellow,red}`, `.settings-nav`, `.yaml-editor`). Tests: `tests/test_health_probes.py` (subprocess + urllib mocks; 20 cases), `tests/test_per_app_settings.py` (load/save/reset/merge/atomic), `tests/test_settings_routes.py` (integration). All Python tests green; `tsc --noEmit` + `vite build` clean.
- notes: One bug discovered during testing — the integration test was monkeypatching `androscan.web.health_probes.probe_adb_version`, but `androscan.web.status_routes` imports `probe_adb_version` *by name*, so the patch never reached the consumer. Fix: monkeypatch the **consumer** module (`androscan.web.status_routes`). Documented inline in `test_settings_routes.py` and in DEC-021. The "force-with-warning" per-app reset semantics + the `restart_required` pill for non-live-reloadable fields were both explicitly user-confirmed during design.
- follow-up: (P2) Migrate hot-path handlers (`/api/llm/info`, chat, RAG, decompile) to read from `app.state.config` so live reload covers their fields. (P2) "Compare to global" diff view in the per-app panel. (P2) Persist raw-YAML draft to sessionStorage. (when Hook Lab starts) Add a "Hook Lab readiness" rollup probe.

### 2026-04 Phase 6 polish (UX step 3, partial): Lane-1 RAG indexer
- outcome: New `androscan/rag/` package (`chunking.py`, `embed.py`, `index.py`, `search.py`) implementing a per-app SQLite vector index of method-level chunks over the jadx decompiled sources. Brace-balanced chunker for `.java`/`.kt` (no tree-sitter). `EmbedProvider` protocol with `FastEmbedProvider` (default ONNX local), `OllamaEmbedProvider` (HTTP), and `HashProvider` (deterministic, test/no-deps fallback). Per-app store at `apps/<app_id>/.decompiled/<sha>/rag.sqlite` (WAL, packed `float32` blobs); brute-force cosine top-k (numpy when present, pure-Python fallback). Endpoints `GET /api/rag/{app_id}/status`, `POST /api/rag/{app_id}/rebuild`, `POST /api/rag/{app_id}/query` in `androscan/web/rag_routes.py`; jadx completion auto-schedules a daemon-thread RAG build via `schedule_rag_build_after_decompile`. New `llm`-tier skill `search_decompiled_sources` (query, top_k, package_prefix, file_substr) — advertised in prompt catalog, fail-open. Inspect-tab chat (`androscan/web/chat.py::_enrich_inspect_with_rag`) appends top-k chunks as `code` attachments before guardrails run; fail-soft when RAG is unavailable. `Config` extended with `rag_embed_provider` / `rag_embed_model` / `rag_top_k_default`; `global_config.yaml` adds `rag:` section; env overrides `ANDROSCAN_RAG_*`. New `pyproject` extra `[rag]` (fastembed, numpy). New tests: `tests/test_rag.py`, `tests/test_rag_integration.py`, plus three RAG endpoint tests in `tests/test_web.py` (197 tests total, all passing).
- notes: Click-to-code backend was already in place via `POST /api/inspect/map` + `androscan/web/inspect_map.py` (foreground activity + uiautomator dump + handler grep) and persistent decompile cache (`/api/decompile`, `/api/code/{tree,file}`); package-scoped logcat (with stable Linux-UID filtering) and mirror online/offline (`/api/device/status`) were also already implemented. The remaining backend gap was the `resolve_ui_element` LLM skill — see the next entry. Multimodal chat attachments (PDF/CSV/screenshot) are deferred to after Hook Lab. `sqlite-vec` is reserved as a future drop-in replacement for the brute-force scan.
- follow-up: Done in two follow-up entries below — `resolve_ui_element` LLM skill + `/api/inspect/map` fusion (backend), and the Inspect-tab frontend wiring entry. Next is UX step 4 (Hook Lab — Smali graph + Frida).

### 2026-04 Phase 6 polish (UX step 3, partial): `resolve_ui_element` LLM skill + `/api/inspect/map` fusion
- outcome: New `androscan/skills/resolve_ui_element.py` — `llm`-tier deterministic fuser that takes the element + foreground activity + raw handler candidates from `inspect_map.find_handlers` and produces a single `best` answer with reasoning, plus a ranked `alternatives` list. Score model: handler-kind base (`findViewById` 1.00 > `onClick_near` 0.80 > `compose_id` 0.60 > `reference` 0.20 > rag-cosine), additive bonuses for foreground-activity match (+0.50), activity-named files (+0.10), and an early-line decay bonus (≤+0.10 for line 1, → 0 at line 200). Optional Lane-1 RAG enrichment: synthesises a free-text query from `text` > `content_desc` > short resource id, fetches top-k chunks, and feeds them through the same scorer (so a clean `findViewById` in the foreground activity always beats an arbitrary semantic match). Pure-function `resolve()` is reused inline by `POST /api/inspect/map` (`androscan/web/app.py`) which now returns a `resolution` block (`{best, alternatives, rag_hits, reasoning}`) so the UI gets one ranked answer without an LLM round-trip. Skill is fail-soft on missing app dir / decompile cache / embed provider. Registered in `androscan/skills/__init__.py`. New tests: `tests/test_resolve_ui_element.py` (13 tests — pure helpers, deterministic scoring, foreground boost, early-line tie-break, RAG enrichment with `HashProvider`, fail-soft, registry path); `tests/test_web.py::test_api_inspect_map_returns_element_and_candidates` extended to assert the new `resolution` block. **210 tests total, all passing.**
- notes: This closes the *backend* of Phase 6 step 3. The frontend wiring landed in the next entry below.
- follow-up: Inspect-tab frontend wiring (done — see next entry); then UX step 4 (Hook Lab — Smali graph + Frida).

### 2026-04 Phase 6 polish (UX step 3, final): Inspect-tab frontend wiring for the fuser
- outcome: Surfaced the `resolution` block from `POST /api/inspect/map` end-to-end in the Inspect tab UI. Extended `androscan/web/frontend/src/api/inspect.ts` with `Resolution`, `ResolutionCandidate`, `ResolutionRagHit` types. New sub-components in `ElementMappingPanel.tsx`: `BestBanner` (regex/RAG `SourceBadge`, kind pill, score, reasons grid, snippet, "Open in Code browser"), `ResolutionAlternatives` (collapsible details with per-alt source badge + score + open button), `RagHitsList` (collapsible details with synthesised query and per-hit class.method label). `InspectTab.handleTap` now prefers `resolution.best` over `candidates[0]` when auto-opening the Code Browser, seeds `scrollTarget` + `highlightRange` (via `requestAnimationFrame` so re-clicks still fire), and propagates `best.method_name` so the method-emphasis label is correct. Chat attachments memo now appends a `best_handler` JSON block (file, line, class, method, source, score, reasons) so the LLM sees the fused pick alongside the element + candidate list. New CSS: `.source-badge` (deterministic = green, rag = warn), `.best-banner` (accent border + soft background, reasons grid), `.resolution-alts` / `.rag-hits` collapsibles. Vite production build clean (308 modules, 32 KB CSS, 393 KB JS); `tsc --noEmit` clean; 210 Python tests still passing.
- notes: The rest of the Inspect-tab frontend (4-column resizable layout, mirror with status dot, click-to-map, scoped logcat, adb shell, Code-browser tab with `CodeView`) was already in place before this entry — only the fuser-output visualisation was missing. With this, Phase 6 step 3 is functionally complete (chat enrichment, RAG indexer, click-to-code backend, fuser skill, and full UI surface).
- follow-up: UX step 4 (Hook Lab — Smali graph + Frida overlay + scope/hooks).

### 2026-04 Phase 6 (initial): RE Workbench web shell
- outcome: `androscan/web/` FastAPI app with CORS; REST `/api/health`, `/api/projects`, `/api/projects/{app_id}/runs`, `/api/dossier/{app_id}/{run_ts}`, `/api/findings/{app_id}/{run_ts}`, `POST /api/input/tap`; WebSockets `/ws/mirror` (adb screencap PNG polling), `/ws/logcat`; config `web_host`, `web_port`, `web_screencap_interval_ms` + env overrides; CLI `--serve`, `--web-port`; optional `--apk … --serve` after run. Vite + React + TypeScript UI under `androscan/web/frontend/` (build → `static/`, gitignored). Tests in `tests/test_web.py`. Dependencies: fastapi, uvicorn, websockets; dev httpx for TestClient.
- notes: scrcpy path and logcat filter UI deferred; `GET /` returns JSON how-to when `static/` not built.
- follow-up: Phase 6 polish; Phase 7 click-to-code.

### 2026-03-30 Phase 4 partial: Error handling + docs cleanup
- outcome: Broader error handling across config, LLM client, ADB skills, workflow, and CLI. Config: explicit --config fails on missing/invalid file; type coercion with clear errors. LLM: resp.json() wrapped; empty content retried; non-404 HTTP errors show body detail; is_ollama_available returns diagnostic detail. ADB: TimeoutExpired caught in all subprocess calls (app_env_check, run_exploit_command, capture_signals). Workflow: generate_report result checked; disk errors on meta/observations caught; LLM parse failures logged. CLI: top-level exception handler; report read warnings. Misc: skills registry import errors logged; vuln_signals_config corrupted JSON caught; cache store disk errors caught; generate_report disk errors handled. Docs: DESIGN_DOC, ARCHITECTURE, DECISIONS, PROJECT_BRIEF, README, KNOWN_ISSUES, AI_ENGINEERING_CONSTITUTION updated for Phase 5, three-tier skills, and stale references fixed.
- notes: CI (pytest on push/PR) remains parked.
- follow-up: CI setup; provider signal stubs; second vulnerability module.

### 2026-03-30 Phase 5: Exploit verification
- outcome: Exploit verification on emulator + ADB. 5 exploit-tier skills (app_env_check, build_exploit_command, capture_signals, run_exploit_command, verify_exploit_result). Template catalog for 5 component profiles (exported_activity, exported_service, exported_receiver, exported_provider, deep_link). Vuln–skill–signal profile JSON. Before/after signal capture (volatile then non-volatile). LLM-based verification. Report generated after verification with verified flag, reasoning, and artifact refs. Artifacts per hypothesis under exploit_verification/<module>/<hyp_id>/. All 10 sub-tasks complete.
- notes: `exported_provider` has 2 stub signal types (content_provider_query, app_data_snapshot); logcat and exploit command are real. RAG for exploit templates deferred to backlog.
- follow-up: Unpark Phase 4 (CI, hardening) or pick from backlog (provider stubs, second vulnerability module).

### Phase 3: First vertical slice (exported components)
- outcome: Real extraction (apktool), real Ollama client, real prompts and skills catalog, evidence_ref validation, run artifacts (report.json, run_meta.json, run.log, observations.json), skill results cache (get_decompiled_class keyed by resolved class name). get_decompiled_class and get_decompiled_method real via jadx. 54 tests; mock LLM in CI.
- notes: All five sub-tasks done. Integration test with fixture APK parked in backlog.
- follow-up: Phase 4 (harden and extend, CI).

### 2026-03-13 Phase 2: Build skeleton
- outcome: Repo layout, pyproject.toml, CLI (androscan.py with --apk, --task multi-valued, --output), config, dossier model, extraction stub, LLM stub (client, prompt builder, parser), run folder creation, stub skills, workflow with multi-turn loop, report.json writing. 14 tests (import, config, extraction, LLM, CLI parsing, workflow integration).
- notes: Sub-tasks 1–7 (project setup, config, dossier+extraction stub, LLM stub, workflow+run folder, CLI wiring, minimal tests) completed. All stubs; Phase 3 will add real extraction and real LLM.
- follow-up: Phase 3 (first vertical slice).

### 2026-03-12 Phase 1: Finalize architecture
- outcome: DESIGN_DOC.md is the single source of truth for MVP (architecture, repo structure, dossier schema, LLM I/O schema, prompts/skills, roadmap, risks, first vertical slice).
- notes: Phase 2 (skeleton) completed 2026-03-13.
- follow-up: Phase 3 (first vertical slice).

### 2026-03-12 Added agent protocol and starter docs
- outcome: onboarding and task-loading docs added
- notes: architecture and design docs still pending
- follow-up: create first active engineering task

Keep this section concise.

---

## Task writing rules

Each task should be:
- specific
- scoped
- reviewable
- testable
- bounded in time and complexity

Avoid tasks like:
- “improve architecture”
- “make app better”
- “add security”
- “clean everything up”

Prefer tasks like:
- “add normalized finding model for first feature module”
- “implement adapter wrapper for tool X behind interface Y”
- “add negative tests for malformed artifact input in module Z”

---

## Phase and task list rules

**So other IDEs and agents know where to take over:**

1. **List sub-tasks explicitly.** For every phase or multi-step task, the implementation plan in this file MUST list sub-tasks explicitly (numbered or bulleted), not only in prose. Each sub-task should be verifiable on its own.

2. **Update status as soon as a sub-task is completed.** When a sub-task is finished (code done, tests pass, behaviour verified), update this file in the same work session:
   - Mark the sub-task done (e.g. add `[x] Done` or `— [x] Done` next to the heading, or update a status table).
   - Do not leave a phase "in progress" without reflecting which sub-tasks are already done.

3. **Keep a short status overview.** For the active phase, keep a compact status table or checklist (e.g. "Sub-task status") at the top of the implementation plan so the next agent can see at a glance what is done and what is next.

4. **External tool availability.** When adding or changing code that uses an external tool (apktool, jadx, etc.), the implementation must check tool availability (e.g. `shutil.which(cmd)`) and handle missing tools without crashing (return a clear result; do not raise raw subprocess/OS errors). See `docs/CONVENTIONS.md` §4 External tool availability.

---

## Default execution rule

Unless explicitly instructed otherwise:

- do one task at a time
- complete it properly
- add tests
- update docs
- then move to the next task